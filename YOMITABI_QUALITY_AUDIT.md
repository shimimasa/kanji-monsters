# 漢字ヨミタビ 品質監査

監査日：2026-09-06（JST）  
対象：ルートの現行実装、HEAD `d58c4ce`  
視点：ゲームディレクター、子ども向け教材設計者、UX / Visual Director、シニアエンジニア

## 公開判断

**現状のまま子ども向け公開版v1とすることは勧めない。最優先は、記録を失わないこと、間違えた字に正しく再接触できること、子どもが自分で開始・解答・中断・再開できること。**

「漢字を読んで旅をし、モンスターを集める」という核は伝わる素材がある。50音パッド、ふりがな、文字拡大、練習への退避、誤入力への配慮、セーブスロットも既にある。機能を増やす段階ではなく、これらが一貫して正しく働く状態に仕上げる段階。

今回の指摘は **P0：2件、P1：18件、P2：15件、P3：2件、計37件**。4つの立場を分け、複数の立場に影響する同じ指摘は参照で結んだ。件数には静的な問題と品質評価を含み、37件すべてを実機で再現したという意味ではない。難易度はG06、上達実感はG07・U08、「勉強をやらされている感覚」はG08に追加評価を記載した。

教材として現在の中心は、**提示された漢字から、登録された読みの一つを思い出して、かなで入力する練習**。読む行動が攻撃・回復・収集へつながる核は成立している。一方、漢字の総合的な習得や、自力での読みの定着まで保証する記録にはなっていない。既存の学習支援を残し、採点・復習・表示の意味を揃えることが完成品への近道になる。

| 優先度 | 定義 | 公開判断への扱い |
| --- | --- | --- |
| P0 致命的 | 既存の記録を失うなど、取り返しのつかない障害 | 公開を止めて修正する |
| P1 子どもへ公開する前に直す | 起動・学習記録・入力・主要操作・読みやすさを損なう | v1の公開条件にする |
| P2 品質を明確に上げる | 基本機能は成立するが、見た目・テンポ・一貫性・運用に明確な弱点がある | v1で直す範囲を最後の10項目に限定する |
| P3 将来改善 | 大きな整理や継続的な磨き込み | v1のための全面改修に広げない |

## 調査方法と限界

- プロジェクトの構成、`PROJECT_EVIDENCE.md`全文、README、設計・改善・スモークテスト文書、`src/`の主要画面と共通処理、`public/data/`、画像素材、HTML/CSS、ビルド・配信設定を調査した。全ファイルを逐語校閲したという意味ではない。過去の監査文書の指摘を、そのまま現状の不具合として転記してはいない。
- 現行コードを一時ディレクトリへ本番ビルドし、既存のステージID整合検査、全JSONの構文検査、IDの重複・参照検査、画像ファイルの存在・容量確認を実施した。
- セーブ消失と音声切替は、実際のモジュールをNode.jsで読み込み、メモリ上のStorage／Audio／フレーム処理の代用品で障害を再現した。ユーザーのブラウザーの保存データやFirebaseには触れていない。
- 追加で、チェックポイントの保存・読込、復習予定の再誤答時の挙動、復習画面の連続Enterを実モジュールと依存先の代用品で確認した。戦闘内再出題の候補除外は、実装関数を切り出して確認した。いずれも実機での通しプレイとは区別する。
- **接続可能なブラウザーが0件だったため、完成画面のスクリーンショット、実際のタップ、IME、実端末のFPS、音の試聴は未実施。** 視覚面は素材の実見と描画コードに基づく評価。操作上のリスクを、実機で再現済みとは扱わない。
- iPad Safari／iPhone Safari／Android Chromeの実機差、公開先のFirestoreルール、旧Service Workerが残った実端末、全漢字・例文の教育的正確性は未確認。この監査はそれらの合格証明ではない。
- アプリのコード・データ・素材は変更していない。監査の成果物はこのMarkdownのみ。ビルドは既存の`dist/`を上書きしていない。

以下の「再現」は模擬環境による再現、「静的確認」はコード／データで確認した事実、「評価」はゲーム・教材・見た目に関する判断を指す。子どもの楽しさ、理解率、定着率、実際の最初の30秒は未測定。

## PROJECT_EVIDENCE.mdとの照合

[PROJECT_EVIDENCE.md](PROJECT_EVIDENCE.md)にある実装経緯と確認済み事項を基礎資料にした。ただし、「機能が実装され、過去の正常系テストを通ったこと」と「今回の版が公開品質に達したこと」を分ける。

| 証拠文書の内容 | 現行実装との照合・今回の判断 |
| --- | --- |
| 2,136字、通常88＋追加12ステージ、多数の素材 | 量は強み。ただし別字のID重複9組と参照切れがあり、量だけで内容の正確性は保証できない（E07） |
| 5体目の旗、セーブスロット、バックアップ、クラウド復旧の実装・確認 | 同一ページ内の再挑戦と、保存してページを閉じた後の再開は別。旗の永続化漏れ、保存失敗・空の復元・起動順の問題を追加確認した（G03、E01〜E05） |
| SRS、誤答の再出題、入力の「おしい」判定 | 支援は既に存在する。再誤答時の予定更新、復習画面の採点、ヒント使用時の学習評価は揃っていない（T02〜T04） |
| 例文出題は小学1年生に対応、読みの限定は既定OFF | 現状の範囲として扱う。全学年の例文追加や難しい出題制約の復活をv1の必須事項にしない（T01） |
| 世界ステージの出題範囲、教材・説明文の一部に未確認事項 | コード側でも世界のステージ別プールが使われていないことを確認した（G05）。説明文全件の真偽は今回未校閲で、誤り確定とはしない |
| オフライン／PWAからの撤退 | 意図した現行方針。PWA機能不足を新規開発要求に変えず、名前・説明・旧版からの更新だけ揃える（E14） |
| 過去の動作確認、ビルド成功 | 歴史的な確認記録として尊重する。今回の本番ビルドも成功したが、保存障害・採点の意味・実端末品質の合格を代替しない |

公開先のFirestoreルールや実際に有効なホスティング設定は、リポジトリだけでは確定できない。漏えいがあるとは断定しない。クラウド保存を提供する公開構成では、他ユーザーの記録を読み書きできないことを実設定で確認してから公開する。認証とユーザーIDに基づく制御の確認箇所は[Firebase公式：Firestoreのルール条件](https://firebase.google.com/docs/firestore/security/rules-conditions)を参照。

## ゲームディレクター

**総評：読む→反応が返る→敵を倒す→旅が進む、という遊びの核はある。完成度を下げているのは遊びの種類の不足より、開始前の手間、中断の信頼性、復習への行き先、進行と出題範囲のずれ。**

| 評価対象 | 現状の強みと課題 | 判定先 |
| --- | --- | --- |
| 最初の30秒 | 地図とキャラクターは目的を伝える素材になるが、初回は起動待ちと複数の選択が最初の一問に先行する | G01／E06・E11 |
| 操作理解 | 50音パッドと案内がある。二度押しで開始する地図操作、Enter中心の案内、縮小表示が理解を妨げる | G01／U01・U04 |
| ゲームループ | 読みが攻撃・回復へ直結し、戦闘・結果・地図へ戻る流れは成立している | G02／T01 |
| テンポ | 正解後の待ち、敵行動後の待ちが積み重なる。演出時間も端末のFPSに依存する | G02／E13 |
| 正解時の快感 | 即時のSE、読み上げ、ダメージ、撃破・収集の反応がある。視線の集中と入力復帰を整える余地がある | G02／U04 |
| 失敗時の納得感 | 「おしい」支援、正答表示、練習への退避がある。同じ入力が画面によって違う扱いになる | T03／U06 |
| 戦闘 | 音訓と弱点、攻撃・回復の選択が読みへ意味を与える。操作や演出の不整合を先に除くべき | T01／E08・E09 |
| ステージ進行 | 進む目的はあるが、中断地点が永続化されず、復習入口からボーナスの解放条件を迂回できる | G03・G04 |
| 達成感 | 捕獲・クリア・マスター表示は既にある。クリア数二重加算と過大な学習評価が信頼を損なう | E10／T04・T05 |
| リプレイ性 | 未読の読み、復習、収集が再訪理由になる。世界では地域内のステージを変えても同じ学年全体から出題される | G05／T02 |
| 難易度 | 自分の学年から始める子にも、全ステージ一覧の後半ほど敵HPが増える。漢字の難しさと戦闘の長さが同時に上がる | G06 |
| 上達実感 | 初正解・マスター・育成の反応はあるが、以前つまずいた字を今回読めたという具体的な変化が集計に隠れる | G07／T04・U08 |
| 勉強をやらされている感覚 | 行き先や練習を選べる一方、不足回数の案内と練習完了後の自動継続が、本人の満足や終了判断より先に来る | G08 |

### G01 — P2：最初の一問までの選択と説明が多い

**対象：最初の30秒、操作理解。静的確認・体験設計上の評価。**

初回経路はタイトル→名前入力→コース→地域→ステージ→準備→戦闘。ステージは一度選び、同じ対象をもう一度押して開始する。名前入力確定にもクラウド初期化待ちが入る。初めての子には「まだ遊べていない時間」に複数の操作規則を覚える負担がある。起動の重さはE11、通信による停止はE06として別途修正が必要。

**最小修正：** 既存の初期選択・案内を使い、最初に押す対象と「選択／出発」の違いを明確にする。初回案内を最初の一問に必要な操作へ絞る。新しい導入モードを作る必要はない。

**完了条件：** 対象年齢の子の初回利用で、成人が操作を代行せず最初の問題へ進める。最初の30秒で何が表示され、どこで止まったかを記録する。30秒以内に解答できたという実測結果は、現段階ではない。

根拠：`src/screens/titleScreen.js:485`付近、`playerNameInputScreen.js:174`付近、`courseSelectScreen.js`、`regionSelectScreen.js`、`stageSelectScreen.js:1325`、`src/tutorial/tutorialData.js`。

### G02 — P2：正誤の反応から次の操作までの待ちと、答えを見る時間が不均衡

**対象：ゲームループ、テンポ、正解時の快感、戦闘。静的確認・評価。**

通常戦闘は正解にSE・読み上げ・ダメージを返す点が良い。一方、倒し切らなかった通常正解でも次の問題まで約1.3秒を待つ。誤答経路は約1.3秒後の敵行動、その後約1.7秒の次問待ちがある。復習画面は複数の正しい読みを表示しても1秒で次へ進む。操作待ちは長い箇所があるのに、読み直したい場面は短い。

**最小修正：** 既存演出の重複待ちを整理し、入力可能に戻ったことを見た目と一致させる。誤答後は答えを確かめてから次へ進めるよう、既存の決定操作や表示時間を調整する。一律高速化で読む時間を削らない。

**完了条件：** 正解直後の反応と入力復帰が分かり、決定連打で先へ飛ばない。誤答した子が答えを確認でき、SE・読み上げ・次問表示が競合しない。楽しさや最適秒数は実プレイで確かめる。

根拠：`src/screens/battleScreen.js`の`onCorrect`・誤答後タイマー（`:4795`以降、`:5321`付近）、`src/screens/reviewStage.js:129`付近。

### G03 — P1：5体目の旗がセーブに入らず、ページを閉じると再開地点を失う

**対象：中断・再開、ステージ進行、達成感。実モジュールで保存・読込を再現。**

5体撃破時に`stageProgress[stageId].checkpoint = 5`として保存を呼び、戦闘開始時はこの値を読む。しかし`saveGameData()`が保存する進行はクリア済みIDなどに限られ、チェックポイントが含まれない。読込側にも復元処理がない。同じページ内での再挑戦では旗が効いても、終了して翌日戻る使い方では失う。

メモリStorageで実際の`gameState.js`と`saveData.js`を使い、旗5を保存→メモリの進行を初期化→読込すると、保存された進行にも復元後にも旗が無いことを確認した。

**最小修正：** 既存の旗を正規セーブへ保存・復元し、スロット・バックアップ・クラウドで同じ意味にする。全戦闘状態を逐次保存する新機能へ広げる必要はない。

**完了条件：** 5体目の旗を立てて終了→ページ再読込→6体目から再開できる。クリア後の遊び直しは1体目から。スロット切替とバックアップ復元でも同じ結果になる。

根拠：`src/screens/battleScreen.js:1012`、同`:5136`、`src/core/gameState.js:413`、同`:560`付近。

### G04 — P1：「今日の復習」が、対象ゼロでは未解放ボーナスへ進む

**対象：操作理解、ステージ進行、失敗後の導線。静的確認。**

総復習の「今日の復習に挑戦！」は、期限到来済みの復習があれば`reviewStage`へ進むが、ゼロなら推奨学年の`bonus_gN`を設定して戦闘準備へ進む。この分岐には通常のボーナス選択が行う解放条件の検査がない。ボタンの約束と行き先が異なり、通常の進行条件も迂回する。

さらに新しい誤答は原則、次の午前4時に復習予定が立つ。「今日の復習に入れた」という学年クイズの案内から直後に復習へ行っても、対象がない場合がある。翌朝に間隔を空ける設計そのものは問題ではなく、案内との不一致が問題。

**最小修正：** 今日の対象がなければ既存の空状態へ進め、ボーナスへの代替遷移をやめる。登録時の文言を実際の予定に合わせ、解放条件はどの入口でも一致させる。

**完了条件：** 初期状態・翌朝待ち・期限到来済み・全件終了の4状態で、表示した行き先と実際の画面が一致する。未解放ボーナスへ復習ボタンから入れない。

根拠：`src/screens/stageSelectScreen.js:1301`〜の分岐、同`:1328`以降の解放検査、`src/screens/gradeQuizScreen.js`、`src/models/reviewQueue.js:46`付近。

### G05 — P2：世界のステージ進行と出題範囲が結びついていない

**対象：ステージ進行、達成感、リプレイ性。データ・静的確認。**

アジア・ヨーロッパ・アメリカ・アフリカは、ステージIDの接頭辞だけで学年全体を返し、各ステージに定義された問題プールを使わない。同じ地域内ではステージが変わっても候補がそれぞれ313／284／328／185字のまま。敵や景色の変化はあるが、「ここまでの字を練習したから次へ」という区切りを出題が支えない。`kanto_area7`の95字など、他のステージにも範囲のばらつきがある。

**最小修正：** 現在のステージ定義と実際の出題範囲を合わせる。定義を使う場合は先にE07の参照切れを直す。全地域を同じ問題数にする必要はないが、増える理由と再訪時に練習する範囲を説明できる状態にする。

**完了条件：** 代表ステージの「意図した字の集合」と実際に抽選される集合が一致する。未習字・復習字の再登場が、背景を変えただけの無差別な反復にならない。

根拠：`src/loaders/dataLoader.js:433`〜、`public/data/stages_proto.json`、`PROJECT_EVIDENCE.md`の出題範囲に関する留保。

### G06 — P2：自分の学年から始める子に、後半ステージの戦闘負担が乗る

**対象：難易度、戦闘の長さ、学年別の開始条件、ボスへの理解。静的確認・実装式の計算・設計評価。**

**難易度の結論：一律に「難しすぎる」「簡単すぎる」とは判定できない。現状は、漢字の難しさ、必要な正解回数、音訓ルールの理解、入力の難しさが重なって上がる。まずこれらを分けて調整するべき。** 実際の子どもの正答率・クリア時間・離脱率は未測定なので、適正年齢や目標勝率を数値で保証しない。

#### 同じ初期能力でも、開始する学年で敵のHPが変わる

小学1〜6年生の地域は最初から選べる。一方、敵の能力は「全通常ステージ一覧で何番目か」とプレイヤー能力で決まる。小学6年生の子が新しいセーブで自分の学年から始めても、それ以前の地域を遊んできた場合と同じステージ係数が乗る。ゲームの習熟度と学校の学年を分離できていない。

以下は現行の`computeEnemyParams`を切り出し、実際のステージ順で計算した値。**全行でレベル1・攻撃力10・最大HP100を固定**して比較した。通常敵の必要正解数はダメージ10固定の単純計算であり、±10%の乱数、弱点1.5倍、マスターボーナス、途中のレベルアップ、回復は含まない。ステージ全体の所要時間の予測ではない。

| 最初のステージ | 通常敵HP | 通常敵の攻撃力 | 通常敵1体に必要な正解数の基準 | ボスHP |
| --- | ---: | ---: | ---: | ---: |
| 小1・北海道 | 31 | 9 | 4回 | 62 |
| 小2・東北 | 34 | 9 | 4回 | 66 |
| 小3・関東 | 44 | 9 | 5回 | 79 |
| 小4・中部 | 55 | 10 | 6回 | 95 |
| 小5・近畿 | 70 | 10 | 7回 | 114 |
| 小6・中国 | 81 | 11 | 9回 | 130 |
| 世界・アジア | 89 | 11 | 9回 | 141 |
| 世界・アフリカ | 137 | 13 | 14回 | 207 |

小1と小6の最初の通常敵では、HPが約2.6倍になる。攻撃力の上昇よりHPの上昇が大きく、差は「すぐ負ける厳しさ」以上に「正解しても一戦が終わるまで長い負担」として現れやすい。世界側ではG05の広い出題範囲も重なる。

レベルが上がると攻撃力＋2・最大HP＋10・全回復などがあるため、表の回数が全戦闘中ずっと続くとは限らない。ただし敵側もプレイヤー能力を参照して強くなる。育成後に戻ったときの快適さと、初めて自分の学年へ入ったときの負担を、それぞれ確認する必要がある。

#### ボスでは「正解」の意味が実質的に厳しくなる

通常は音訓限定が既定OFFで、知っている読みの一つを答えれば正解になる。しかしシールド中のボスは、弱点の音訓で正解するとシールドを1減らし、弱点以外ならHPへのダメージを1に固定する。シールドの初期値は通常3。これは、音訓の選択が通常敵の追加ダメージから、ボスでは進みやすさを大きく左右する条件へ変わる設計。

例えば初期の北海道ボスはHP62なので、弱点以外の読みだけを正しく答え続ける場合、HPを削るだけで62回の攻撃正解が必要になる。倒せないわけではないが、子どもには「読めたのに、ほとんど効かない」と感じられる可能性がある。シールドや弱点の案内は既にあるため、案内が存在しないとは評価しない。問題は、その案内だけで対象年齢の子が音訓を選び直せるかが未確認なこと。

なお、出題時に音訓の配列をひらがなへ正規化していることを確認した。元データがカタカナだから音読み弱点が必ず無効になる、という不具合ではない。

#### 「じっくり」と、誤答しても減らないことは別

現行の既定は`jikkuri`、タイマー非表示、正解後の敵攻撃なし、回復後の敵攻撃なし。考える速度への負担を抑える方向は良い。ただし通常の攻撃解答で読みを間違えた場合は、既定でも敵の攻撃を受ける。`jikkuri`を「誤答ダメージなし」と説明してはいけない。回復解答の誤答ペナルティには別のモード判定がある。

初期の北海道では通常敵の攻撃力9なので、HP100から回復などが一切なければ12回の被弾で0になる。1回の誤答で即終了する設計ではなく、ヒント・近い入力のやり直し・回復・練習への退避もある。一方で、同じ誤答から学び直す経路にT02・T03の不具合があり、救済の存在だけで難易度が適切とは判断できない。

**最小修正：** 学年を直接選んだ初回と、育成して後半へ来た場合の双方で、敵HP・攻撃力の既存係数を見直す。既存のボス案内で「今は音読み／訓読みを選ぶ」と分かるようにし、弱点外の正解を何十回も要求する負担が必要か再検討する。漢字を知らないことと、音訓ルールや入力に迷ったことを同じ難易度調整で解決しない。新しい難易度モードの追加は不要。

**完了条件：** 新規セーブで小1・小3・小6からそれぞれ開始し、最初の通常敵・最初のボスまでの正解回数、誤答回数、所要時間、ヒント使用、回復・中断を記録する。育成済みセーブの再訪も比較する。「漢字は読めるがゲームは初めて」の子が、前学年の戦闘でレベル上げをしないと過度に長引く状態を解消する。ボスで正解が続くのに進めない理由を、子どもが既存の案内から理解できることを確認する。

根拠：`src/screens/regionSelectScreen.js`の`isRegionUnlocked`、`src/screens/battleScreen.js:931`以降のステージ順・敵能力算出、同`:4880`以降のダメージ、同`:4942`以降のシールド、同`:5276`以降の誤答処理、同`:5540`付近・`:5671`以降、同`:5872`以降の読み正規化、`src/core/gameState.js:253`以降、`src/core/saveData.js:39`以降。

### G07 — P2：数が増えたことに比べ、「何が読めるようになったか」が伝わりにくい

**対象：上達実感、達成感、再挑戦する意味。静的確認・集計処理の比較・体験設計上の評価。**

**上達実感の結論：キャラクターの成長、収集、取り組んだ量は見える。本人の「前は難しかったこの読みを、今は答えられた」という変化を返す力は弱い。** 成長を感じさせる仕組みが無いわけではない。既にある結果画面・復習の正解表示・マスター演出で、何を祝うかを磨くことが優先。

| 現在ある表現 | 子どもへ伝わること | 上達の証拠としての限界 |
| --- | --- | --- |
| 正解のSE・読み上げ・敵へのダメージ | 今の行動が成功した | 過去よりできるようになったかは分からない |
| 「はじめて読めた漢字：X個」 | 新しく正解した字が増えた | 勝利画面は数が中心。どの字か、ヒントなしで答えたかはこの表示から分からない |
| 新規正解ゼロ時の「いままでに読めた漢字：X字」 | 過去の積み重ねは残っている | 同じ字を読み直せた、以前より少ない支援で答えたという今回の価値が埋もれる |
| マスター時の祝い・既マスター字の攻撃力2倍 | 練習の成果が戦闘を助ける | マスターの条件自体はT04・T05の見直しが必要 |
| 「こんしゅうのがんばり」の正解回数 | 最近取り組んだ量 | 同じ字を多く答えても増える。読みの種類・自力の想起・定着率の増加とは別 |
| 復習画面の「正解！」 | 前に復習予定へ入った字を今回は答えた | 通常の正解と同じ短い表示で終わり、つまずきからの変化が本人へ伝わりにくい |

通常戦闘の`newlyReadKanjiList`への登録条件は、それまでの正解回数が0であること。一度ヒントで答えを見て正解すると、後日初めて自力で答えられても「はじめて」の条件には入らない。最初に少し支援を要した子ほど、後から起きた大切な変化をこの指標では祝えなくなる。ヒントによる記録の意味はT04のP1、結果画面での実感の弱さは本項のP2として区別する。

週次表示は直近7日とその前7日の正解回数の差であり、「がんばり」の量としては使える。しかしそれだけで上達とは判断できない。実際の`recordKanjiAnswer`と週次集計を隔離したNode.js環境で実行し、「誤答→正解」と「正解→誤答」が、どちらも`correct: 1, incorrect: 1`・週次正解1回になることを確認した。この累計だけから「前は間違えたが今回は読めた」という順序や、ヒントが減ったことを後付けで復元することはできない。

**最小修正：** 既存の結果欄で、数だけでなく今回正解できた代表的な漢字・読みを少数示す。既存の復習や戦闘内再出題では、今回の出来事として確認できる範囲で「この字、今回は答えられた」と返す。ヒント使用の扱いはT04と揃え、初正解が増えなかった日も、実際に読み直せたことを肯定する。既存のレベル・収集・マスター演出はその役割で残し、新しい成長画面・ランキング・連続日数報酬を増やす必要はない。

**完了条件：** 初正解の日、同じ字を練習し直した日、復習した日それぞれで、本人が「今日はこの字を答えられた」と具体例を一つ挙げられるか確認する。支援を減らせたことを表示する場合は、その前後を実際に確認できた場合だけにする。「新しい字0＝上達0」と受け取らせず、記録から言えない改善を作らない。子どもの実感は未測定なので、この監査だけで達成済みとはしない。

根拠：`src/screens/resultWinScreen.js:422`以降、`src/screens/battleScreen.js:4847`・`:4882`・`:5895`・`:6242`付近、`src/screens/reviewStage.js:110`付近、`src/screens/profileScreen.js:197`以降、`src/core/gameState.js:171`以降の正誤・週次集計。

### G08 — P2：不足の案内と終わりにくい練習が、勉強を続けさせられる印象を生む

**対象：「勉強をやらされている感覚」、自分で選ぶ余地、ノルマ、失敗時の負担、やめどき。静的確認・完了処理の実行・体験設計上の評価。**

**結論：冒険の目的と、本人が選べる余地はある。ただし「あとこれだけ答える」「全部を終える」「終えたら次の問題」という流れが重なると、ゲームが課題をこなすための画面に感じられる可能性がある。** 子ども本人への観察は未実施であり、全員が強制感を覚えると断定する評価ではない。

#### 既にある良い土台

小学1〜6年生の地域を選べること、戦闘中に攻撃・回復・ヒント・練習への移動を選べること、モンスターを集めて地図を進める目的は、自分で遊び方を選ぶ材料になる。「おしい」入力のやり直しや、失敗後の再挑戦・地図へ戻る導線もある。誤答だけを扱う専用の短い復習には、完了後に続けるか戻るかを確認する処理が既にある。

したがって、選択肢やミニゲームを大量に足す必要はない。現在の選択と区切りが、本人に分かり、期待どおりに動くよう揃えることを優先する。

#### やらされ感につながる具体的な箇所

| 場面 | 現行実装 | 強制感につながる可能性・評価上の注意 |
| --- | --- | --- |
| 最近の学習量が少ない | プロフィールは前7日より1〜20回少ない場合、「先週のペースまで あと○回」と表示する | 本人が決めた目標でなくても、過去の回数が埋めるべき不足になる。回数が少ないことと学習成果が小さいことは同じではない（G07） |
| 通常練習を終えた瞬間 | 全マスター後にレビューへ自動移行し、次の問題を出す | 達成が「今日は終えてよい」という区切りになりにくい。戻る操作はあるため、退出不能という不具合ではない |
| 短い誤答復習を終えた瞬間 | 専用画面には継続確認があるが、「○で隠した読みのレビュー」「OK／キャンセル」という案内 | 終わる選択は実装されている。ただし子どもには「キャンセル」が、完了して帰る意味だと読み取りにくい |
| 学年ボーナスを選ぶ | 「通常ステージ全クリ＋学年漢字を全マスターで解放」と提示する | 任意のごほうびでも、先に全件の学習を済ませる宿題のように見えやすい。通常の旅全体がこの条件で止まるという意味ではない |
| 正解しているのに戦闘が長い | 敵HPとボスのシールドによって、同じ入力行動が長く続く | 「旅を進めたい」より「必要回数を入力する」が前に出る可能性がある。既存係数と案内の調整はG06 |
| 間違えた・途中で終えたい | 誤答でHPが減る。戻る判定や旗の永続化に不具合がある | 未知の字を試したり、休んだりすることが損だと感じる原因になる。保存・操作の不具合は既存のP1（G03・U06）として先に直す |

ボーナスの実際の条件は、学年内の通常ステージ全クリアと、各ステージのレビュー解放フラグ。画面の「全マスター」とすべての読みの習得は常に同一ではない（T05）。条件を曖昧に隠すのではなく、追加の挑戦であることと、実際に必要な条件を分かる言葉で伝える必要がある。

通常練習の`_completePractice()`を切り出し、画面遷移・保存・次問選択を代用品へ接続して実行すると、保存1回、レビュー状態への切替、次問選択1回となり、終了画面への遷移はなかった。専用の`quickReviewPracticeScreen`は別実装で継続確認を持つため、すべての復習が無条件に続くとはしていない。

「きょうは○ふん」で区切る仕組みも既にあるが、既定は区切らない設定で、時間超過の確認は通常戦闘の敵撃破後に行う。通常練習を自動で区切る仕組みとしては呼ばれていない。設定時間ぴったりに止まるものでもなく、G06の長い戦闘と組み合わさると区切りが遅くなる。強制的な短時間モードを追加するより、本人が既存の終了操作を迷わず使えることを優先する。

**最小修正：** 通常練習でも完了時に一度区切り、続けるか地図へ戻るかを本人が選べるよう、既存の完了・戻る処理を揃える。専用復習の選択は「もう少し遊ぶ／今日はここまで」など、行き先と結果が分かる表現にする。週次欄は今回取り組んだ事実を返し、過去の回数との差を埋めるノルマにしない。ボーナスは任意の追加挑戦として、現行の条件を正確に伝える。失敗後の再接触、戦闘の長さ、中断保存はT02・G06・G03の修正にまとめる。

**完了条件：** 対象年齢の子が「今日はどこへ行くか」「ヒントを使うか」「ここで終えるか」を自分で選び、その結果が分かる。通常練習・短い復習の完了後、本人の継続意思を確認せず新たな課題を始めない。学習量が少ない日も、埋め合わせを求める案内を出さない。実際の観察では、成人に次を指示される回数、本人が選んだ行動、やめたいときに戻れたかを確認する。長く遊び続けたことだけを、楽しさや自主性の証拠にしない。

根拠：`src/screens/profileScreen.js:203`以降、`src/screens/practiceBattleScreen.js:599`・`:675`・`:2238`付近、`src/screens/quickReviewPracticeScreen.js:119`以降、`src/screens/stageSelectScreen.js:552`・`:1336`付近、`src/core/bonusManager.js:19`以降、`src/core/sessionTimer.js`、`src/screens/battleScreen.js:4652`・`:5152`付近。関連する保存・戦闘・入力はG03・G06・T02・U06を参照。

## 子ども向け教材設計者

**総評：読みを思い出す練習の土台と、失敗を責めない工夫はある。ただし、入力判定・ヒント使用・復習間隔・達成表示が一貫せず、「何をできたと数えるか」が曖昧。支援を減らさず、記録とフィードバックを正確にすることが優先。**

| 評価対象 | 現行実装から言えること | 判定先 |
| --- | --- | --- |
| 実際に何を学習しているか | 主に単字の読みの想起と、かな入力。書字、文章理解、語の文脈に合う読みの選択を一律に測ってはいない | T01 |
| 漢字の読みとゲーム行動の結びつき | 読みに正解すると攻撃・回復するため、行動との因果はある。弱点の音訓は既定では正誤の制約にならない | T01 |
| 反復 | 戦闘内の再出題、練習、翌日以降のSRSが既にある。反復の回数・間隔を壊す経路が残る | T02・T03 |
| 誤答後の再接触 | 正答を見せるが復習画面では1秒で消える。既存の復習項目をまた間違えても予定が早まらない | G02／T02 |
| 支援と評価の区別 | ヒントを見て正解できることは学習の一部だが、自力の想起と集計が分かれていない | T04 |
| 到達度の伝え方 | 10問の結果や登録された読みの入力実績を、学年全体の習得と受け取れる表現で示す | T05 |
| 上達実感 | 学習量と能力の変化を分け、今回できた具体例を返す必要がある。累計だけから過去の解答順序やヒント使用は復元できない | G07／T04 |
| 自分から取り組む感覚 | 本人が選んでいない回数目標や、完了後の自動継続を抑える。既存のヒント・練習・終了の選択を分かりやすくする | G08 |
| 内容の信頼性 | 漢字ID衝突は別字の記録を混ぜる。読み・例文・由来説明の全件正確性は未確認 | E07／公開前の校閲 |

### T01 — P2：現在測っている力と、学習範囲の説明を揃える必要がある

**対象：実際の学習内容、読みとゲーム行動。静的確認・教材設計上の評価。**

既定では音訓限定がOFFで、単独の漢字に対して登録された読みの一つをかな入力すれば正解になる。例えば「生」の一つの読みを答えられても、文中で「生える」「生きる」などを読み分けられるとは限らない。例文出題は既にあるが既定OFFで、データ上の例文対応は小学1年生80字。これを全範囲で文脈読みに対応済みとは扱えない。

また世界側を一括して「中学生」と扱う記述があるが、grade9／10は漢検準2級／2級の範囲。漢検の目安は準2級が高校在学程度、2級が高校卒業・大学・一般程度であり、中学校の学年とは対応しない。[日本漢字能力検定協会：各級の目安](https://www.kanken.or.jp/kanken/grades/meyasucheck/)

**最小修正：** 説明を「漢字の読みをかなで答える練習」と実際の出題範囲へ合わせる。読みの想起・入力の練習として価値を伝え、漢字学習全般を完了できるとは表現しない。全学年の例文・書字機能を追加する要求にはしない。

**完了条件：** 保護者・子ども向け説明、コース名、結果表示が実際の出題内容と矛盾しない。例文モードの対象範囲と、世界コースの難度が分かる。

根拠：`src/core/readingScope.js`、`src/core/exampleMode.js`、`src/utils/readings.js`、`src/loaders/dataLoader.js:445`以降、`public/data/kanji_g*.json`。

### T02 — P1：間違え直した字が、必要なタイミングで戻ってこない

**対象：反復、誤答後の再接触。実モジュール・実装関数で再現。**

翌日以降のSRSでは、`add(id)`は既存IDなら何もしない。通常戦闘などの誤答はこの追加処理を呼ぶため、以前の正解で6日後に延びていた字を今日また間違えても、復習予定は6日後のまま。実モジュールで2回正解→再追加を行い、予定と間隔が変わらないことを確認した。`updateReview(id, 1)`には誤答時の間隔を短くする実装があるが、この経路で使われていない。

戦闘内の再出題にも別の穴がある。敵の弱点で絞った候補集合に予約字が無いと、`pickWeightedKanji()`は「別ステージの字」とみなして予約を削除する。ステージ内には存在しても、現在の敵に合う音訓を持たない場合がある。切り出した実関数に、音読みのみの予約字と訓読み側の候補を渡し、予約が消えることを確認した。なお再出題の待ち5問は直近除外5問に合わせてあり、単に直近除外と待ち時間が食い違うという問題ではない。

**最小修正：** 既存項目への誤答を復習予定へ反映する。戦闘内予約は一時的な候補外という理由で消さず、出せる時まで保持する。既存の二つの反復経路を直す。

**完了条件：** 6日先の字を再誤答すると予定が適切に早まる。敵の音訓が変わっても再出題予約を失わない。途中終了で戦闘内の再出題に届かなかった字も、翌日以降の予定に残る。

根拠：`src/models/reviewQueue.js:59`付近、`src/main.js`の`addToReview`購読、`src/screens/battleScreen.js:5276`付近・`:5710`以降の抽選処理。

### T03 — P1：復習画面だけ採点と確定制御が弱く、学習記録を誤らせる

**対象：失敗時の納得感、反復、入力と採点。静的確認・ハンドラー再現。**

通常戦闘・練習・学年クイズには近い入力をやり直せる支援があるが、`reviewStage`は完全一致以外をすぐ誤答として記録する。空欄でも記録し、判定中の入力ロックもない。実ハンドラーを同じ問題・同じ正解で1秒以内に3回呼ぶと、正解記録3回、次問への予約3個、復習間隔16日となった。一度答えただけの字を3回学習済みとして扱い、次の問題を飛ばす原因にもなる。

「おしい」支援側も、濁点や小書き文字の違いに対して「よめてるよ」と断定する表現には注意が必要。文字入力だけでは、正しく発音できていて入力だけ違ったのか、読みそのものを誤って覚えているのかを判別できない。罰を与えず再入力を促す方針は維持できる。

**最小修正：** 既存の正規化・近い入力への案内・確定中ロックを復習にも揃える。空欄や変換途中は学習結果に数えず、1問につき1回だけ記録する。「よめた」と断定する前に、直す文字を穏やかに案内する。

**完了条件：** 同じかな入力を4つの学習画面へ与えた時に、画面の目的に沿って説明できる判定になる。連続Enterは1件のみ記録され、予定は1回の正解分だけ更新される。離脱後の1秒タイマーも残らない（E08・E09参照）。

根拠：`src/screens/reviewStage.js:98`以降、`src/screens/gradeQuizScreen.js:224`以降、`src/screens/battleScreen.js`の近似入力判定と案内、`src/utils/readings.js`。

### T04 — P1：答えを見て入力した結果も、自力で読めたように集計する

**対象：実際の学習内容、支援、学習記録。静的確認。**

通常戦闘はヒント使用に応じてSRS品質を変え、答え表示まで使った場合は復習へ戻す配慮がある。しかし`recordKanjiAnswer(..., true)`と読みマスターの更新は、ヒント4の答え表示後でも行う。練習でも同様で、ヒント1〜3での正解は一律品質5としてSRSへ渡す。CSVの「よめた回数」「よめた割合」にはヒント使用の区別がない。

見て写して正解することにも練習価値はある。しかし、それを自力で思い出せた証拠や、定着した読みとして同じように扱うと、子ども・保護者が記録を誤解する。

**最小修正：** ゲーム上の成功と、支援なしで想起できた記録を区別する。現行のヒント段階を採点へ一貫して反映し、区別できない累計は「正解入力回数」など事実を表す名称にする。既存のヒントと成功の喜びを取り上げない。

**完了条件：** ヒントなし／一部ヒント／答え表示の各ケースで、戦闘・練習・SRS・表示の意味が一致する。過去の記録から支援の有無を推測して「自力正解」に作り替えない。

根拠：`src/screens/battleScreen.js:4852`・`:4905`、`src/screens/practiceBattleScreen.js:918`・`:943`、`src/core/reviewExport.js`のCSV列、`src/core/gameState.js`の`recordKanjiAnswer`。

### T05 — P1：少数問の結果を「この学年はバッチリ」と伝えてしまう

**対象：達成感、到達度の説明。静的確認・教材設計上の評価。**

学年クイズは既定10問で、8問以上の正解時に「この学年は バッチリ！」と表示する。今回抽選した10問の成功と、学年全体を読めることは同じではない。また「マスター」は登録された各読みを一度ずつ正解した実績で、期間を空けた保持や文脈中の読み分けを確認する評価ではない。T04のヒントによる過大評価も加わる。

**最小修正：** 「今回の10問」「この字の登録された読み」など、達成した範囲を正確に伝える。称賛や★は残し、学年全体の習得を断定しない。到達度を証明するための新しい大型テストを作る必要はない。

**完了条件：** 8／10の結果が、全80〜200字などの習得を意味する文言にならない。マスター・図鑑・CSVの説明も同じ意味で読める。なおボーナス解放には別の読み数ゲートもあり、全読みマスターと常に同一の条件とは扱わない。

根拠：`src/screens/gradeQuizScreen.js:361`、`src/screens/practiceBattleScreen.js:2276`以降・`:2455`付近、`src/screens/battleScreen.js:6220`以降。

## UX / Visual Director

**総評：素材の量と世界観の方向性はある。完成品らしさを妨げる中心は、小画面での可読性、画面ごとに異なる仕上げ、問題より目立つ情報や動き。素材を増やすより、配置と強弱を編集する。**

| 評価対象 | 判断の要点 | 判定先 |
| --- | --- | --- |
| 画面の第一印象 | ロゴの表示が小さく、上下移動が速い | U02 |
| 文字 | 縦持ち縮小で小さくなる。ルビ・拡大機能はあるが配置を含めた検証が必要 | U01・U04 |
| UI・情報量 | 今の問題、直前の問題、戦闘情報の強弱が揃わず、押せる範囲も見た目と違う | U04・U06 |
| アニメーション・エフェクト | Canvasの動き軽減未対応、FPS依存、毎フレーム変わる背景ノイズ | U05／E13 |
| 背景 | 画風・明るさ・UIとの密度差が大きい | U03 |
| モンスター | 地域・強さの差に加え、輪郭と描き込み密度もばらつく | U03・U07 |
| 音 | 切替競合を再現。音質・音量バランス・ループの聴感評価は未実施 | E12 |
| 画面遷移 | 配色とボタンの表現が変わり、戻る判定・遅延処理も不整合 | U03・U06／E08 |
| 素人制作物に見える部分 | 小さな文字、揃わない枠・余白、見えない当たり判定、旧名称の残存 | U01・U03・U06／E14 |
| AI個人開発作品に見える部分 | 個別素材の描き込みに比べ、画面全体の統一・情報の編集が弱い。制作方法の断定ではない | U03 |

### U01 — P1：スマートフォンで文字と操作対象が小さくなる

**対象：文字、UI、端末差。静的確認・寸法計算。**

内部画面は800×600固定で、画面全体を`object-fit: contain`で縮小する。幅390pxの縦持ちでは倍率が最大0.4875。16pxのCanvas文字は約7.8px相当、50px高のボタンは約24.4px高、32px高の「れんしゅうへ」は約15.6px高になる。文字拡大をONにしても、配置や押せる面積自体は大きくならない。

さらに、`style.css`の768px以下の`width/height: ... !important`と、`index.html`の50音パッド用`.vh-lock`の寸法指定が競合している。`max-width/max-height`による制約もあるため、必ずパッドに隠れるとは断定しないが、意図した配置が端末をまたいで成立する証拠はない。

**最小修正：** 対応画面幅で文字・主要ボタンの実寸を確保し、パッドを開いた状態まで含めてレイアウトを一本化する。Canvasの高DPI対応も文字の輪郭と合わせて確認する。

**完了条件：** 幅390pxの縦持ちと対象タブレットで、問題・入力欄・決定・戻るが同時に見える。主要操作の当たり判定を実表示で44px程度以上に確保することを本作の設計目標とし、文字拡大でも重なりがない。

根拠：`src/main.js:39`付近、`style.css:18`、`style.css:63`、`index.html`の`#gameCanvas.vh-lock`、`src/screens/battle/theme.js:19`、`src/ui/textScale.js`。

### U02 — P2：第一印象でタイトルが弱く、動きが落ち着かない

**対象：画面の第一印象、アニメーション。素材実見・静的確認・評価。**

`logo.png`は「ヨミタビ」「ゴトモン」と地図の本を一枚に収めた512×512画像。タイトル描画は高さを600×0.25に制限するため、ロゴ全体が150×150で表示される計算になる。スマートフォンではさらに約73px四方となり、名前より装飾の塊として見えやすい。

上下移動はミリ秒で増える時間に`0.02`を掛けているため、周期は約314ms、約3.18往復／秒。「ゆっくり浮く」演出としては速い。これは実見した動画の感想ではなく、式から分かる速度。

**最小修正：** 現行ロゴの余白・表示寸法・背景との境界を調整し、名称を読める大きさにする。上下移動を止めるか、十分に遅くする。新しいロゴ案を大量に作る必要はない。

**完了条件：** 最小対応画面で「ヨミタビ」を読める。初回案内とスタートが名称を奪わず、静止状態でも構図が成立する。

根拠：`public/assets/images/logo.png`、`src/screens/titleScreen.js:20`、同`:203`付近。

### U03 — P2：画風・背景・パネルの仕上げが画面ごとに異なる

**対象：UI、背景、モンスター、画面遷移、素人制作物／AI個人開発作品に見える部分。素材実見・静的確認・評価。**

確認した素材には、輪郭の大きなドット絵のロゴ・北海道モンスター、細密な竜、絵画的な砂漠背景、粒状テクスチャとぼかしのあるパネル、ほぼ単色の北海道地図が混在する。異なる地域や強さに応じた差は必要だが、輪郭・密度・余白の差まで大きい。

画面の描画も、タイトルの茶色＋金の光、名前入力の青い単色背景、ステージ準備の黒背景＋緑バー、Canvas／HTMLの別系統のボタンへと切り替わる。大きな画像一枚を枠へ押し込む箇所と、CSS・Canvasで描く枠の質感が揃っていない。

「AIで作った」と制作過程を断定することはできない。しかし、**素材の情報量は多いのに、組み合わせの規則と画面間の統一が弱い**点は、生成素材を個別に貼り合わせた印象につながる。素人らしさの主因は素材不足より、この編集不足。

**最小修正：** 現行素材を使い、背景の明るさ、パネルの塗り、枠の太さ、文字の色、主要／補助ボタンの表現を少数の共通ルールに揃える。モンスターの余白・接地位置を合わせる。

**完了条件：** タイトル→名前入力→地図→準備→戦闘→結果の代表画面を並べ、同じ製品のUIに見える。どの背景でも問題の漢字が最も読み取りやすい。

根拠：`src/screens/titleScreen.js`、`playerNameInputScreen.js:76`、`stageLoadingScreen.js:101`、`battleScreen.js:1190`付近。実見素材：`public/assets/images/ui/panel_player.png`、`stage.select/stage.select.1.png`、`monsters/full/grade1-hokkaido/HKD-E01.webp`、`monsters/full/grade7-asia/AS-F01.webp`、`backgrounds/Africa_area1.webp`。

### U04 — P2：戦闘中の情報と案内の優先順位が揃っていない

**対象：文字、情報量、UI。静的確認・評価。**

現在の問題に加え、前の漢字、読みの進捗、画数、直前の入力、ログ、敵の弱点、HP、経験値、コンボ、通知が同じ画面で描かれる。前の漢字欄などに11〜14pxの文字があり、U01の縮小と組み合わさると補助情報が細かな模様になりやすい。

ふりがな・文字拡大は既にあるため「未対応」とは評価しない。ただし出題は`serif`、UIは教科書体指定と`sans-serif`が混在し、フォントが端末に無い場合の最終表示やルビの余白は未検証。

初回の戦闘案内も「かなで入力→Enterキー」と端末キーボード中心の文言。ヒントの案内位置は右上を返す一方、操作ボタンの基本配置は画面下寄り。50音パッドや実際の配置に案内が追いついていない。

**最小修正：** 常に読む必要のある情報を「今の問題・入力・結果・次の操作」に絞って強くし、過去の情報と装飾の強さを下げる。既存チュートリアルの文言・指示位置を現行UIに合わせる。

**完了条件：** 初回の子が説明を読み返さず、何を読むか・どこへ入力するか・どう決定するか分かる。最小画面＋文字拡大＋ルビONで確認する。

根拠：`src/screens/battleScreen.js:1542`、同`:1752`、同`:2227`、`src/tutorial/tutorialData.js`の`battle`、`src/ui/ruby.js`。

### U05 — P1：動きを減らす設定がCanvas演出には効かない

**対象：アニメーション、エフェクト。静的確認。**

CSSには`prefers-reduced-motion`対応があるが、変更するのはCSSのanimation／transitionだけ。戦闘の赤・白の画面フラッシュ、揺れ、タイトルの粒子などはJavaScriptでCanvasへ描いており、その設定を参照していない。タイトルの紙の繊維も毎フレーム乱数で描き直すため、静かな背景として固定されない。

**最小修正：** 動きを減らす指定を描画処理にも反映し、全画面フラッシュ・揺れ・不要な常時粒子を止める。通常表示も、問題の読み取りを妨げない強さに抑える。

**完了条件：** 動きを減らす設定時、正誤の文字表示は残り、画面全体の揺れ・点滅・ロゴの往復が止まる。光刺激の医学的安全性をこの監査で評価したわけではない。

根拠：`style.css:123`付近、`src/screens/titleScreen.js:145`、`src/screens/battleScreen.js:2059`、同`:2955`、同`:4968`、同`:5688`。設定の意味は[MDN：prefers-reduced-motion](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/prefers-reduced-motion)を参照。

### U06 — P1：「もどる」の中に見えない別の当たり判定が重なる

**対象：UI、画面遷移、素人制作物に見える部分。静的確認。**

描画される「もどる」は`x=20,y=20,w=120,h=36`。一方、描画されない旧`BTN.back`が`x=20,y=20,w=100,h=30`で残り、クリック処理ではこちらを先に判定する。

そのためボタン中央のゲーム座標`(80,38)`は「タイトルへ」に取られ、右端や下端を押すとステージ選択へ戻る。同じラベルのボタンで、押した場所によって行き先が変わる構造になっている。

**最小修正：** 見せるボタンと判定する矩形を同じ定義へ揃え、旧ボタンの判定を除く。

**完了条件：** ボタンの中心・四隅付近をタップして、すべて同じ意図した画面に戻る。

根拠：`src/screens/battle/theme.js:20`、`src/screens/battleScreen.js:1209`、同`:3661`。

### U07 — P3：モンスター全体の仕上げ基準を揃える余地

**対象：モンスター、AI個人開発作品に見える部分。素材のサンプル評価。**

序盤の丸いモンスターは表情とシルエットを読み取りやすい。一方、細密な竜などは同じ512px素材でも輪郭の密度が大きく違い、小さく表示したときの印象が揃いにくい。全モンスターを確認したわけではなく、全素材が不統一という判定ではない。

**将来改善：** 既存モンスターを小さな表示サイズで並べ、外周のぼけ・余白・視認性を順に整える。v1のために全モンスターを作り直す必要はない。

根拠：U03で挙げた2体の実見素材。まずU03の表示規則を揃え、その後に素材固有の問題を判断する。

### U08 — P2：収集バーが常に満タンになり、積み重ねが見えない

**対象：上達実感、進捗表示、UI。静的確認・実装式の計算。**

プロフィールの「漢字収集」「モンスター収集」は、`makeBar`の現在値と総数の両方へ現在の収集数を渡している。そのため、未収集時は0/0・0%、1つ以上あれば1/1・100%、10/10・100%となり、収集が進んでもバーは伸びない。実装の割合計算に0・1・10・100を与えて確認した。

子どもが見ているのは本来「どのくらい集まったか」の表示だが、現行では常に自分自身を分母にしている。文字の収集数は増えても、進捗バーは完成したように見える。さらに収集の進捗と漢字の習得は別であり、このバーを学習到達度と解釈させるべきではない。

**最小修正：** 対象範囲を定義した正しい総数を使うか、総数を決めないなら既存欄を「X字／X体集めた」という数の表示にする。全2,136字を一律の分母にして初心者へ巨大な未達量を示す必要はない。マスター欄と収集欄の意味も区別する。

**完了条件：** 0・1・途中・対象範囲の全件で表示を確認し、1つ集めただけで全体完了には見えない。現在数を増やすと、定義した範囲に対する進捗が正しく変わる。数のみの表示を選ぶ場合は、意味のない100%バーを残さない。

根拠：`src/screens/profileScreen.js:166`以降の`makeBar`、同`:192`以降の呼び出し、`src/models/profile.js:37`以降の収集数。

## シニアエンジニア

**総評：ビルドは通り、正規セーブへの統合やFSM整理は進んでいる。ただし保存失敗、非同期処理の順序、画面離脱、データの一意性という境界条件が弱い。全面改築より、公開を止める不具合の修正と回帰検査を優先する。**

| 評価対象 | 判断の要点 | 判定先 |
| --- | --- | --- |
| バグ | 保存消失、見えない戻る判定、二重加算、復習の連続確定 | E01・E02・E10／U06・T03 |
| 状態管理 | 復元後のメモリと保存内容、離脱後のタイマーが一致しない | E04・E05・E08 |
| セーブ | 成功表示、スロット退避、破損復旧、旗の永続化を修正すべき | E01〜E05／G03 |
| データ | 別字の同一IDと参照切れ。学習集計にも影響する | E07／T04 |
| PWA | 撤退方針を尊重し、旧SW退役・名称・配布説明を揃える | E14 |
| 端末差 | 縮小表示、IME、フレーム依存を確認。実機は未検証 | E09・E13／U01 |
| パフォーマンス | 起動画像の直列読込、データ二重読込、キャッシュ方針の混在 | E11・E15 |
| 保守性 | 大きな画面モジュールと暗黙のイベント引数 | E16 |
| 技術的負債 | 正常系中心の確認では今回の障害を捕捉できない。修正点の検査を追加し、全面移行は後へ送る | E16 |

### E01 — P0：スロットの退避失敗で現在のセーブを失う

**対象：セーブ。モジュール単位で再現。**

`snapshotCurrentSlot()`は保存例外を握りつぶす。`switchToSlot()`は結果を確認せず`clearOwnedEntries()`へ進み、現在の記録を消す。復元や現在スロット番号の更新にも成功確認がない。

容量不足を想定し、`yomitabi_slot_data_1`への書き込みだけを失敗させた結果：

```text
switchToSlot(2) = true
krb_save = null
yomitabi_slot_data_1 = null
現在のスロット = 2
```

**最小修正：** 退避・検証に成功するまで削除しない。復元とスロット番号更新が途中で失敗した場合に元の状態を保持／回復できるようにし、失敗を呼び出し側へ返す。

**完了条件：** 退避失敗・復元失敗・番号更新失敗の各地点で、元の子の記録が残る。通常の1→2→1で名前・図鑑・進捗・復習が一致する。

根拠：`src/core/saveSlots.js`の`snapshotCurrentSlot`、`restoreEntries`、`switchToSlot`（`:158`）。

### E02 — P0：バックアップ読込と破損復旧が元データを先に上書きする

**対象：セーブ、データ。静的確認・破損時の動作を再現。**

バックアップ読込はJSONとして解析できれば、ゲームのセーブ構造かを検証せず`saveNow(data)`で現在の`krb_save`を置き換える。例えば誤って別用途の`{}`を選んでも保存され、次の読込でデフォルト値が補完される。読み込み前の退避もない。

また`loadSave()`はJSON解析失敗時にデフォルトのセーブを書き込む。壊れた文字列を与えた実験では元文字列が失われ、キーは`krb_save`だけ、レベルは1になった。部分的に救出可能なデータも残らない。

**最小修正：** バージョン・必須構造・型を検証してから反映する。取り込み前の正常データと破損した原文を保持し、復旧を選ぶまでは自動上書きしない。

**完了条件：** 別用途のJSON、途中までのJSON、未知バージョン、不正な型を与えても元の正常セーブが変わらない。

根拠：`src/screens/settingsScreen.js:1486`付近、`src/core/saveData.js:60`、同`:138`付近。

### E03 — P1：保存できていなくても「保存しました」と表示する

**対象：セーブ、UXの信頼性。静的確認。**

`saveGameData()`は動的importのPromiseを返さず、`saveNow()`も失敗をログだけで処理する。設定画面は150ms待ってから成功トーストを出すため、容量不足や保存拒否を成功と区別できない。バックアップ書き出し前の100ms待機も保存完了の保証ではない。

**最小修正：** 保存APIが完了・失敗を返すようにし、成功を確認してから表示／書き出しを行う。

**完了条件：** Storage書き込みを失敗させると成功表示が出ない。保存完了前の状態をバックアップに書き出さない。

根拠：`src/core/gameState.js:318`、同`:459`、`src/core/saveData.js`の`saveNow`、`src/screens/settingsScreen.js:1424`、同`:1460`付近。

### E04 — P1：起動時の初期セーブ生成がクラウド復旧条件を消す

**対象：セーブ、初期化順。静的確認・前提をモジュール単位で確認。**

`gameState.js`はモジュール末尾で`loadGameData()`を開始し、その先の`loadSave()`は保存が無い／壊れているときに初期セーブを書く。一方、`main.js`は画像・データ読み込みと認証の後で`recoverKrbSaveFromFirestoreIfMissing()`を呼ぶ。この関数は有効なローカルセーブがあれば復旧を打ち切る。

空のStorageへ`loadSave()`を呼ぶだけでv1セーブが存在する状態になることを確認した。通常の起動順では「復旧が必要だった」という情報を後段へ渡せなくなる。実Firestoreからの復旧試験はしていない。

**最小修正：** 起動前の保存状態を保持し、読込・復旧・新規作成の順序を明示する。復旧判定前に初期値を確定保存しない。

**完了条件：** 同一匿名ユーザーのクラウド記録があり、ローカルのみ欠損／破損したケースで、初期値として開始せず復旧できる。認証自体を失った端末の復旧まで保証するという意味ではない。

根拠：`src/core/gameState.js:469`、同`:634`、`src/main.js:194`以降、`src/services/firebase/firebaseController.js:324`。

### E05 — P1：復習データのメモリ・ローカル・クラウドが揃わない

**対象：状態管理、セーブ、データ同期。静的確認。**

`reviewQueue`はモジュール読込時にStorageを一度読み、以後は内部の`items`を使う。バックアップ読込やFirebaseのスナップショットはStorageを直接書き換えるが、その`items`を再読込する入口がない。復元した予定がその場の復習に反映されず、その後の操作で古いメモリ内容が保存され得る。

加えて、`loadGameData()`は復習配列が空の場合に既存キーを消さず、ステージ進捗も既存オブジェクトへ追記する。古い／空のバックアップを読んでも、以前の状態が残る経路がある。`DataSync`の復習キューはリモート優先で、更新時刻による競合判定もない。

**最小修正：** 復元は空の状態も含めて一括置換し、メモリとStorageを同時に更新する。クラウドの古い復習予定を無条件に採用しない。

**完了条件：** 復習あり→空のバックアップ、復習予定変更後→復元、古いリモート到着の各ケースで、画面・次回出題・再起動後の記録が一致する。

根拠：`src/models/reviewQueue.js:10`、同`:36`、`src/core/gameState.js:493`付近、`src/services/firebase/dataSync.js:68`以降。

### E06 — P1：外部SDKや認証の失敗で描画ループが始まらない

**対象：バグ、起動、端末・通信差。静的確認。**

Firebase SDKはHTMLの外部scriptで読み込む。`initializeFirebaseServices()`がfalseなら`initGame()`はreturnし、描画ループまで到達しない。匿名認証の拒否も上位でcatchされず、同じくループが開始されない。ローカルセーブだけで遊べる部分まで外部サービスの成功に依存している。

起動時の画像・fetchにもアプリ側の待機上限と再試行導線がなく、ロード表示を消した後にもデータ準備・認証が残る。

名前入力の確定もクラウドの新規データ初期化を待ってからコースへ進む。起動だけでなく初回の名前確定でも、通信が遅い・失敗する条件を確認する必要がある（G01）。

**最小修正：** ローカルで成立する起動を同期サービスの成功から切り離し、必須データの失敗には読めるエラーと再試行／戻るを用意する。

**完了条件：** SDK取得失敗、認証失敗、画像・JSONの失敗を個別に発生させても、無表示や永久待機にならない。通常オンラインでの起動も維持する。

根拠：`index.html`末尾のFirebase script、`src/main.js:202`付近、`src/services/firebase/firebaseController.js:87`、同`:136`、`src/loaders/assetsLoader.js:363`。

### E07 — P1：別の漢字が同一IDになり、参照切れも存在する

**対象：データ、学習記録。全学年JSONの機械検査で確認。**

| 重複ID | 同じIDを持つ別の漢字 |
| --- | --- |
| `g5-041` | 許／均 |
| `g5-043` | 禁／句 |
| `g6-134` | 潮／賃 |
| `g7-062` | 凶／叫 |
| `g8-046` | 犠／菊 |
| `g9-053` | 拒／享 |
| `g9-302` | 愉／諭 |
| `g10-121` | 爪／鶴 |
| `g10-165` | 弥／喩 |

図鑑・読み進捗・復習はID単位で記録し、`getKanjiById()`は先に見つけた1件を返す。そのため別の字の習得が混ざったり、復習で意図した字を取り出せなかったりする。

`stages_proto.json`と`stages.bonus.json`の問題リストには、存在しない漢字IDの参照が延べ42件ある。例は`kinki_area7`の`g5-192/193/194`。世界編には学年全体のプールへ切り替える実装があるため、「42問が実プレイで必ず欠落する」とは数えていない。

敵は`shikoku_area3`の`EHM-E02`が参照切れ。敵の補完処理は0体のときだけで、1体だけ欠ける場合を補完しない。

**最小修正：** IDの一意性と参照を修正し、既存セーブのID移行を同時に設計する。既に2字が混在した記録を単純なID置換だけで完全復元できるとは扱わない。

**完了条件：** 全学年＋通常／ボーナスステージでID重複0、参照切れ0。図鑑・復習で各字を独立に取り出せる。読み・例文の内容校閲は、この構造検査とは別に確認する。

根拠：`public/data/kanji_g1_proto.json`〜`kanji_g10_proto.json`、`stages_proto.json`、`stages.bonus.json`、`src/loaders/dataLoader.js:333`、同`:533`、`src/models/kanjiDex.js`、`src/screens/battleScreen.js:6201`。

### E08 — P1：戦闘を離れても予約済みの敵行動・出題が残る

**対象：バグ、状態管理、画面遷移。静的確認。**

通常の離脱は`battleStateFactory.exit()`から`battleScreenState.exit()`を呼ぶ。この`exit()`はゲームオーバー予約等を解除するが、`_timeouts`全体は解除しない。全解除が書かれた別の`cleanup()`は、`src/`内に呼び出しが見つからない。

さらに、読み間違い後の敵行動には管理外の`setTimeout`があり、1.3秒後の敵攻撃と、その1.7秒後の出題を予約する。戻る・練習への移動後でも、共通のHPや現在問題を書き換える経路が残る。

**最小修正：** 実際に使う`exit()`へ解除を集約し、予約と非同期読み込みに「まだ同じ画面／戦闘か」の確認を持たせる。

**完了条件：** 正解直後・不正解直後・撃破直後に戻る／練習へ移動し、待ってもHP・問題・画面が勝手に変化しない。戻った直後の再入場も確認する。

根拠：`src/states/battleStateFactory.js:65`付近、`src/screens/battleScreen.js:3371`、同`:5223`、同`:5321`、同`:5944`、`src/screens/stageLoadingScreen.js`。

### E09 — P1：日本語変換のEnterを回答確定として扱う

**対象：入力バグ、端末差。静的確認、実IMEは未検証。**

戦闘と名前入力のEnter処理に、変換中かどうかの判定がない。端末キーボードを使う場合、変換確定のためのEnterで攻撃／名前送信まで進む可能性がある。50音パッドの存在は、利用可能な端末キーボード経路の問題を解消しない。

**最小修正：** composition開始・終了とEnterの関係を扱い、変換の確定とアプリの送信を分ける。ヒント後などにEnterが実行するコマンドも、案内と一致させる。

**完了条件：** 日本語変換の確定では送信されず、回答決定で1回だけ処理される。Windowsの日本語IMEと対象モバイル端末のキーボード、50音パッドの全経路で確認する。

根拠：`src/screens/battleScreen.js:871`、同`:2627`、`src/screens/playerNameInputScreen.js:146`付近。composition中の判定については[MDN：KeyboardEvent.isComposing](https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent/isComposing)を参照。

### E10 — P1：1回のステージクリアで統計を2回増やす

**対象：バグ、学習・進行データ。静的確認。**

勝利画面は`recordStageCleared()`を呼んだ直後に`gameState.playerStats.stagesCleared++`も行う。前者も同じ値を加算するため、1回につき2増える。`_countCommitted`はこのブロック全体の再実行防止なので、ブロック内の二重加算には効かない。

**最小修正：** 加算と保存の責任を1か所にする。すでに保存された過大な累計の扱いも明記する。

**完了条件：** 通常クリアと再クリアでそれぞれ1だけ増える。結果画面への再描画では増えず、再起動後も一致する。

根拠：`src/screens/resultWinScreen.js:77`、`src/core/gameState.js:127`。

### E11 — P1：タイトル前に大きな共通画像を順番に読む

**対象：パフォーマンス、起動体験。ファイル計測・静的確認。**

起動時`loadAll()`は画像41エントリを直列に読む。実在ファイルの容量合計は約24.26MiB（同一パスの重複定義1件を含む）。ボタンやアイコンにも約1.3〜1.4MBのPNGが使われ、初めての子が遊ぶ前から全地域の地図などを待つ。

存在しない共通画像も3件ある：`wooden_sign.png`、`region_marker.png`、`title_background.png`。代替描画があるため、欠損だけで起動不能とはしていない。

漢字の段階読み込みを呼んだ後、`setupFSM()`内の`loadAllGameData()`が再び全学年を取得するため、起動経路で同じデータの取得・構築が重複する。

**最小修正：** 起動に必要な素材だけに限定し、既存画像を表示寸法に合わせて軽量化する。段階読み込みと一括読み込みを競合させず、同じ取得を共有する。

**完了条件：** 冷たいキャッシュで開始可能になるまでの転送量・時間を計測し、使用しない地域の素材を待たない。対象学校回線相当の条件で確認する。実測前に「何秒で起動する」と保証しない。

根拠：`src/loaders/assetsLoader.js:21`、同`:453`、`src/main.js:190`以降、`src/init/fsmsetup.js:41`、`src/loaders/dataLoader.js:25`、同`:638`付近。

### E12 — P2：リトライのフェード停止が次のBGMを止める

**対象：音、画面遷移。Audio代用品で実モジュールの競合を再現。**

ゲームオーバーのリトライは、`stopBGM(0.2)`を発行してすぐ戦闘へ遷移する。`stopBGM()`は古い曲のフェード完了を待った後、その時点の`#currentBGM`を停止するため、既に新しい戦闘曲へ変わっていればそれを止める。

実モジュールで「gameover再生→停止開始→battle再生→フェード完了」の順を与え、**gameoverとbattleが両方pausedになる**ことを確認した。実画面での発生は、戦闘曲開始とフェード完了のタイミングに依存する。

**最小修正：** 停止対象を開始時の音声要素に固定し、別の曲を止めない。音量変更・連続リトライ時も確認する。

**完了条件：** リトライを繰り返しても戦闘曲が継続し、旧BGMが復活しない。

根拠：`src/audio/audioManager.js:235`、`src/screens/gameOverScreen.js:497`。

音全体については、再生可能形式の判定・代替音源・未登録SEの抑制は既にある。実績音とマスター音、決定音とキャンセル音の共用は表現上の区別を弱めるが、音源の大規模追加より上記の競合修正を優先する。音量の実聴、音質、ループの継ぎ目、SEとBGMの聴感上のバランスは未評価。

### E13 — P2：フレーム数依存で演出速度が変わり、離席時間も加算する

**対象：アニメーション、端末差、状態管理。静的確認。**

敵の演出は30／45／60フレームなどを1フレームずつ減らす設計。30fpsでは60fpsの約2倍、120fpsでは約半分の長さになる。音声フェードも毎フレーム0.016秒を引いている。

一方、プレイ時間は`now-lastTime`をそのまま加算し、非表示から戻った際のリセットがない。離席時間や初期化時間が混ざる。`requestAnimationFrame`は表示更新に依存し、背景タブでは停止されることがあるため、固定フレームを実時間とみなせない。[MDN：requestAnimationFrame](https://developer.mozilla.org/en-US/docs/Web/API/Window/requestAnimationFrame)

**最小修正：** 演出時間を経過ミリ秒で扱い、非表示／復帰をプレイ時間と進行に反映する。

**完了条件：** 30／60／120fps相当でも演出の秒数が揃い、アプリを離れた時間がプレイ時間に丸ごと入らない。

根拠：`src/screens/battle/theme.js:30`、`src/screens/battleScreen.js:1319`、`src/audio/audioManager.js`の`#fadeOut`、`src/main.js:76`。

### E14 — P2：PWA撤退の実装と名前・配布説明が揃っていない

**対象：PWA、公開品質。静的確認。**

現行`public/sw.js`は壊れたJavaScriptではなく、旧登録を解除するための有効なスクリプト。オフライン用のfetchキャッシュは実装していない。「SWが不正」という古いREADMEの注意書きを現状の障害として扱うのは誤り。

ただしmanifestは旧名「漢字読みバトル／漢字バトル」のまま。READMEには現行ビルド経路に無いWorkbox設定の説明も残る。HTMLとSWの掃除は同一オリジンの全キャッシュ／登録を対象とし、同じオリジンに別アプリを置く構成では影響範囲が広い。現状の単独配信で他アプリを壊したと確認したわけではない。

**最小修正：** v1は現行どおりオンラインWeb版として説明・名称を統一する。旧SWの退役処理は必要な登録／キャッシュに限定し、更新確認を行う。PWA再実装はv1の必須項目にしない。

**完了条件：** 通常アクセスとホーム画面からの起動で名前が一致する。オフライン対応を誤って案内しない。旧SWのある端末へ更新が届き、保存データが保たれる。

根拠：`manifest.json`、`index.html`のSW掃除、`public/sw.js`、`README.md`。

### E15 — P2：画像キャッシュを毎回無効化する経路と、長期間固定する設定が混在

**対象：パフォーマンス、配信・更新。静的確認。**

背景取得はURLへ`Date.now()`のクエリを付ける。メモリキャッシュが消える再起動などでは同じ画像も異なるURLになり、ブラウザーキャッシュを活用しにくい。一方、Vercel設定は固定名の画像／音声に1年間の`immutable`を指定し、同じURLで差し替えた素材が更新されにくい設計。

`public/`は3,958ファイル、約999.8MiBあり、そのままビルドへコピーされる。これは配布物全体の容量であり、初回に約1GBをダウンロードするという意味ではない。

**最小修正：** 現行参照素材を確認し、リリースに結びつく安定したバージョン指定とキャッシュ規則へ揃える。不要な旧素材・作業ファイルは参照確認後に配布対象から外す。

**完了条件：** 同じ版では再利用され、画像・音声を差し替えた次の版では既存端末にも更新が届く。FirebaseとVercelを同じ挙動とみなさず、実際の公開先で確認する。

根拠：`src/loaders/assetsLoader.js:133`、同`:179`、`vercel.json`、`vite.config.js`、`public/`の容量検査。

### E16 — P3：巨大画面モジュールと暗黙のイベント契約が保守を難しくする

**対象：保守性、技術的負債。静的確認・ビルド確認。**

`battleScreen.js`は6,607行、`practiceBattleScreen.js`は2,631行、`settingsScreen.js`は2,369行。描画・入力・採点・保存・演出の責務が集中している。FSMの旧実装も残るが、実際のセットアップは`core/fsm.js`へ寄せられており、二つが同時稼働するという問題ではない。

`publish(event, payload)`は第3引数を受け取らないのに、`publish('changeScreen', 'resultWin', resultData)`などの呼び出しがある。モジュール単位で第3引数が届かないことを確認した。現行の捕獲・勝利画面には`gameState`からの補完があるため、これだけで必ず画面が壊れるとは判定しない。ただし、正しい引数が届いて動くのか、補完で偶然動くのかが分かりにくい。

ビルドには静的／動的importの重複警告が出る。`package.json`にはtest／lintの実行定義がなく、`checkJs`もfalse。既存検査はステージID中心で、E07やE10を見つけない。

**将来改善：** 直す箇所から責務とイベント引数を揃える。v1のための全面TypeScript化・全画面分割は不要。今回のP0/P1を再発させない小さな回帰検査は、各修正の完了条件に含める。

根拠：上記3画面、`src/core/eventBus.js:24`、`src/states/battleStateFactory.js:59`、`src/init/fsmsetup.js`、`package.json`、`tsconfig.json`。

## 検証結果

| 検証 | 結果 | 解釈 |
| --- | --- | --- |
| 本番ビルド | 成功。Vite 5.4.19、75モジュール、約9.28秒 | 動作・保存の正しさまで保証しない |
| 出力JS | メイン約497.02kB、gzip約146.35kB | 起動時画像の24.26MiBと分けて評価する |
| `node scripts/verify_stage_id_integrity.mjs` | 旧ID 0、ステージ参照整合OK | 漢字IDの一意性や問題プールの参照は別検査が必要 |
| `public/data/`の全JSON | 20ファイルすべて通常のJSON解析に成功 | 過去の「JSON構文が壊れている」という指摘は現状に該当しない |
| 漢字ID一意性 | 別字の重複9組 | E07 |
| 問題／敵ID参照 | 漢字の不明参照延べ42、敵1 | 実際のプール経路による差はE07に記載 |
| スロット退避失敗の注入 | 元セーブと退避が両方消失 | E01、メモリStorageで実モジュールを実行 |
| 破損セーブの読込 | 元文字列を保持せず初期値を書込 | E02、同上 |
| 5体目チェックポイントの保存・読込 | 保存された進行に旗がなく、再読込で復元されない | G03、実モジュール＋メモリStorage |
| 復習予定を持つ字の再誤答経路 | 2回正解で6日先に延びた項目へ再追加しても予定は不変 | T02、実reviewQueueモジュール |
| 戦闘内再出題の候補外処理 | 現在の候補にない予約字が削除される | T02、実装関数を切り出し。通しプレイは未実施 |
| 復習で同一正解を3回連続確定 | 正解記録3件、遷移予約3件、間隔16日 | T03、実ハンドラー＋依存先の代用品。タイマー実行前の3呼出し |
| 学習範囲・ヒント・達成表示 | 既定の単字出題、ヒント使用時の同一集計、10問から学年全体への称賛を静的確認 | T01・T04・T05。学習効果の実測ではない |
| 学年ごとの初回戦闘難度 | Lv1・攻撃10・最大HP100固定で、通常敵HPは北海道31、中国81。その他地域も実装式から算出 | G06。実際のステージ順を使用し、敵能力算出関数を切り出して実行。通しプレイの勝率・時間ではない |
| 上達を表す集計の意味 | 誤答→正解と正解→誤答は同じ累計・週次値になる | G07。実集計処理を隔離実行。自動読込を外し、ユーザーの保存データは使用しない |
| プロフィール収集バー | 0件は0%、1・10・100件はいずれも100% | U08。呼び出し引数と実装式による計算。画面の実見ではない |
| 通常練習の完了処理 | 保存後にレビューへ切り替わり、次問選択1回・終了画面への遷移0回 | G08。実メソッドの切り出しと依存先の代用品で確認。専用の短い復習には別の継続確認がある |
| 音声切替の競合 | 新しいbattle曲も停止 | E12、Audio／rAF代用品で実モジュールを実行 |
| 起動UI画像 | 41エントリ、40ユニークパス、欠損3 | 容量合計は重複定義を含む |
| PWAアイコン | manifest記載の2ファイルは存在 | インストール／旧SW退役の実機成功は未確認 |
| ブラウザー操作・音の試聴 | 未実施 | 接続可能なブラウザーなし。視覚／端末の実測判断は残る |

ビルド実行：`npm.cmd run build -- --outDir <一時ディレクトリ>/yomitabi-quality-audit-build-20260906`。追加の検査はインラインで実行し、テストコードをリポジトリへ追加していない。

## 公開版v1を作るために直すこと

以下の**10項目に限定**する。まず1のP0を解消し、その後P1を公開条件として満たす。P2はこの一覧に含む既存ゲームの調整に絞る。新しい遊び・収集要素・モードを増やさない。

1. **やめても記録と旗を失わない保存にする。** E01〜E05・G03。失敗時は元データを残し、保存成功後だけ成功を表示する。スロット・バックアップ・クラウド復元・空の復元で状態を揃え、ページを閉じた後も5体目の旗から再開できるようにする。
2. **漢字データと累計の誤りを直す。** E07・E10。ID重複9組、参照切れ、クリア数二重加算を修正する。既存記録の移行を確認し、公開範囲の読み・例文・説明文を校閲する。
3. **「間違えた字を復習する」経路を通して直す。** T02・T03・G04。再誤答で予定を更新し、戦闘内の予約を失わず、連続確定で記録・出題を進めない。対象ゼロでは既存の空状態を使い、未解放ボーナスへ飛ばさない。
4. **学習結果を正しく、上達が具体的に分かる表示にする。** T01・T04・T05・G07・U08。ヒント使用と自力の想起を混同せず、既存の結果欄で今回答えられた漢字・読みを少数示す。初正解ゼロでも練習し直した価値を返し、常に100%になる収集バーを修正する。CSV・マスター・学年クイズ・コースの説明は、確認できた実績と範囲だけを表す。
5. **初回の一問までを軽く、迷わず進めるようにする。** G01・E06・E11。開始前に読む素材を絞り、認証や名前登録の通信失敗で止めない。既存の選択画面と案内で、最初に押す対象・出発操作を明確にする。
6. **対象端末で読めて、入力・確定できる画面にする。** U01・U04・E09。文字・押せる面積・50音パッド表示・変換Enterを修正し、案内を実際の入力方法へ合わせる。通常戦闘だけでなく練習・復習・クイズにも同じ基準を適用する。
7. **自分で終える・戻る・再挑戦する操作を揃える。** G08・U06・E08・E12。練習完了後は自動で課題を続けず、続行か終了を本人が選べるよう既存の導線を整える。週次欄で埋め合わせを求めず、ボーナスは任意の追加挑戦として伝える。見えない戻る判定と離脱後の予約処理を修正し、前画面の処理が次の画面・BGMへ干渉しないようにする。
8. **既存画面の見た目と演出を整える。** U02・U03・U05・G02・E13。ロゴと今の問題を読みやすくし、背景・枠・文字・ボタン・モンスターの位置を揃える。重複待ち、速い揺れ・フラッシュを抑え、答えを確かめる時間と動き軽減を確保する。
9. **出題範囲と戦闘難度を、学年から始められる設計へ揃える。** G05・G06。世界を含め、定義と実際の抽選集合を一致させる。新規セーブで自分の学年を選んでも過度に長引かないよう既存の敵能力係数を見直し、ボスの音訓・シールドの意味を伝える。新しい難易度モードは追加しない。
10. **公開する版・端末・説明で最終確認を通す。** E14・E15。オンラインWeb版として名称と対応範囲を統一し、公開先の更新・旧SW退役・クラウドのアクセス制御を確認する。対象iPad／iPhone／Androidで開始→解答→誤答→復習→中断→再開→保存復元を通し、音も試聴する。対象年齢の子が支援なしで進めるか確認し、残るP1を解消してから公開する。

全面TypeScript化、全画面の分割、PWA再実装、全学年への例文拡張、全モンスターの描き直しは、今回のv1範囲に含めない。E16の大規模整理とU07の全素材磨き込みはP3として将来へ送る。
