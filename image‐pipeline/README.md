# image‐pipeline（素材生成ツール）

このディレクトリは **素材生成/加工ツール** です。**プロダクト本体ではありません**。

## 実行（例）

```bash
cd image‐pipeline
npm install
node process-images.js
```

※具体的な手順は、このディレクトリ配下の各スクリプト（`process-*.js` など）を参照してください。

