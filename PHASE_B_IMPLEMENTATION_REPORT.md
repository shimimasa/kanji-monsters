# PHASE B 実装報告

実施日: 2026-09-06。対象は実装計画の B01〜B05。基準資料は `YOMITABI_QUALITY_AUDIT.md`、`YOMITABI_V1_REMEDIATION_PLAN.md`、`PROJECT_EVIDENCE.md`。

PHASE B の実装と自動回帰検証を実施した。最終結果は **PHASE B 22件成功、0件失敗、0件スキップ**。PHASE A も **86件成功**し、production build と `git diff --check` に成功した。公開・デプロイ・コミットはしていない。PHASE C の実装には着手していない。

実ブラウザーを利用できない環境だったため、Windows日本語IME、iPhone、iPad、Android、50音パッドの実操作は未確認。PHASE B の実装と自動検証は完了しているが、実端末を含む最終合格判定はPHASE Dで行う。

## 1. 作業開始時の確認と変更ファイル

開始時のブランチは `main`。`git status`、現在のブランチ、未コミット変更を確認した。PHASE Aを含む多数の未コミット変更が存在していたため、既存変更の削除、初期化、巻き戻し、上書きは行っていない。

PHASE Bで変更した既存ファイル:

| 区分 | ファイル |
| --- | --- |
| 学習記録・復習 | `src/core/gameState.js`、`src/core/reviewExport.js`、`src/models/reviewQueue.js` |
| 入力・支援 | `src/ui/kanaPad.js`、`src/utils/readings.js` |
| 画面と採点 | `src/screens/battleScreen.js`、`src/screens/practiceBattleScreen.js`、`src/screens/quickReviewPracticeScreen.js`、`src/screens/reviewStage.js`、`src/screens/gradeQuizScreen.js`、`src/screens/playerNameInputScreen.js` |
| 遷移・結果 | `src/screens/stageLoadingScreen.js`、`src/screens/stageSelectScreen.js`、`src/screens/resultWinScreen.js`、`src/screens/profileScreen.js`、`src/screens/courseSelectScreen.js` |
| 音声 | `src/audio/audioManager.js` |
| 説明・テスト実行口 | `README.md`、`package.json` |

新規ファイル:

- `src/core/answerSubmission.js`: 空欄、IME composition、連続送信、送信ロックを扱う共通入力確定ゲート。
- `src/core/learningOutcome.js`: 学習記録と復習予定を同じ保存候補へ反映し、失敗時にメモリを戻す処理。
- `src/core/battleRetry.js`: 戦闘内再出題を現在候補とステージ全体の候補に分けて判断する処理。
- `src/core/screenLifecycle.js`: 画面世代、管理タイマー、遅延コールバックの有効期間を管理する処理。
- `src/core/learningPresentation.js`: 学年クイズで今回確認した実績だけを集計する処理。
- `tests/phase-b/` の6テストファイル。
- 本報告書。

漢字ID、漢字・ステージデータ、画像、音声アセット、3D、TypeScript、戦闘難度はPHASE Bでは変更していない。

## 2. 監査IDと実装計画への対応

| 計画 / 監査ID | 調査した失敗と修正 |
| --- | --- |
| B01 / E08・U06・E12 | 戦闘離脱後も敵行動、次問、ロード結果が残る経路を、画面世代と管理タイマーで停止した。入力購読を `exit()` で解除し、遅延Promiseは古い世代へ結果を反映しない。表示されていない旧戻る判定を除去し、見えているボタンの遷移へ統一した。BGM停止開始時の音声要素を保持し、古いフェード完了が新しいBGMを停止しないようにした。ステージロードの画面遷移payloadも既存の有効な形式へ揃えた |
| B02 / E09・T03・T04 | Enter、IME composition、空欄、送信済みロック、50音パッドを共通の送信口へ接続した。IME変換中と `keyCode=229` のEnterは回答を確定しない。近似入力は穏やかな案内を残し、正誤・ダメージ・学習記録を確定せず再入力できる。50音パッドは疑似KeyboardEventを作らない。名前入力は読みへ正規化せず、入力値をそのまま扱う |
| B03 / T02・T03・T04 | `commitLearningOutcome` から正誤記録と復習キューを一回の保存で確定する。保存失敗時は解答累計、読み進捗、日別集計、復習キュー、問題の重複防止状態を戻し、同じ問題を再試行できる。既存復習項目への再誤答と答え表示は間隔を翌日へ戻す。戦闘内再出題は現在の敵の弱点候補から一時的に外れても保持し、ステージ全体から消えた予約だけを除く |
| B04 / G04・G08 | 「今日の復習」が0件のときに未解放ボーナスへ迂回する分岐を削除し、専用復習画面の空状態へ進める。通常練習は全対象完了後に自動で次問を始めず、採点と保存が済んだ状態で停止する。Enterで続けるか左上から地図へ戻る既存操作を明示した。学年クイズの翌朝予定を「きょうのふくしゅう」と案内していた残存文言も、失敗テストで検出後に「つぎのふくしゅう」へ修正した |
| B05 / T01・T04・T05・G07・G08・U08 | 学年クイズは「今回の正解入力」「ヒントなしで正解入力」と今回の具体例を表示し、学年全体の習得を断定しない。勝利結果も今回正解した字を最大3字示す。CSVは自力、ヒントあり、答え表示後、支援不明の旧正解、誤答を別列にした。分母を持たず常に100%だった収集バーを件数表示へ変更し、週次減少時に不足分を要求する文言をやめた。READMEとコース選択へ、単字・例文・日本編・世界編の実際の対象範囲を記載した |

ヒントや「おしい」判定は削除していない。支援後に正しい読みを入力した場合も、攻撃・回復・練習などゲーム上の成功は残す。学習記録では支援状況を分け、自力で確認した読みだけを読み習得・マスター進行へ使う。

復習品質は次のように統一した。

| 結果 | 復習品質 | ゲーム上の成功 | 自力習得 |
| --- | ---: | --- | --- |
| ヒントなし正解 | 5 | 成功 | 進める |
| ヒント1正解 | 4 | 成功 | 進めない |
| ヒント2・3正解 | 3 | 成功 | 進めない |
| 答え表示後の正解 | 1 | 成功 | 進めない |
| 誤答 | 1 | 失敗時の既存ゲーム処理 | 進めない |

## 3. 通常戦闘・練習・復習・学年クイズの違い

全モードで、空欄は回答として確定しない。IME変換中のEnterは変換確定だけに使い、回答決定時の一回だけ記録する。近似入力では学習記録とゲーム上の失敗を確定せず、同じ問題へ再入力できる。

| モード | 正解として受け付ける読み | 支援 | ゲーム上の処理 | 学習記録と進行 |
| --- | --- | --- | --- | --- |
| 通常戦闘 | 通常は登録された読み。敵の音訓指定時は指定系統、例文出題時は文中で求めた読み | ヒント1〜4と「おしい」を維持 | 正解入力で攻撃または回復。支援後でも成功する。確定誤答は既存の敵行動へ進む | 自力のみ読み習得を進める。誤答は戦闘内再出題と翌朝以降の復習対象になる |
| 練習 | 通常練習は登録された読み。練習内の復習では伏せられた対象読み | ヒント1〜4と「おしい」を維持 | 正解として練習を進める。通常戦闘の敵ダメージ目的ではない | 自力のみゲーム上のマスター進行へ使う。全対象完了後は停止し、続行か地図へ戻る操作を待つ |
| 専用復習 | 期限到来した先頭5項目の登録読み | 通常ヒントボタンはない。「おしい」2回目以降は答えを提示 | 正誤を表示して次の復習へ進む。0件なら空状態を表示する | 自力正解は品質5。再誤答・答え表示後は翌日へ戻す。キューから先に削除しない |
| 学年クイズ | 選択学年から重複なしで抽選した漢字の登録読み | 通常ヒントボタンはない。「おしい」を維持 | 今回の問題の正解入力数を数える。戦闘ダメージはない | 自力と支援後正解を分ける。結果は今回確認した問題だけを示し、学年全体の習得とは表現しない |

## 4. 新規テスト

| ファイル | 主な検証 |
| --- | --- |
| `answer-submission.test.mjs` | 空欄、近似入力、カタカナ正規化、完全な誤答、連続Enter、IME composition、近似入力後の再入力、離脱後の送信 |
| `review-outcome.test.mjs` | 既存復習項目の再誤答、期限到来済み初期間隔、自力・ヒント1〜3・答え表示の品質、答え表示後正解、同一問題の重複、正式な再出題、保存失敗時のロールバックと再試行 |
| `lifecycle-retry.test.mjs` | 画面離脱直後のタイマーとPromise、戦闘内再出題の一時候補外、ステージ外予約の除去、復習0件時の空状態 |
| `audio-lifecycle.test.mjs` | 古いBGMのフェード完了が新しいBGMを停止しないこと |
| `presentation.test.mjs` | 10問中8問を学年全体と扱わないこと、CSVの旧正解・自力・ヒント・答え表示の区別 |
| `screen-wiring.test.mjs` | 4学習画面が共通入力・学習保存を通ること、50音パッド、戦闘離脱、見えない戻る判定、練習完了停止、翌朝復習の案内文 |

実装前には新しい共通モジュールが存在せず、最初のPHASE Bテストは3ファイルすべて失敗した。実装計画との最終照合では「きょうのふくしゅう」という残存文言を失敗テストで再現し、文言修正後に全22件を再実行した。

## 5. 実行した検証と成功結果

```powershell
npm.cmd run test:phase-a
npm.cmd run test:phase-b
npm.cmd run build
git diff --check
```

- PHASE A: **86 / 86成功**。失敗・スキップ0、終了コード0。
- PHASE B: **22 / 22成功**。失敗・スキップ0、終了コード0。
- Production build: 成功。85モジュールを変換、終了コード0。
- `git diff --check`: 成功、終了コード0。
- テスト出力中のStorage容量不足エラーは、保存失敗時のロールバックを確認するため意図的に発生させたもの。
- テスト用Node環境での `localStorage is not defined` ログは、ブラウザーStorageをテストごとに注入する前のモジュール初期読込ログ。テスト結果は成功している。

## 6. 既存データへの影響

PHASE Bでは保存スキーマ、漢字ID、漢字・ステージデータを変更していない。既存セーブを初期化せず、過去の正解、マスター、復習予定、解放状態を一括補正していない。

過去の正解に支援状況の証拠がない場合は、自力正解へ付け替えず「支援不明の旧正解」として保持する。CSVの列名と表示は変わるが、保存された旧累計は維持する。

今後の回答では、自力、ヒント1〜3、答え表示を区別する。既存の復習項目は、その項目へ新しく回答したときだけ予定が変わる。再誤答や答え表示時は翌日へ戻すが、無関係な全項目を一括で期限到来させない。新規の誤答は従来の方針どおり次の午前4時に予定する。

学習記録と復習キューは同じ保存候補に含める。容量不足などで保存できなければ、メモリ上の解答記録とキューを両方とも元へ戻し、同じ問題を再試行可能にする。画面離脱は確定済みの保存を取り消さない。

## 7. 残存リスクと未検証

- 接続可能な実ブラウザーがなかったため、Windows日本語IME、iPhone、iPad、Android、50音パッドでの変換・確定・連打は未確認。自動イベント試験だけで実IME合格とは判定しない。
- 日本編と世界編での復習開始、途中終了、全件終了、練習完了後の続行・地図戻りは実画面で通していない。
- 本番Firestore、実ユーザーデータ、複数タブ、スロット切替直後の学習記録はPHASE Bの実端末操作では未確認。PHASE Aの合成データ回帰は通過している。
- ヒント後もゲーム上の成功を残す設計が、子どもに支援として自然に伝わるかは対象年齢の利用確認が必要。
- 結果、プロフィール、CSVの文言と同じ実績が実画面で読み取れるか、本人が今回読めた字を理解できるかはPHASE Dで確認する。
- ビルドには主要チャンク約516 kB、動的・静的import併用、Vite CJS APIの警告が残る。起動性能とコード分割はPHASE C以降の対象であり、今回は変更していない。
- 既存の未コミット変更が多数残っている。PHASE A・Bの変更はコミットされておらず、公開版として固定された状態ではない。

## 8. PHASE Bの判定

B01〜B05のコード実装と要求された自動回帰テストは完了した。PHASE A回帰、PHASE B回帰、production build、差分検査はいずれも成功している。

実装計画が要求する実IME・実端末・実画面の手動確認は未実施のため、PHASE Bを公開候補として最終合格にはしていない。PHASE Cへ進む前提となるロジック実装は揃っており、公開前の最終合格はPHASE Dの実端末確認と統合回帰で判断する。
