# プロジェクト証拠レポート：漢字ヨミタビ

調査日: 2026-09-05  
調査範囲: 現在の追跡済みソース、データ、設定、ドキュメント、および `git log --all`（645コミット）。  
この文書では、**事実**はファイルまたはGit履歴で確認した内容、**推定**はそこから合理的に導いた内容、と明記する。外部サービスの実設定、実機で未確認の動作、作者の意図は断定しない。

## 1. プロジェクト概要

### 事実

- ブラウザゲームのタイトルは `index.html` の `<title>` にある「漢字ヨミタビ」。`package.json` のパッケージ名は `japanese-literature-game`。
- 仕様・実装には、小学校1〜6年の漢字を読みで答える戦闘、国内地域・世界・ボーナスステージ、モンスター収集、漢字／モンスター／ことわざ図鑑が存在する。
- 学習データは小1〜小6で計1,026字（80 + 160 + 200 + 202 + 193 + 191）、中学相当の g7〜g10 を含めると計2,136字である（`public/data/kanji_g*_proto.json`）。
- 主要ループは、ステージ選択 → 漢字の読みを入力して敵と戦闘 → 勝利／力尽きる → 進捗・経験値・漢字の読みに関する記録・モンスター収集を更新 → 図鑑または復習へ、である。`battleScreen.js`、`resultWinScreen.js`、`gameOverScreen.js`、`gameState.js`、`reviewQueue.js` がこの流れを実装する。
- 読みの入力は端末キーボードだけでなくゲーム内50音パッドを実装している（`src/ui/kanaPad.js`）。読み上げ用に Web Speech API を利用する実装もある（`src/audio/speech.js`）。
- 学習要素として、音読み／訓読みの弱点、ヒント、読み別の達成、誤答・ヒント利用を反映する復習キュー、例文モード、学年まとめテスト（力だめし）、今日の復習、漢字図鑑、ふりがな、学習記録CSV書き出しをコードと直近履歴で確認した。

### 推定

対象は、漢字の読みを学ぶ小学生、とりわけ漢字に苦手意識のある子どもを中心に、教室利用も想定した設計である。根拠は児童向けのかな中心文言、失敗を強く否定しない表示への改修履歴、複数セーブスロット、先生向け「ふりかえり」CSV、引継ぎ資料中の教室・iPad・児童への言及である。実利用者数や教育効果は、このリポジトリからは確認できない。

## 2. 技術構成

| 項目 | 確認できた事実 |
|---|---|
| 言語 | JavaScript（ES Modules）、HTML、CSS、JSON。データ整備用に Python / Node.js スクリプトもある。 |
| ビルド／開発 | Vite 5.3.1。`npm run dev`、`npm run build`、出力先は `dist/`。 |
| 描画 | `#gameCanvas` を 800×600 の 2D Canvas として初期化し、`requestAnimationFrame` で更新・描画する。入力欄、ローダー、50音パッド等はDOMも併用。 |
| 画面管理 | 独自FSM（正史は `src/core/fsm.js`）、`setupFSM.js` が画面遷移を集約。多数の画面モジュールを登録するSPA構成。 |
| 音 | HTML Audioを管理する `AudioManager`、Web Speech APIを使う `speech.js`。BGM・SEに m4a / ogg / mp3 を保持。 |
| 保存 | `localStorage` の `krb_save` を正史とする。旧キーは読み取り互換・移行用。セーブスロット、バックアップ、オートセーブを実装。 |
| クラウド同期 | Firebase Auth（匿名認証）と Firestore。`krb_save` が無い／壊れた場合の復旧と非同期同期キャッシュとして実装されている。Firebase SDK 9.22.1 compat版をCDNから読む。 |
| 配布候補 | Firebase Hosting（`npm run deploy`）、Vercel、CNAME（GitHub Pages式）の設定が並存。実際の配布先は未確定。 |
| PWA | manifest は存在するが、PWAとしては停止中。`index.html` と `public/sw.js` は旧Service Workerとキャッシュを解除するためのコードで、オフラインキャッシュ機能を提供しない。 |

## 3. 現在存在する機能

分類は「現在のコードに到達経路と実装があるか」と、引継ぎ資料の検証記録を基準にした。未実機検証は完成と断定しない。

| 分類 | 機能 | 根拠 |
|---|---|---|
| 完成（コード＋検証記録） | 通常戦闘、勝利／ゲームオーバー、10体ステージ、5体チェックポイント、経験値・進捗の保存 | `battleScreen.js`、`resultWinScreen.js`、`gameState.js`。2026-08-28スモークテスト記録。 |
| 完成（コード＋検証記録） | 今日の復習、SRSキュー、週次がんばり、空キュー時の案内 | `reviewQueue.js`、`reviewStage.js`、`quickReviewPracticeScreen.js`。2026-08-28の再修正・実機記録。 |
| 完成（コード＋検証記録） | 50音パッド、読みの「おしい」判定、保存スロット、旧Service Workerからの復旧 | `kanaPad.js`、`readings.js`、`saveSlots.js`、`index.html`。2026-09-04の実機／Node検証記録。 |
| 完成（コード＋検証記録） | 例文モード、弱点読み限定設定、ふりがな設定、文字拡大、色覚対応配色、ことわざ図鑑、学習記録CSV出力 | `exampleMode.js`、`readingScope.js`、`ruby.js`、`textScale.js`、`settingsScreen.js`、`proverbDexScreen.js`、`reviewExport.js`。直近コミットと検証記録。 |
| 完成（コード上） | タイトル、名前入力、国内／世界ステージ選択、プロフィール、実績、漢字・モンスター図鑑、モンスター捕獲、ボーナス、力だめし、練習バトル、チュートリアル | `setupFSM.js` に画面を登録し、各画面実装が存在する。すべての導線を今回実機確認したわけではない。 |
| 部分実装 | Firebase同期・復旧 | 実装と公開Firebase設定はある。ただしFirestoreセキュリティルールが追跡されておらず、コンソール側設定は未確認。 |
| 部分実装 | 読み上げ | 実装済みだが、実際に鳴ることはiPadで未確認。 |
| 部分実装 | セッション時間で区切るモード／色覚対応配色／ルビの密なUIでの表示 | 実装あり。時間切れ演出、HP減少時の配色、密なタブ等でのルビ表示は未確認。 |
| 未完成／未接続 | チャレンジモード | `settingsScreen.js` の作成処理が呼ばれておらず、利用者が切り替える経路がないと資料で確認されている。 |
| 未完成／未接続 | PWAのオフライン提供 | manifestは残るが、SWは自己解除専用であり、PWA機能は意図的に撤退中。 |
| 現在不明 | 本番デプロイ先、Firestoreの現行ルール、実運用時の同期・利用状況 | リポジトリ外の管理画面／DNS／実機・運用情報が必要。 |

## 4. アセットとデータ資産

| 種別 | 主な保存場所 | 確認量・内容 |
|---|---|---|
| モンスター画像 | `public/assets/images/monsters/`、`enemy/` | 画像は主に WebP。敵データは通常880、伝説120、世界129、ボーナス6、四国・九州120レコード。 |
| 背景・地図 | `public/assets/images/backgrounds/`、`stage.select/`、地域名の画像ファイル | ステージ／地域／世界用の背景と地図・マーカー。 |
| UI | `public/assets/images/ui/`、`icons/`、`icon/` | パネル、ボタン、読みに関するアイコン等。 |
| 効果音・BGM | `public/assets/audio/` | 521ファイル、約854.5MB。BGM、地域／エリア曲、戦闘・画面用の音源を m4a / ogg / mp3 で保持。 |
| ことわざ | `public/assets/images/proverbs/`、`public/data/proverbs_final_400.json` | ことわざデータ400件。 |
| 漢字／ステージ／実績データ | `public/data/` | 漢字10ファイル、通常88面、ボーナス12面、実績73件。 |

`public/assets` 全体は画像3,412ファイル（主にWebP、約142.5MB）と音声521ファイルである。画像名の重複・旧命名・未参照を完全自動照合する仕組みは確認できず、未使用アセットの厳密な件数は断定できない。履歴には、2026-09-04に未参照の制作素材1.95GBを `public/` 外へ退避した記録がある。

## 5. Git履歴からの時系列

全645コミット。作者別では `shimizu` 619、`shimimasa` 26コミット。コミット件数は変更量や実作業時間そのものではない。

| 時期 | 事実として確認できるフェーズ |
|---|---|
| 2025-05-12 | 最初のコミット（`c53e011`）。 |
| 2025-07-25〜08 | 漢字読みバトルRPGのプロジェクト一式を追加後、Vercel配布・パス問題を修正。8月は235コミットで、全画面表示／座標変換、国内・世界ステージ、シールド、敵バランス、設定、捕獲・図鑑・プロフィール、画像／音楽を急速に実装・調整。 |
| 2025-09〜10 | 236コミット。例文、復習・練習、チュートリアル、総復習、データ読み込み進捗、アクセシビリティ／保存設定、画像パイプライン・アセット改善などを継続。 |
| 2026-01 | 64コミット。保存の正史を `krb_save` に寄せ、Firebase同期を整理、画面遷移をFSMに集約、stageIdを正規化、アセットパス／SPA rewriteを修正。`dist` の追跡停止と構成整理も実施。 |
| 2026-02 | 児童の感情的安全性・動機づけ・認知負荷を対象とする専門レビュー文書を追加。 |
| 2026-07 | 28コミット。失敗時体験の再設計、SRS・復習統合、週次成長、チェックリスト、デッドコード／中間生成物の整理、Canvas共通化、力だめし公開。 |
| 2026-08 | 38コミット。監査で見つけた保存・図鑑・音・ステージ・SRSの不具合を修正。チェックポイント、ボーナス解放条件、子ども向け文言を改善。 |
| 2026-09-04〜05 | 38コミット。50音パッド、音声読み上げ、適応的な出題、セーブスロット、色覚対応、文字拡大、例文／弱点読み、教師向けCSV、ことわざ図鑑、画面崩れ・戦闘進行・力だめしを集中的に改善。直近はボーナス表示名・地図ピン、力だめし背景の調整。 |

## 6. 開発量が大きい領域

### 事実

- 現在の最大実装は `battleScreen.js`（約257KB）、`practiceBattleScreen.js`（約99KB）、`settingsScreen.js`（約88KB）、`worldStageSelectScreen.js`（約61KB）、`stageSelectScreen.js`（約54KB）。
- 選択画面4種（stage / worldStage / region / continent）は引継ぎ資料で約4,900行、コピペ率65〜75%と記録されている。
- 通常敵880、伝説120、世界129、四国・九州120、漢字2,136、通常88面／ボーナス12面というデータ量がある。
- 2025年8月（235）と9〜10月（236）にコミットが集中し、2026年1月（64）と7〜9月（104）にも大きな改修波がある。

### 推定

最も開発量が大きかったのは、(1) Canvas戦闘とその派生である練習・力だめし、(2) 国内／世界の地図選択とステージデータ、(3) モンスター・音楽・背景を含むアセット制作／統合、(4) 保存・復習・図鑑・同期を含む進捗システム、の順に見える。これはファイル規模、データ件数、コミット集中、監査／修正履歴に基づく推定であり、作業時間の計測ではない。

## 7. 残存する問題・技術的負債

### 確認済みの残件

- Firestoreセキュリティルールがリポジトリに無く、現行ルールをレビュー／履歴管理できない。児童データを扱う可能性があるため、引継ぎ資料では最優先とされる。
- Firebase／Vercel／CNAMEが併存し、本番配信元が未確定。
- 北海道専用BGMがなく、現在は汎用バトルBGMを代替使用している。
- `quickReviewPracticeScreen.js` に児童向けでない `alert` / `confirm` が残る。資料にはリポジトリ全体で `alert` 36箇所（うち「失敗」を含むもの10箇所）の監査記録がある。
- 旧／到達不能候補として、`result`、`gradeSelect`、`prefSelect`、importゼロと記録された `loadingScreen.js`、`tutorialScreen.js`、`tutorialBattle.js`、`states/regionSelectState.js`、孤立した `menuScreen.js` がある。今回、全呼び出しグラフを再構築してはいないため「削除可能」までは断定しない。
- `core/stateMachine.js` は `core/fsm.js` への移行後の廃止候補。`main.js` は本番時に `console.log/debug/info` を上書きする暫定策を採り、ログ移行は未完了（資料上447箇所）。
- `battleScreen.js` は分割予定、`practiceBattleScreen.js` は `{...battleScreenState}` と `.call()` による危険な継承と資料に記録される。テスト基盤は未導入。
- 選択画面群の重複、世界40面のデータ側プールが読み込みロジックで短絡される件、`kanto_area7` の95字プール、復習キュー既存項目の再誤答で予定を前倒ししない件、九州総復習の存在しないID 5件は、判断待ちまたは現状維持として残る。
- 一部伝説モンスターの名称・説明・豆知識と、`enemies_proto.json` の `ISK-E07` 説明／豆知識は教材的事実確認が残る。

### 検索結果

ソースコード中に明示的な `TODO` / `FIXME` / `HACK` は実質的に見つからなかった（`DEBUG = false` とデバッグログは残存）。ただし、残件は主に `docs/progress-handoff.md` と `docs/improvement-todo.md` に明文化されており、TODOコメントが無いことは負債が無いことを意味しない。

## 8. 再利用可能な資産価値

| 資産 | 再利用性（事実） |
|---|---|
| ゲーム基盤 | Canvas描画、FSM、画面管理、座標変換、入力、音声、イベントバス、セーブを分離したモジュール群。教育ゲームの派生に転用可能。 |
| 学習ロジック | 読み正規化・ローマ字変換、音訓／弱点、ヒント、例文、SRS復習、到達度・読めた回数の記録。 |
| データ | 漢字2,136件、通常／伝説／世界／地方モンスター計1,255レコード、通常88＋ボーナス12ステージ、ことわざ400、実績73。JSONで分離され、拡張・監査しやすい。 |
| UI・アクセシビリティ | 子ども向けCanvas UI、50音パッド、ふりがな、文字拡大、色覚対応配色、負の表現を抑えたフィードバック、チュートリアル。 |
| 進捗・運用 | `krb_save`、スロット、バックアップ、CSV書き出し、Firebase同期／復旧の土台。 |
| 配布・素材 | Viteビルド設定、Firebase／Vercel配備候補、大量の背景・モンスター・UI・音源。 |

再利用時には、Firebaseルールの管理、PWA方針、巨大な画面モジュールとアセットの参照整理を先に行う必要がある。

## 9. 実装から確認できる開発者の能力

### 客観的に確認できる能力

- **ブラウザゲーム実装**: Canvas描画ループ、ヒット判定・座標変換、画面状態遷移、モバイル表示、DOMとの混在、音声制御を実装している。
- **複雑な状態・データ設計**: 進捗、読みに別れた達成、復習、実績、捕獲、設定、スロット、旧保存形式の移行を実装し、2026年1月には保存の正史・同期の役割を明文化して整理した。
- **データコンテンツの構築と整備**: 2千字超の漢字、千件超のモンスター、100面のステージ、ことわざ・実績データをJSONで管理し、ID正規化・検証スクリプトも作成した。
- **品質改善とデバッグ**: Service Workerの古いキャッシュ、保存欠損、復習キュー、図鑑、遷移、画像パスなど、再現条件を伴う不具合を調査し修正した履歴がある。監査、スモークテスト、引継ぎ文書を残している。
- **教育UX・アクセシビリティの実装**: 誤答の否定的表現の削減、無料ヒント、再挑戦、チェックポイント、正答率を強調しない表示、ルビ・文字拡大・色覚対応、教師用CSVを実装した。
- **運用・配布の基礎**: Vite、Firebase Hosting／Auth／Firestore、Vercel、キャッシュヘッダー、匿名認証を扱っている。

### 推測に留めるべきこと

教育学的な有効性、UIデザインの完成度、セキュリティ運用の成熟度、テスト駆動開発の習熟度、実際の利用者への導入成果は、リポジトリだけでは評価できない。特にFirestoreルールと本番配信先はリポジトリ外で未確認であり、「安全に運用されている」とは断定できない。

## 調査上の留保

- 作業ツリーには、この調査前から `.firebase/hosting.ZGlzdA.cache` の変更と `.claude/` の未追跡ファイルがあった。本レポートではそれらを変更していない。
- Git履歴の最初は2025-05-12だが、現行ゲーム一式を追加したコミットは2025-07-25である。履歴全体には試作・アーカイブ・削除済み素材も含まれる。
- アセットの「未使用」は、パスを静的に検索するだけでは動的生成・データ参照を判別できないため、件数を断定していない。
- 実機未検証項目、Firebase Console・DNS・配布環境の状態、テスト未導入部分は本調査の範囲外である。

## 主な調査対象

- 実装: `src/main.js`、`src/init/fsmsetup.js`、`src/core/gameState.js`、`src/core/saveData.js`、`src/models/reviewQueue.js`、`src/screens/battleScreen.js`、`src/screens/practiceBattleScreen.js`、`src/screens/settingsScreen.js`、`src/loaders/dataLoader.js`、`src/loaders/assetsLoader.js`、`src/services/firebase/`、`src/ui/`
- データ／素材: `public/data/`、`public/assets/images/`、`public/assets/audio/`
- 設定: `package.json`、`vite.config.js`、`firebase.json`、`vercel.json`、`manifest.json`、`index.html`、`public/sw.js`
- 文書: `docs/architecture-constitution.md`、`docs/progress-handoff.md`、`docs/improvement-todo.md`、`docs/smoke-test-checklist.md`、監査資料
- Git: `git log --all`、コミット時系列・作者集計・コミット総数・各時期の件名
