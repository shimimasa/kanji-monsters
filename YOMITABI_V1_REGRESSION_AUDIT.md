# 漢字ヨミタビ 公開版v1 回帰監査

監査日：2026-09-06（JST）  
対象：`main`、HEAD `d58c4ce116ba4b029de5fb36ebfb4629ed5a89f6` に、作業ツリーのPHASE A/B/C変更を含めた現在のコード。  
立場：独立QA。元監査の37項目と完了条件を評価対象とし、修正済みという過去の報告を合格証拠には使わない。

## 公開判断：NO-GO

**まだ公開候補にできない。** 元のP0であるスロット切替時の消失と破損・不正バックアップによる上書きは、今回の障害注入でも防げた。しかし、練習で誤答すると再回答できない、保存失敗後に通常戦闘が入力不能になる、学年クイズの表示件数と保存記録が食い違う、というP1の回帰を再現した。モバイルの主要Canvas操作も元監査の寸法目標に達していない。

既存自動テスト125件と本番ビルドは成功した。これと公開可否は別である。共通関数の単体テストや「関数名がソースに存在する」検査だけでは、画面が次の入力を受け付けること、保存失敗後に表示を戻すことまで保証できていなかった。

## 方法・証拠の範囲

- `YOMITABI_QUALITY_AUDIT.md`、`PROJECT_EVIDENCE.md`、計画、現在の保存・入力・採点・画面・読み込み・配信設定を調査した。実装レポートの成功記載は、現在の実行結果で取り直した。
- 作業開始時にGit状態とブランチを確認。PHASE A/B/Cの多数の未コミット変更が既に存在した。コミット済みHEADだけを監査していない。
- 既存のA/B/Cテストを実行し、追加で独立した障害注入・画面メソッド実行を行った。ユーザーのStorageやFirebaseには触れず、メモリStorage、DOM入力の代用品、制御可能なタイマー、ローカルJSONを返すfetchを使用した。
- 通常戦闘・練習・復習・クイズは、実モジュールの回答処理を実行した。Canvasの描画・音声・ブラウザー操作は代用品または省略。名前決定は現在のメソッド本文を切り出し、クラウドPromiseを制御した。これらを実機通しプレイとは呼ばない。
- Browserスキルの接続・診断を行ったが、利用可能なブラウザーは `[]`。実画面のスクリーンショット、物理IME、タッチ、音の試聴は未実施。モンスター2点のローカル画像は今回も実見した。
- ビルドと追加再現スクリプトはOSの一時ディレクトリへ出力。アプリのコード、テスト、データ、アセット、既存`dist/`は変更していない。リポジトリへ追加した成果物は本書のみ。

以下で「再現」は上記の隔離環境での実行、「静的」は現在のコード・寸法式による確認。「FIXED」は当該不具合の確認可能な範囲が解消したことを指し、全端末・実クラウドの認証済み合格を意味しない。部分的に修正の証拠がある項目は、実機未確認部分を明記してPARTIALLY_FIXEDとした。

## 37項目の再分類

| 分類 | 件数 |
| --- | ---: |
| FIXED | 11 |
| PARTIALLY_FIXED | 22 |
| NOT_FIXED | 2 |
| REGRESSION | 2 |
| NOT_VERIFIABLE | 0 |
| 合計 | 37 |

NOT_VERIFIABLEが0件なのは、各項目についてコード・モジュール実行・素材の少なくとも一部を確認できたため。実機品質まで確認できたという意味ではない。確認不能な下位条件は末尾に独立して列挙した。

NEWは別枠で6件。元項目のREGRESSION判定との関連を示しており、37件に足して「43件の独立した原因」とは数えない。

### ゲームディレクター：8項目

| ID・元優先度 | 再分類 | 現在の証拠と残件 |
| --- | --- | --- |
| G01 P2 最初の一問までの負担 | PARTIALLY_FIXED | 起動画像の限定、地図の「もういちど押すと しゅっぱつ」を確認。ただし`playerNameInputScreen.js:215`付近はローカル保存後もクラウドをawaitし、保留Promiseでは遷移0回。最初の30秒・子ども単独開始は未測定。 |
| G02 P2 待ち時間と読み直す時間 | PARTIALLY_FIXED | 通常正解で500msの装飾解除と1,300msの次問予約を実測。通常誤答の1,300＋1,700msも残存。練習は1,100／2,200msへ変更されたが、実際の`reviewStage.js:144`は正誤とも1,000msで次へ進む。練習の再回答はNEW-01で停止。 |
| G03 P1 5体目の旗の消失 | FIXED | `gameState.js:448`付近で未クリア面のcheckpointを正規セーブへ保存、`:551`付近で再構築。Aの保存→再読込、クリア時解除テスト成功。独立スロット試験の元データにも旗5を入れ、失敗時保持を確認。物理ページ再読込の通し確認は未実施。 |
| G04 P1 復習0件からボーナスへ迂回 | FIXED | `stageSelectScreen.js`の総復習分岐は対象数によらず`reviewStage`へ進む。実`reviewStage.enter()`を空キューで実行し、入力非表示・空案内・1,800ms後のstageSelect遷移を確認。ボーナスへの代替遷移なし。Bの空状態経路検査も成功。 |
| G05 P2 ステージと出題範囲の不一致 | PARTIALLY_FIXED | 正常読込時、通常88面すべてで定義ID集合と`getKanjiByStageId()`の集合が一致。しかしgrade7だけ取得失敗させると読込全体は成功扱い、アジア初面は定義30字に対し小学6年の191字へ置換された（NEW-06）。 |
| G06 P2 学年直行時の戦闘負担 | PARTIALLY_FIXED | 実データと現行式で小1・小3・小6の先頭面は同じ係数0、通常HP31・ボスHP62・盾1になった。学年プールは維持。ただし弱点外の正解は盾中ダメージ1のまま、説明の理解・ボスまでの実時間・育成後再訪は未検証。数式合格をプレイ合格に置き換えない。 |
| G07 P2 上達の具体例不足 | PARTIALLY_FIXED | `resultWinScreen.js:431`付近に今回正解した字の例があり、初正解0でも具体例を出す。復習正解文も以前の復習項目への再接触を説明する。一方`profileScreen.js:210–228`の週次は支援込み累計を「よめた」と表示。子どもが変化を説明できるか未観察。 |
| G08 P2 終了の区切り・強制感 | REGRESSION | 自動継続を止める完了フラグは追加されたが、「Enterで もう1もん」は通常の空入力で`reason: blank`となり動かない（NEW-02）。専用短期復習の`quickReviewPracticeScreen.js:139`付近にはOK／キャンセル文言も残る。週次の不足回数を埋めさせる文言は一部改善。 |

### 子ども向け教材設計者：5項目

| ID・元優先度 | 再分類 | 現在の証拠と残件 |
| --- | --- | --- |
| T01 P2 測っている力と説明 | PARTIALLY_FIXED | コースボタンは「世界編（漢検4級〜2級）」へ変更。しかし`tutorial/tutorialData.js`のコース案内は今も世界編を「中学生の漢字／中学生レベル」と説明し、画面間で範囲が不一致。単字の読みと文脈理解の区別、例文対応範囲を全説明で確認できる状態にはなっていない。 |
| T02 P1 再誤答・戦闘内再出題 | FIXED | `commitLearningOutcome`→`ReviewQueue.applyOutcome`で既存項目にも品質を適用。Bで6日先→翌日、期限切れ初期項目の再誤答を確認。`takeDueRetry`は一時的な弱点候補外を保持し、ステージ外だけ削除する。画面もこの関数へ接続している。練習の「同じ字をその場で答え直す」障害はNEW-01として別に記録。 |
| T03 P1 復習の採点・連続確定 | REGRESSION | 元の復習Enter増殖は解消。実`reviewStage.enter()`で同じ正解を3送信しても正解1件・次問予約1個、空欄は0件だった。一方、共通確定ゲート導入後の練習は誤答後に再回答できず（NEW-01）、クイズは保存失敗再試行で画面集計が増殖する（NEW-04）。4画面全体の正しさを合格にできない。 |
| T04 P1 ヒント使用と自力正解の混同 | PARTIALLY_FIXED | 実通常戦闘でヒント0／1／2／3／4を通し、`independent`／`hint1`／`hint2`／`hint3`／`revealed`の保存を確認。自力時だけ読みマスターが進み、答え表示後はSRSへ戻る。CSVも分離。一方、漢字図鑑`Dex/kanjiDexScreen.js:820`は旧累計・支援込み`stats.correct`を「よめた回数」と表示し、週次も同様。全表示の意味は揃っていない。 |
| T05 P1 少数問から過大に断定 | PARTIALLY_FIXED | 学年クイズの「この学年はバッチリ」は除去され、今回の問題と自力正解数を使う。Bの8／10検査成功。ただし図鑑のマスター説明と`stageSelectScreen.js:552`の「学年漢字を全マスターで解放」は、読みの一回実績・レビュー解放フラグの違いを説明し切れていない。NEW-04では今回の集計自体も過大になる。 |

### UX / Visual Director：8項目

| ID・元優先度 | 再分類 | 現在の証拠と残件 |
| --- | --- | --- |
| U01 P1 モバイルの文字・操作実寸 | PARTIALLY_FIXED | DOM入力48px、かなキー44px、座標共通化・CSS集約は確認。しかし800幅Canvasは390pxで倍率0.4875のまま。攻撃50単位→24.375px、戻る36→17.55px、練習32→15.6px、文字16→7.8px。主要Canvas操作44pxの目標は未達。復習には座標不一致NEW-05もある。 |
| U02 P2 タイトルの小ささ・速い動き | PARTIALLY_FIXED | `titleScreen.js:24,209`で周期約314ms→6,283ms、ロゴ最大高150→216内部px、動き軽減時振幅0を確認。ただし390px幅では画像全体約105.3px高。名称の可読性・他要素との構図は実画面未確認。 |
| U03 P2 画面間の見た目の不統一 | PARTIALLY_FIXED | 名前・準備画面・共通ボタンの茶系統一は確認。一方、復習背景は`#1e3c72`、練習完了パネルは紺＋緑枠、既存の各描画規則も並存。今回再実見した2体にも輪郭・密度差はある。代表画面を並べた実表示検証がなく、全体の統一完了とは判定しない。 |
| U04 P2 情報量・案内の優先順位 | NOT_FIXED | `tutorialData.js`の戦闘は依然「かなで入力→Enter」、ヒント案内位置は右上。実ボタンは下方。前の漢字等の11〜14px文字や多数の情報欄も残る。入力枠の調整だけでは、この元指摘の主要部分は解消していない。 |
| U05 P1 Canvasの動き軽減 | PARTIALLY_FIXED | 通常フラッシュ・一部shake・ロゴ・粒子抑制は実装。しかし`battleScreen.js:3835`のレベルアップは設定を参照せず、50ms周期のCSS transformによる全Canvasの揺れを開始する。`matches:true`で実行して揺れを再現。シールド警告の点滅にも未対応経路が残る。 |
| U06 P1 通常戦闘の見えない戻る判定 | FIXED | 通常戦闘は描画・クリックとも`BTN.stage`を使い、旧`BTN.back`を先に拾う分岐が除去された。実handleClickへ390px表示の中央・四隅近くを与え、5点すべてstageSelectへ遷移。旧矩形定数は残るが、この画面のクリック経路では未使用。復習画面の別原因の戻る不具合はNEW-05。 |
| U07 P3 モンスター全体の仕上げ | NOT_FIXED | HKD-E01とAS-F01の現行画像を再実見。大きな単純輪郭と細密な竜の密度差は残る。全素材の仕上げ統一は実施されていない。意図されたv1対象外であり、単独の公開阻止理由にはしない。 |
| U08 P2 収集バーが常に100% | FIXED | `profileScreen.js:192`以降は漢字数・モンスター数の数値表示へ変更。現在数を分母にしたバーの呼び出しはなく、0／1／途中／全件とも割合を誤表示する式を通らない。`makeBar`関数自体の残存と使用の有無を区別した。 |

### シニアエンジニア：16項目

| ID・元優先度 | 再分類 | 現在の証拠と残件 |
| --- | --- | --- |
| E01 P0 スロット退避失敗でセーブ消失 | FIXED | 現在の`switchToSlot`をメモリStorageで直接実行。ジャーナル・元スロット退避・`krb_save`復元・番号・epochの5地点に継続的Quota例外を注入し、全てfalse、元セーブ一致、スロット1保持。Aの復元・番号失敗、正常1→2→1も成功。 |
| E02 P0 不正バックアップ・破損の先行上書き | FIXED | `{}`、null、無関係文字列、version999のimportは拒否され元データ不変。壊れたJSONと空文字列の読込も原文保持。Aの型検査、原文退避失敗、ロールバック復旧試験成功。保存先が保護されることと、破損から利用者が自力復旧できることは分ける（E04/E06）。 |
| E03 P1 保存失敗なのに成功表示 | PARTIALLY_FIXED | 設定画面の保存・exportは`result.ok`を確認。しかし`titleScreen.js:462–465`は戻り値を見ず「セーブしました」。`showSaveButton=1`相当のボタンを生成してQuota例外を注入し、元セーブ不変なのに成功toastを再現。通常戦闘の保存失敗後停止もNEW-03。 |
| E04 P1 クラウド復旧前の初期値生成 | PARTIALLY_FIXED | 欠損ローカルは復旧判定前に初期保存されず、既存有効ローカルはSDKを待たない。A/CテストとCDN拒否注入でも原本保護を確認。一方`saveBootstrap.js:6`はcorruptを即中止し、破損時のクラウド復旧経路へ入らない。実クラウドからの欠損／破損双方の復旧完了条件は未達。 |
| E05 P1 復元時のキュー・メモリ・クラウド不一致 | FIXED | 空配列も含むprojectionとメモリ再構築、queue読込更新を確認。Aの復習あり→空、進捗・図鑑・旗の置換、復元失敗ロールバック成功。ライブの旧DataSync適用は停止され、クラウドrevision競合では無条件上書きしない。実Firestoreルールは別途未確認。 |
| E06 P1 起動・通信失敗で停止 | PARTIALLY_FIXED | HTML同期SDKを除去し、ローカル有効時は先にタイトルとゲームを開始できる。JSON reject時の再試行UIはある。一方ロゴImage・必須fetch・認証・名前の同期awaitには未完了のまま待つ経路が残る。欠損セーブ＋CDN拒否は起動不可のまま安全に停止し、画面内での再試行は十分でない。NEW-06も参照。 |
| E07 P1 漢字ID重複・参照切れ | FIXED | 現在の2136件を独立再走査し、重複ID0・ステージ漢字参照切れ0・敵参照切れ0。旧衝突9組の移行は証拠なしの記録を`legacyAmbiguousKanji`へ保持し、推測分配しない。Aの9組の曖昧／明示証拠ケース成功。教材内容の全件校閲とは別。 |
| E08 P1 離脱後の処理残留 | PARTIALLY_FIXED | 実際のbattle exitで管理timeoutを解除し、generation判定も追加。レビュー・クイズ・stageLoadingにもlifecycleあり。ただしレベルアップのsetIntervalは管理外で、離脱後にcanvas=nullになると次回tickがTypeError。rAFによるvh-lock解除も次画面の世代を確認しない。名前のawait後遷移にも離脱確認がない。 |
| E09 P1 IME確定と回答確定 | PARTIALLY_FIXED | 共通ゲートがcomposition状態、isComposing、keyCode229を拒否し、名前／4学習画面に接続。疑似イベントによる変換中Enterと回答Enterの分離は成功。Windows／Safari等の実イベント順序、ソフトキーボード、実かなパッドは確認不能。これらを含む完了条件を合格扱いしない。 |
| E10 P1 クリア数二重加算 | FIXED | `resultWinScreen.js:79`は`recordStageCleared()`のみ。`gameState.js:168`もstageRunで二重確定を防ぐ。Aで通常・別run再クリアがそれぞれ＋1、同run再記録なし。旧過大累計を勝手に半減しない。 |
| E11 P1 起動素材・二重取得 | PARTIALLY_FIXED | 起動画像1件・31,234bytes、残り最大3並列、全データPromise共有を確認。Cの検査成功。ただし学校回線相当のcold start時間・実転送量は未測定。全学年データが先行することと、失敗／滞留経路も残るため、公開端末での完了条件は未確認。 |
| E12 P2 BGMフェードが次曲を停止 | FIXED | `audioManager.js:242`で停止対象を開始時のAudioへ固定。旧gameover停止と新battle再生の競合試験成功。曲の実聴・音量バランスは未検証。フェード速度のFPS依存はE13として残る。 |
| E13 P2 FPS依存・離席時間加算 | PARTIALLY_FIXED | 敵の主要3演出は500／750／1000msへ変更。frameClockは非表示時間・1秒超の復帰差分をプレイ時間から除外し、C試験成功。ただし`audioManager.js:307,323`付近のfadeは毎frame±0.016秒、練習粒子・levelUp等もframeカウンターが残る。全演出の秒数が揃ったとは言えない。 |
| E14 P2 PWA撤退・名称・更新 | PARTIALLY_FIXED | manifest／README／HTML名称とオンラインWeb方針は概ね統一。SW掃除は既知の本作キャッシュと同originの`/sw.js`へ限定。ただし旧SW実端末への到達、ホーム画面名、保存を維持した更新、クラウドの実アクセス制御は確認不能。 |
| E15 P2 キャッシュ設定の不一致 | PARTIALLY_FIXED | 背景のDate.nowクエリを固定版へ変更。hashedコードと固定画像のcache方針も整理。一方、配信設定の音声拡張子に実使用のm4aが含まれず、HTML再検証規則も明示は`/`のみ。実際の公開先・深いURL・素材差替え時の応答ヘッダーは未確認。設定JSONの構文成功だけで配信保証はできない。 |
| E16 P3 巨大モジュール・暗黙契約 | PARTIALLY_FIXED | 小さな保存・入力・採点・時間モジュールと125件のテストは追加された。一方battle約6,555行、practiceのオブジェクト展開による継承、第3引数を受けないeventBus、静的／動的import混在と519.13kB mainの警告は残る。全面改修はv1の条件にしない。 |

## NEW：今回追加で特定した6件

### NEW-01 P1／REGRESSION：練習の誤答後、正しい答えを送れない

関連：T03、T02、G08。対象：`practiceBattleScreen.js:344,1079–1086`、`answerSubmission.js:31`、`gameState.js:214`。

再現手順：正常な新規セーブをhydrateし、実練習画面の入力購読を登録。「一」に「ねこ」を確定→予約された2,200msコールバックを実行→「いち」を確定。

```text
最初の送信        accepted:true
誤答待ち終了後    battleState.inputEnabled:true / submission.locked:true
次の正解送信      accepted:false / reason:locked
記録              correct:0 / incorrect:1
```

原因：誤答後タイマーはゲーム側のinputEnabledだけ戻し、共通ゲートをunlockしない。さらに問題の`_recordQuestion`を使い回すので、unlockだけを仮に行っても`commitLearningOutcome`は`reason: duplicate`で正解を拒否する。短期復習もこのメソッドを継承している。

完了に必要な確認：誤答→答えの確認→正式な再回答を、二重送信とは異なる学習行為として扱う。同じ問題IDの連続入力を防ぎつつ、再回答では進行し、支援を受けた事実も正しく記録する。共通関数だけでなく画面の誤答コールバックまで実行する回帰テストが必要。

### NEW-02 P1／REGRESSION：練習完了の「Enterで もう1もん」が空欄で動かない

関連：G08、T03。対象：`practiceBattleScreen.js:344–352,2278–2303`、`answerSubmission.js:32`。

最終回答後は入力欄が空。完了処理はunlockするが、続行用コールバックより先に共通ゲートが空欄を拒否する。実購読で`practiceComplete=true`にして空欄Enterを送ると`reason: blank`、完了状態のままだった。子どもは案内通りに押しても進めず、何かを入力してから送るという案内外の操作が必要になる。

完了に必要な確認：回答の空欄拒否を維持し、完了画面の続行・終了を正しく受け付ける。続行だけで学習件数を増やさない。Enterとかなパッドの確定経路の両方で確認する。

### NEW-03 P1／REGRESSION：通常戦闘の保存失敗後、入力不能・表示の片進み

関連：E03、E05、T03。対象：`battleScreen.js:4774,4823–4834,5246,5407,5492`、`learningOutcome.js`。

正常セーブで実`handleAttack()`を入力購読経由で実行し、`yomitabi_phase_a_pending`への書込にQuota例外を注入。記録のトランザクションは失敗を返し、元セーブは完全一致で残る。しかし呼出元は`inputEnabled=false`のままreturnする。

```text
保存原文保持      true
学習記録          追加なし
inputEnabled      false
submission.locked true
再送信            reason:locked
correctKanjiList   1
player.comboCount 1
```

保存・学習コアは巻き戻せているが、画面側の成功リスト・コンボは先に進んでいる。回復、誤答にも同様の失敗returnがある。これは既存セーブ消失の再発とは区別する。

完了に必要な確認：保存失敗時に子どもへ状態を返し、入力を回復する。記録と成功リスト等が同じ解答を表す。Quota注入→解除→同じ問題で再試行する画面テストで確認する。

### NEW-04 P1／REGRESSION：学年クイズの保存再試行で表示の正解が増殖

関連：T03、T05、E05。対象：`gradeQuizScreen.js:247–259`。

実`_checkAnswer()`で「いち」を入力し、最初の保存だけQuota例外→容量不足を解除→同じ問題を再回答。

```text
初回戻り値       false
再試行戻り値     true
画面stats.correct 2
画面answers件数   2
保存correct       1
```

画面の件数・answers配列を増やしてからcommitするため、失敗時に表示側だけ残る。結果の自力正解集計もこの配列を使うので、今回実際に確認した件数を超える結果になり得る。

完了に必要な確認：保存成功と画面結果の確定を揃え、失敗時に余分な結果を残さない。失敗を挟む複数回再試行でも、1問題の結果・保存・次問予約が各1件になること。

### NEW-05 P1／新規発見：復習の戻るボタンが表示位置と一致しない

関連：U01、U06、E09。対象：`reviewStage.js:79–86,184`付近。

Canvas内部800×600、表示390×292.5、左上(0,0)の条件。描画ボタンは内部`x20,y20,w100,h30`。実`enter()`が登録したclickハンドラーを実行した。

```text
見えるボタン中央 CSS(34.125,17.0625) → 遷移0回
見えない旧座標   CSS(70,35)          → stageSelectへ遷移1回
```

`clientX-left`と`clientY-top`をそのまま内部座標と比較し、縮小・黒帯を反映していない。通常戦闘のU06修正はこの別画面へ及んでいない。これは今回の変更が作ったと断定せず、今回新しく再現した既存漏れとする。

完了に必要な確認：描画と当たり判定に同じ座標変換・矩形を使い、中心と四隅で戻れること。かなパッド表示・縦横・黒帯ありでも同様に確認する。

### NEW-06 P1／新規発見：世界の教材取得失敗を別学年の問題で代用する

関連：G05、E06、T01。対象：`dataLoader.js:59–91,443–450`。

ローカルの現行JSONを返すfetch代用品で、`kanji_g7_proto.json`だけ503相当、それ以外は成功とした。

```text
loadAllGameData成功扱い  true
ステージ                 asia_area1
定義された対象字数        30
実際の取得字数            191（小学6年の代用）
```

上級学年の失敗を握りつぶして小学6年を代用し、明示プールが0件なら学年fallbackを返す。読み込み結果は共有Promiseに成功として残るため、単純な再呼び出しでも再取得されない。正常時の定義集合一致テストでは見つからない。以前からある代用経路を今回の異常系で新たに特定したものであり、コード変更による新規発生とは断定しない。

完了に必要な確認：取得できていない教材を成功扱いせず、元の範囲を再取得できる。子どもの正誤記録を別学年の問題で増やして隠さない。

## 重点項目の再検証結果

| 領域・失敗条件 | 実行結果／確認の限界 |
| --- | --- |
| 保存容量不足 | 保存APIは失敗を返し原本保持。ただしタイトルの成功toast残存、戦闘は入力不能（E03、NEW-03）。 |
| 不正JSON・空文字・未知version | corrupt／unsupportedとして原文保持。`{}`等のバックアップで上書きしない。空データと「正しい形式の空セーブ」は区別。 |
| バックアップ復元失敗 | Aでprojection書込失敗・退避失敗を再実行。元セーブとメモリの保持成功。設定のimportはsaveNow成功後にhydrateする。実際のファイル選択UIは未操作。 |
| 復習あり→正常な空バックアップ | Aでqueue、answers、図鑑、進捗、旗を空へ置換でき、以前のメモリが再保存されないことを確認。 |
| スロット | 独立の継続Quota注入5地点すべて元データ・番号保持。Aの1→2→1、対象破損、番号失敗、旧スロット移行成功。 |
| クラウド復旧前の初期値 | 欠損＋復旧失敗でも`krb_save=null`を維持。原本保護は成立。有効ローカルのSDK非待機も成功。破損からの復旧は入口で停止。サーバー実接続なし。 |
| チェックポイント | Aで旗5の保存・再読込・クリア時解除成功。Storage障害時の保持も確認。画面で5体目を倒して物理再読込する操作は未実施。 |
| 既存復習項目の再誤答 | Bで6日先→翌日、記録とqueueの同時保存成功。queue.addの古い「既存なら何もしない」処理は残るが、通常採点はapplyOutcomeを使う。 |
| 戦闘内の再出題 | 弱点候補外で予約を保持、適合時に取得する試験成功。実画面のpickWeightedKanjiからtakeDueRetryへの接続も確認。 |
| 連続Enter・空欄・近似入力 | B成功。加えて実復習画面で3連続送信→1正解・1予約、空欄0記録。練習の誤答後とクイズの保存再試行は失敗。 |
| ヒントなし／1〜3／答え表示 | 実通常戦闘で0〜4の保存分類が分離。支援なしのみ読み進捗を更新。答え表示後正解はゲーム上の正解1件を保ち、interval1・repetition0で復習へ戻る。 |
| ID | 漢字2136、重複0、ステージ漢字参照切れ0、敵参照切れ0。旧衝突の曖昧記録を9組で保全。 |
| 画面離脱 | 管理timeout／guard試験成功。ただしレベルアップintervalの残存を再現。名前決定のクラウド完了後も離脱を確認せずcourseSelectへ遷移することをメソッド切出しで確認。 |
| IME | 共通ゲートのcomposition、isComposing、229を疑似イベントで確認。物理IMEでのcompositionendとkeydownの順序は未確認。 |
| 起動失敗 | 有効ローカルのSDK非待機成功、欠損＋CDN拒否は安全停止。必須データrejectのUI分岐あり。Image・fetchがsettleしない場合の上限はなく、世界教材失敗はNEW-06。 |
| モバイルUI | 390幅でCanvas操作実寸未達。実復習ハンドラーの座標ズレを再現。DOM最小寸法の存在だけで全画面を合格にしない。 |
| 復習対象0件 | 空表示へ入る実装と分岐試験を確認。未解放ボーナスへ進まない。空表示は1.8秒後に地図へ戻る。 |

### 難易度：現在の数値

新規セーブ相当のLv1・攻撃10・最大HP100で、実ステージ順と現行関数を実行した。これは戦闘画面を最初からボスまで通した測定ではない。

| 開始 | ステージ | 段階 | 対象漢字 | 通常HP／攻撃／EXP | ボスHP／攻撃／EXP | 盾 |
| --- | --- | ---: | ---: | --- | --- | ---: |
| 小1 | hokkaido_area1 | 0 | 40 | 31／9／26 | 62／11／36 | 1 |
| 小3 | kanto_area1 | 0 | 35 | 31／9／26 | 62／11／36 | 1 |
| 小6 | chugoku_area1 | 0 | 35 | 31／9／26 | 62／11／36 | 1 |

- 漢字の難しさ：各学年の内容を維持。正常時の88通常面は定義集合に一致。
- ゲーム操作への慣れ：敵係数は各学年内の順番で上がり、先頭面は同条件になった。
- 必要正解数：乱数・弱点・育成を除くダメージ10固定なら通常4回、ボスHP部分7回＋盾1回。弱点1.5倍を使う実プレイとは違う基準値であり、「全員8回でボスを倒せる」とは言えない。
- 音訓：通常の読み限定は既定OFF。盾中は弱点系統の読みが必要で、別系統の正解は1ダメージのまま。漢字の読み自体とルール理解の難しさは残る。
- 既存セーブ：旧ベストタイムは残し、新規記録は`stageId@balance-2`。以前のタイムとの直接比較は分離されている。
- 未確認：小1・小3・小6での通し正解数、誤答数、所要時間、ヒント、回復、中断、育成済み再訪、子どもが盾の意味を理解するか。

### 保存・学習の修正による影響の読み方

旧衝突IDの記録は、漢字所有の証拠がないものを新IDに分けず退避している。これは推測による補正を避ける適切な挙動であり、その字の表示上の実績が直ちに戻らないことをデータ削除とは扱わない。旧累計も支援なし正解へ作り替えていない。

今回再現した保存失敗は、既存の正規セーブを消さなかった。ただし、保存できなかった新しい解答の表示・再試行まで一貫しているわけではない。原本保全の成功から、ゲーム状態・結果表示の合格を推論しない。

## 実行した検査と再現資料

| 検査 | 今回の結果 |
| --- | --- |
| `npm.cmd run test:phase-a` | 86／86 PASS |
| `npm.cmd run test:phase-b` | 22／22 PASS |
| `npm.cmd run test:phase-c` | 17／17 PASS |
| 独立Storage障害注入 | 5地点のスロット失敗で原本保持、4種の不正import拒否、3種の不正ローカル原文保持 |
| 独立ID／定義検査 | 重複0・漢字参照切れ0・敵参照切れ0、正常時88通常面の集合不一致0 |
| 独立画面処理再現 | NEW-01〜05、E03のタイトルtoast、U05/E08のlevelUp、名前同期待ちを再現 |
| 独立教材取得失敗 | grade7のみ失敗で30字→小学6年191字の代用を再現 |
| 本番ビルド | PASS、Vite 5.4.19、89 modules、main 519.13kB／gzip154.50kB |
| Git差分検査 | `git diff --check`成功。既存の改行コード警告あり。開始・終了時の変更ファイル一覧の差は本書の追加のみ |
| 実ブラウザー | 利用可能0件。実機・スクリーンショット・音試聴の合格証拠なし |

ビルドコマンド：

```powershell
npm.cmd run build -- --outDir C:/Users/socce/AppData/Local/Temp/yomitabi-regression-z7sgUE/build
```

今回作成した隔離再現スクリプトとJSON結果は、`C:/Users/socce/AppData/Local/Temp/yomitabi-regression-z7sgUE/`に保持した。アプリのソースへは追加していない。

```powershell
node --experimental-default-type=module C:/Users/socce/AppData/Local/Temp/yomitabi-regression-z7sgUE/probes.mjs
node --experimental-default-type=module C:/Users/socce/AppData/Local/Temp/yomitabi-regression-z7sgUE/battle-probes.mjs
node --experimental-default-type=module C:/Users/socce/AppData/Local/Temp/yomitabi-regression-z7sgUE/safety-probes.mjs
node --experimental-default-type=module C:/Users/socce/AppData/Local/Temp/yomitabi-regression-z7sgUE/loader-probe.mjs
node --experimental-default-type=module C:/Users/socce/AppData/Local/Temp/yomitabi-regression-z7sgUE/empty-back-probe.mjs
```

`probes.json`、`battle-probes.json`、`safety-probes.json`が観測結果。loader-probeは標準出力へ結果を出す。一時ディレクトリはOSによる削除があり得るため、重要な入力条件と観測値は本書にも残した。

既存テストの限界：Bの`screen-wiring.test.mjs`は主にソース文字列検査であり、誤答タイマー後に実入力を送っていない。完了テストも完了フラグとEnter案内の存在確認で、空入力を送っていない。Cのモバイル検査は座標式・CSS記載を確認するが、Canvasボタン実寸が44px以上であることを確認していない。今回の不具合が全125件成功と両立する理由はここにある。

## この環境ではNOT_VERIFIABLEの条件

| 対象 | 未確認の内容 |
| --- | --- |
| 実機／実画面 | iPhone・iPad・Android・Windowsの初回開始、文字・ルビ・文字拡大、かなパッド／ソフトキーボード、縦横切替、主要ボタン同時表示、画面ごとの統一感 |
| 実IME | 変換開始・確定と回答確定の実際のイベント順序、端末差 |
| 子どもの利用 | 最初の30秒、自力で開始・中断できるか、誤答文を読む時間、ボスルールの理解、楽しさ・上達実感 |
| 通信／配信 | 学校回線相当cold start、公開先の実ヘッダー、旧SWからの更新、ホーム画面起動、素材差替え時の反映 |
| クラウド | 実Firestore上の復旧・競合・スロット分離、他ユーザーの記録を読み書きできないこと、匿名認証の保持と起動時確定 |
| 音 | 自動再生制約、形式fallback、読み上げとの競合、音量・ループの実聴 |
| 教材内容 | 全読み、全例文、モンスター説明等の教育的校閲。ID構造検査の成功で代替しない |

## 公開候補へ進むための再判定条件

新機能追加を求めるものではない。現在ある操作・保存・学習表示を次の単位で閉じる必要がある。

1. 練習の誤答→再回答と、完了→続行／終了を修復し、実画面メソッドを通す回帰検査を追加する（NEW-01／02）。
2. 保存失敗後の通常戦闘とクイズを復旧可能にし、表示・学習記録・再試行を一致させる。タイトルも未保存を成功表示しない（NEW-03／04、E03）。
3. 390px幅と対象タブレットで問題・入力・決定・戻るの実寸を満たし、復習の戻る座標を修正する（U01、U04、NEW-05）。
4. 起動・教材・名前確定の通信失敗／保留を処理し、別教材へ黙って代用しない。破損データは保持したまま復旧の行き先を明確にする（E04／06、NEW-06）。
5. レベルアップ等の離脱後処理と動き軽減漏れを閉じる（E08、U05）。
6. 図鑑・週次・チュートリアル・ボーナス説明を、実際の支援状況と学習範囲へ揃える（T01／04／05、G07／08）。
7. 上記のP1を解消後、実端末・実IME・公開先更新・クラウドアクセス制御・教材校閲の確認結果を揃える。小1・小3・小6の実通しプレイを含めてGOを再判定する。

現時点の判定は **NO-GO**。理由は実機検証がないことだけではなく、現在のコードで主要操作と学習結果の不整合を再現できたためである。
