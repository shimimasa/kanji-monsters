# PHASE A 実装報告

実施日: 2026-09-06。対象は実装計画の A01〜A04。基準資料は `YOMITABI_QUALITY_AUDIT.md`、`YOMITABI_V1_REMEDIATION_PLAN.md`、`PROJECT_EVIDENCE.md`。

PHASE A の実装と自動回帰検証を実施した。最終結果は **86件成功、0件失敗、0件スキップ**。ビルド成功。公開・デプロイ・コミットはしていない。PHASE B〜D の実装には着手していない。

## 1. 作業開始時の確認と変更ファイル

開始時は `main`、HEAD `d58c4ce`。`git status` と未コミット変更を確認した。既存変更は `.firebase/hosting.ZGlzdA.cache`、未追跡の `.claude/` と上記3資料。これらを削除・上書きしていない。3資料とFirebaseキャッシュのSHA256は開始前と作業後で一致した。

今回変更した既存ファイル:

| 区分 | ファイル |
| --- | --- |
| 保存・状態 | `src/core/saveData.js`、`src/core/saveSlots.js`、`src/core/gameState.js` |
| 起動・読込 | `src/main.js`、`src/loaders/dataLoader.js` |
| 復習・図鑑 | `src/models/reviewQueue.js`、`src/models/kanjiDex.js`、`src/models/monsterDex.js` |
| 保存操作・名前 | `src/screens/settingsScreen.js`、`src/screens/titleScreen.js`、`src/screens/playerNameInputScreen.js` |
| 記録の接続 | `src/screens/battleScreen.js`、`src/screens/practiceBattleScreen.js`、`src/screens/quickReviewPracticeScreen.js`、`src/screens/reviewStage.js`、`src/screens/gradeQuizScreen.js`、`src/screens/resultWinScreen.js` |
| クラウド | `src/services/firebase/firebaseController.js`、`src/services/firebase/dataSync.js` |
| カタログ | `public/data/kanji_g5_proto.json`〜`public/data/kanji_g10_proto.json`、`public/data/stages_proto.json`、`public/data/stages.bonus.json` |
| テスト実行口 | `package.json`（`test:phase-a` の追加のみ。依存パッケージの追加なし） |

新規ファイル:

- `src/core/storageTransaction.js`: 複数キー更新のジャーナル、検証、ロールバック、再起動時回復。
- `src/core/saveValidation.js`: 保存形式・版・データ型・危険なキーの検証。
- `src/core/saveProjection.js`: 確定した保存から互換キーを生成。
- `src/core/kanjiIdMigration.js`: 重複IDの対応表、曖昧記録の保全、カタログ検証。
- `src/core/saveBootstrap.js`: 復旧確認と状態読込の順序。
- `src/services/firebase/cloudSave.js`: 完全スナップショットの復旧と基準リビジョンを照合する同期。
- `tests/phase-a/` の13テストファイルと `storage-helper.mjs`。
- `docs/PHASE_A_DATA_MIGRATION.md`: ID変更前に作成した移行設計。
- 本報告書。

画像・音声・3Dアセット、TypeScript、戦闘難度、世界編の問題プール選択、入力の共通化、SRSの採点規則は変更していない。

## 2. 監査IDへの対応

| 監査ID / 計画 | 調査した失敗と修正 |
| --- | --- |
| E01 / A01 | スロット退避の失敗後にも削除・復元が進む。元スロットの保存、対象キーの置換、スロット番号更新をジャーナルで保護し、途中失敗は元へ戻す。回復自体が失敗した場合はジャーナルを残して次回回復し、それまでは保存を止める |
| E02 / A01 | 不正JSON、無関係なJSON、空の本体、型不正、未知版を拒否。読み取りで本体の初期値を生成しない。明示的な取込は検証・原文保全後に確定する。現在の端末の旧キーを取込ファイルへ混ぜない |
| E03 / A01 | `saveNow` / `saveGameData` が確定結果を返す。手動保存・バックアップ出力は固定時間待ちを廃止し、実際の成功を確認する。名前保存や初期化操作も失敗を確認する。ファイル読込中に保存の文脈が変われば取込を中止する |
| E07 / A02 | 重複9組の18文字へ固有IDを付与。曖昧な旧IDを現役IDとして残さない。文字情報が一致した記録だけ移し、IDのみ・矛盾する文字情報・移行先衝突は保全する。実在しない漢字参照42件を除去し、敵参照 `EHM-E02` を実在する `EHIM-E02` へ修正。古い重複カタログを実行時にも受け入れない |
| E04 / A03 | 旧ローカル記録の移行、クラウド復旧確認、状態読込、ゲーム開始の順にする。クラウドが空でも初期セーブを作らない。通信失敗時は既存ローカル記録を使えるが、未確認の空状態を新規ユーザーと断定しない。読込できない場合も既存の設定・バックアップ取込・スロット選択へ到達でき、ゲーム開始は止める |
| E05 / A03 | 復習・図鑑・解答・進行を空も含めて置換する。復習モデルは古いモジュール内配列を使い続けない。図鑑・復習の変更は現在の学習スナップショットとともに保存する。旧クラウドの通知でミラーだけを直接上書きする経路を止める |
| G03 / A03 | ステージ別checkpointを保存・復元する。5体撃破後の旗が再読込後も5として残り、クリア確定後は外れる。過去に保存されなかった旗を推測生成しない |
| E10 / A04 | 勝利画面の余分なクリア加算を削除。戦闘ごとの識別子で同一クリアを一度だけ記録する。再描画・再確定で増やさず、次の正式な周回は加算する。過去の値は半減しない |
| T04 / A04 | PHASE Aに含まれる支援区分の記録を実装。問題識別子、読み、解答元、自力・ヒント1〜3・答え表示・支援不明を記録し、累計・日別の新規観測と読み進捗へ保持する。「おしい」で答えを提示した場合も答え表示扱い。旧正解・旧マスターは維持し、自力習得へ変換しない。入力受付やSRS規則の統一はPHASE Bに残す |

名前変更に使われていたクラウド初期化関数が、レベル・経験値を初期値に戻す経路も修正した。これはA01/A03の保存呼出し監査に直接必要な範囲。

## 3. 新規テスト

| ファイル | 主な検証 |
| --- | --- |
| `save-safety.test.mjs` | Storage容量不足、不正JSON、未知版、空データ、退避失敗、復元途中の失敗、スロット番号更新失敗、1→2→1 |
| `save-integration.test.mjs` | 実際の保存失敗の戻り値、途中停止後の回復、回復不能ジャーナルの保持、取込原文の保全、破損・未知版ジャーナル |
| `validation.test.mjs` | 版だけある空本体、読み進捗・日別集計・ID・checkpoint・設定の型境界、未知の記録版、プロトタイプキー、不正ミラー、明示的初期化と保護コピー |
| `catalog.test.mjs` | 全2136文字、重複ID 0、漢字参照切れ 0、敵参照切れ 0、各ステージの敵10体を維持 |
| `catalog-loader.test.mjs` | 古いカタログをロード対象にしない、新しい2文字のIDを別々にロード |
| `id-migration.test.mjs` | 9組それぞれのIDのみ／文字付き記録、冪等性、入力非破壊、矛盾する文字情報、衝突、原文退避の容量不足 |
| `restore.test.mjs` | 復習あり→空、解答・図鑑・ステージの置換、5体目checkpoint、取込ミラー書込失敗、旧形式、旧クリア回数とチュートリアルフラグの往復 |
| `context.test.mjs` | 読込前の保存禁止、旧コードの上書き検知、別の子のメモリからの保存禁止、非アクティブスロットの遅延移行 |
| `bootstrap.test.mjs` | クラウド待ち中に初期値を作らない、通信失敗時の分岐、旧ローカル学習記録の優先保全 |
| `cloud.test.mjs` | 完全復旧、空リモート、遅延応答とスロット切替、未知版、リビジョン分岐、改名で育成値を維持、checkpoint/SRS情報の同期、復旧時容量不足、旧ステージ文書、明示的クラウド初期化の失敗と対象スロット |
| `model-storage.test.mjs` | キュー・図鑑と学習記録の同時スナップショット、失敗時にミラーだけを進めない |
| `recording.test.mjs` | 支援7条件、各解答元、同一問題の重複、正式な再出題、既存累計を保持したクリア＋1 |
| `quick-review-recording.test.mjs` | 実際のクイック復習の2つの出題メソッドで問題識別子と支援状態を更新し、同一確定を二重記録しない |

各不具合の修正前に失敗するテストを実行してから修正し、対象テストとPHASE A全体の回帰を繰り返した。最初の保存テストは11件中10件失敗、カタログ検査は3件すべて失敗、9組の移行テストは当初19件すべて失敗していた。

## 4. 実行した検証

```powershell
npm.cmd run test:phase-a
$phaseABuild = Join-Path $env:TEMP 'yomitabi-phase-a-build'
npm.cmd run build -- --outDir $phaseABuild
git diff --check
```

追加の読み取り検査で、HEADの漢字データと今回の全2136行を比較し、ID以外の全フィールドが一致することを確認した。原本資料とFirebaseキャッシュはSHA256で照合した。ビルド出力は一時ディレクトリへ置き、既存の`dist`と配信キャッシュを更新していない。

## 5. 成功結果

- 自動テスト: **86 / 86成功**、失敗・スキップ0。最終コマンドの終了コード0。
- カタログ: **2136文字維持、重複ID 0、漢字参照切れ 0、敵参照切れ 0**。
- ビルド: 成功、終了コード0。
- `git diff --check`: 成功、終了コード0。
- 既存資料3件とFirebaseキャッシュ: 開始前のハッシュと一致。

## 6. 既存データへの影響

保存形式は2、漢字カタログ版は2、新しい支援区分の記録版は1として区別する。旧`kanjiGameSave`および形式1の正規保存を検証して移行する。新しい形式では少なくともレベル・経験値が有効であることを要求し、不正な断片から育成状態を推測しない。

移行・明示的取込・初期化の前には、旧コードのスロット削除対象外である`yomitabi_preserved_*`に原文と関連キーを保全する。新版の確定コピーは`yomitabi_confirmed_<slot>`へ保持する。容量不足なら元データを消して空きを作らず失敗として停止する。未使用スロットは一括上書きせず、開いたときに移行する。

曖昧な重複IDの情報は`player.study.legacyAmbiguousKanji`へ元の場所・値を保持する。現役の習得・SRSからは除外するため、その分は新IDの「習得済み」や「今日の復習」に直接は出ない。これは記録の分配や削除ではない。原文と曖昧情報は残り、文字情報で判別できる記録だけ正規IDへ移る。育成値、捕獲、クリア総数、既存の解放・獲得済み実績を推測で補正していない。

新しいクラウド保存は従来の子ごとのパス配下の`progress/save_v2`に完全スナップショットを保存する。スロット1と2・3のパス分離を維持する。旧`profile/playerStats`・`progress/state`・ステージ文書は復旧資料として扱い、取得原文を保全する。通常同期で旧summaryを上書きしない。旧通知による常時ミラー書換は廃止し、起動時復旧と確定保存後の同期を使う。分岐は両側を保持し、自動合算・自動上書きを止める。

音量・入力方式などの共有設定はスロット切替で動かさず、子ごとの表示・読み設定と進行データを切り替える。バックアップ取込は空の復習・空の進行も含む置換。

## 7. 残存リスクと未検証

- 実際の子のセーブ、本番Firestore、Firebaseセキュリティルール、実端末のブラウザー・IME・PWAは操作していない。クラウドテストは合成データとFirestoreのモックであり、本番通信の合格を意味しない。
- `localStorage`自体は複数キー・複数タブのDBトランザクションではない。ジャーナルと確定コピー、文脈照合で保護するが、実ブラウザーの完全同時書込み・強制終了・OSによるStorage削除は実機未検証。未確定の進行まで無条件に復旧できる保証はない。
- 退避・確定コピー・ジャーナルの分だけ保存容量を使う。容量が足りない端末では安全に停止する。保全データを勝手に間引く仕組みは追加していない。
- 帰属不明の重複ID、旧クラウドに元々保存されていなかった解答履歴や旗、旧正解のヒント使用状況は復元できない。旧クラウド由来の不完全な復旧には由来を記録する。
- クラウドで履歴が分岐した場合の手動統合UIは追加していない。自動同期を止め、両方を保全する。安全な解決手順は公開前に検証が必要。
- ビルドには約508 kBの主要チャンク、動的/静的import併用、ViteのCJS APIに関する警告が残る。起動性能や構造の全面整理は今回の範囲外。
- 手動での保存ボタン・ファイル選択・3スロット切替・旗からの実戦再開・旧実績表示の通し操作は、実ブラウザーで未実施。合成セーブを使う公開前検証として残す。
- PHASE BのIME/連打の共通処理、画面タイマー全体の所有者管理、SRS規則の接続、PHASE CのモバイルUI・見た目・難度調整には着手していない。
