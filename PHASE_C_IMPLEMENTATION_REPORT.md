# 漢字ヨミタビ PHASE C 実装レポート

作成日: 2026-09-06

## 結論

`YOMITABI_V1_REMEDIATION_PLAN.md` の C01〜C06 を、公開版v1の10項目に含まれる範囲で実装した。PHASE A/B の保存、復習、採点、入力確定、画面ライフサイクルの契約は維持しており、PHASE A 86件、PHASE B 22件、PHASE C 17件の自動テストと本番ビルドが成功した。

新しいゲームモード、収集・成長・ランキング、3D、モンスターや背景の全面描き直しは追加していない。PHASE Cではコード、HTML/CSS、配信設定、名称、説明だけを変更し、漢字・ステージデータと画像・音声アセットは変更していない。

## C01: 起動と初回導線（E06、E11、G01）

- Firebase SDKの同期scriptをHTMLから外し、タイトル表示後に必要時だけ読み込むようにした。
- 有効な既存ローカルセーブはクラウドSDKを待たず読み込む。ローカルセーブ欠損時はPHASE Aの保全契約どおり、クラウド復旧確認が終わるまで新規初期値を生成しない。
- 起動時画像を41件からロゴ1件へ減らした。残りはセーブ確認後のアイドル時に最大3並列で読み込む。
- JSONの全体読込をPromiseで共有し、同時呼出しによる二重取得・二重構築を止めた。中学相当4学年の取得も直列から並列へ変更した。
- 必須データ取得に失敗した場合は、止まった進捗画面の代わりに「もういちど 読みこむ」を表示する。
- ステージ選択時に「もういちど押すと しゅっぱつ」と明示した。

| 起動前の共通画像 | 変更前 | 変更後 |
| --- | ---: | ---: |
| 件数 | 41 | 1 |
| ファイル容量合計 | 24.26 MiB | 0.03 MiB（31,234 bytes） |
| 削減率 | - | 99.9% |

## C02: モバイル表示・入力・座標（U01、E09、U04）

- 4:3 Canvasの実表示領域を求める計算を共通化し、入力座標、ゲーム座標からDOM座標への変換、戦闘入力欄の配置を同じ計算へ接続した。
- HTML入力欄は最小48px、18px文字にした。名前入力欄も画面幅内で最低240px、48px高を確保した。
- 50音パッドのキーと入力方法切替は最小44px高にした。
- `index.html`のインライン寸法と`style.css`の競合を解消し、50音パッド表示時も4:3を保つ規則を1か所へ集約した。
- 幅390px縦持ちではCanvas実表示倍率が0.4875、44pxをCanvas内部寸法へ換算すると91単位になることを境界値テストで固定した。

## C03: テンポ・時間・動き軽減（U05、G02、E13、U02）

- 戦闘の主要演出タイマーをフレーム数からミリ秒へ変更し、描画中の二重減算を除去した。
- プレイヤーHP表示の追従速度を2 HP/frameから120 HP/secへ変更した。
- フレーム差分は100msで上限を設け、1秒を超える復帰差分と非表示中の時間をプレイ時間へ加算しない。
- 練習・復習の正解表示を1,000msから1,100ms、不正解表示を1,000msから2,200msへ変更した。
- タイトルロゴの上下動周期を約0.31秒から約6.28秒へ落とし、振幅を8から5へ変更した。OSの動き軽減時はロゴ上下動、タイトル粒子、戦闘の揺れ・全画面フラッシュ・シールド破壊粒子を止める。
- タイトルと結果背景のランダム模様を固定配置にし、毎フレームのちらつきを除去した。

| 敵演出 | 変更前（30/60/120fps） | 変更後 |
| --- | --- | ---: |
| 被ダメージ | 約0.50 / 0.25 / 0.13秒 | 0.50秒 |
| 敵攻撃 | 約0.75 / 0.38 / 0.19秒 | 0.75秒 |
| 撃破 | 約2.00 / 1.00 / 0.50秒 | 1.00秒 |

## C04: 既存2D画面の仕上げ（U02、U03）

- ロゴの最大表示高を画面高の25%から36%へ拡大し、歓迎表示との余白を調整した。
- 名前入力、ステージ準備、共通ボタンをタイトルと同じ茶・ベージュ系の背景、枠、主ボタンへ合わせた。
- ステージ準備の文字を16pxから18px、見出しを26px太字にし、進捗の情報順を整理した。
- 結果画面の背景ちらつきを止め、OSの動き軽減を反映した。
- モンスター、背景、2D素材そのものは描き直していない。

## C05: 出題範囲と戦闘難度（G05、G06）

- 世界編は学年全件より先に各ステージの`kanjiPoolIdList`を使う。40通常ステージのプールは14〜59字、平均28.1字になり、変更前の学年全件（185〜328字）から反復可能な単位へ戻った。
- 敵係数のステージ番号を全世界通算から「選択学年・級内の通常ステージ順」へ変更した。漢字の難しさは選択した学年のプールに残し、操作への慣れだけを学年内ステージ順で上げる。
- ボスシールドは学年内ステージ0〜2で1回、3〜5で2回、6以降で3回とした。音訓の弱点ルールは維持している。
- 難度版を`balance-2`とし、新しいベストタイムは`stageId@balance-2`へ保存する。旧キーと旧記録は削除・比較・補正しない。

前提は新規セーブ、Lv1、攻撃10、最大HP100。必要正解数は通常ダメージ10として計算し、ボスは「シールド破壊 + HPダメージ」を分けて示す。

| 開始学年 | 変更前 通常敵 HP/敵攻撃/必要正解 | 変更後 | 変更前 ボス HP/敵攻撃/必要正解+盾 | 変更後 |
| --- | --- | --- | --- | --- |
| 小1 | 31 / 9 / 4 | 31 / 9 / 4 | 62 / 11 / 7+3 | 62 / 11 / 7+1 |
| 小3 | 44 / 9 / 5 | 31 / 9 / 4 | 79 / 12 / 8+3 | 62 / 11 / 7+1 |
| 小6 | 81 / 11 / 9 | 31 / 9 / 4 | 130 / 14 / 13+3 | 62 / 11 / 7+1 |

## C06: 配信・キャッシュ・名称（E14、E15）

- 公開名称をHTML、manifest、READMEで「漢字ヨミタビ」へ統一した。
- 背景URLの`Date.now()`を廃止し、同じ公開版では`PUBLIC_ASSET_REVISION = 'v1'`を再利用する。
- Vercel/Firebaseの両方で、SWは`no-store`、データは再検証、ハッシュ付きJS/CSS/manifestは長期immutable、固定名の画像・音声は1日 + stale-while-revalidateとした。
- 旧Service Workerの退役は、同一オリジンの`/sw.js`と本作の既知キャッシュ名だけを対象にした。localStorage、IndexedDB、認証Storage、別アプリのキャッシュは削除しない。
- READMEにオンラインWeb版であり、オフライン保証のPWAではないことと更新規則を記載した。

## 既存セーブへの影響

- セーブスキーマ、漢字ID、ステージID、復習項目、学習記録、レベル、経験値、HP、攻撃力、収集、解放、チェックポイントは変更していない。
- 難度は新しく戦闘へ入る時、またはチェックポイントから再入場する時に計算する。進行中の敵状態を途中で書き換えない。
- 旧ベストタイムはそのまま残し、新難度の記録だけ別キーへ保存する。
- プレイ時間は今後の非表示・長時間復帰分を加算しない。過去値を推測で減算しない。
- 世界編のステージ外になった既存復習項目は、全体復習キューから削除しない。

## 追加した自動テスト

- 学年1・3・6直行時の敵能力、必要正解数、段階シールド、版付きベストタイム。
- 世界40通常ステージの明示プールと、ステージ定義優先順。
- 30/60/120fps相当、非表示・長時間復帰、プレイ時間除外。
- 390px縦持ちと横長の4:3座標、44/48px入力寸法。
- 起動画像1件、同時JSON読込共有、既存ローカルセーブのクラウド非待機。
- 名称、同期Firebase script不在、SW/Cache退役範囲、背景URLの固定版。

## 検証結果

| 検証 | 結果 |
| --- | --- |
| `npm.cmd run test:phase-a` | 86/86 PASS |
| `npm.cmd run test:phase-b` | 22/22 PASS |
| `npm.cmd run test:phase-c` | 17/17 PASS |
| `npm.cmd run build` | PASS、89 modules |
| `git diff --check` | エラーなし（既存の改行形式警告のみ） |

ビルドには既存の動的・静的import重複警告と、main chunk 519.13kBのサイズ警告が残る。全面的な分割はE16のv1対象外なので今回行っていない。

## PHASE Dへ残す確認

- ブラウザ操作環境がこのセッションでは利用できなかったため、iPhone、iPad、Android、Windows日本語IMEでの実表示・実タップ・回転・ソフトキーボード・50音パッド確認はD02で行う。
- Vercel/Firebaseへは公開していないため、実配信ヘッダー、冷たいキャッシュ、旧SW端末、ホーム画面起動はD03で確認する。
- 代表ステージを実際にボスまで通した所要時間と、誤答2.2秒が子どもに十分かはD02の観察で確定する。
