**漢字ヨミタビ — 回帰監査で再現された公開阻止要因の修正報告**

確認日：2026-09-06。対象：`YOMITABI_V1_REGRESSION_AUDIT.md`全文、とくに「公開候補へ進むための再判定条件」1〜6。これは修正とローカル検証の報告であり、公開可否の再判定ではない。実機等を必要とする条件7は未完了。

**作業前の状態と変更の扱い**

開始時に `git status`、branch、diffを確認した。branchは `main`、HEADは `d58c4ce116ba4b029de5fb36ebfb4629ed5a89f6`。PHASE A/B/Cの未コミット変更・未追跡ファイルが存在していたため、それらを土台に今回の修正を加えた。reset、revert、commit、push、デプロイは行っていない。今回、教材JSON・漢字ID・アセット・難易度係数は変更していない。

NEWの再現テストを先に追加して失敗を確認し、実画面処理を最小修正して成功を確認した。周辺確認で見つけた共通処理の引数受け渡し、動き軽減の途中切替、クイズ回答ボタン、チュートリアルの黒帯座標、練習の再試行案内の文字サイズについても、失敗する実行テストを追加してから修正した。既存PHASE A/B/Cテストの期待値は変更していない。

編集中、練習画面ファイルの一部を編集スクリプトで誤って欠落させた。直ちに検出し、作業前の差分記録とHEADから該当部分を復元したうえで、既存125件と今回の実画面テストを再実行した。一時的な欠落をそのまま成果物にはしていない。

**1. NEW-01〜NEW-06の修正結果**

| ID | 修正 | 修正後に実行確認したこと |
| --- | --- | --- |
| NEW-01 | 練習の誤答表示終了時に回答ゲートを解除し、同じ漢字への再回答に新しい問題トークンを発行する。表示済みの答えによる支援情報を引き継ぐ | 誤答→誤答表示→2,200ms終了→正答→1,100ms後の次問処理。誤答1件・正答1件。待機中と正答直後のEnter連打では増殖しない。再正答は `revealed` と記録 |
| NEW-02 | 完了画面のコマンドを解答の空欄判定より前で処理。続行／終了の描画と判定を共通矩形へ合わせる | 空入力Enterで「もう1もん」が成立し、入力可能な次問へ進む。「今日はここまで」でステージ選択へ戻り、学習記録は増えない。実際に有効な `update()` が両方の選択を描画する |
| NEW-03 | 通常戦闘の正答・誤答は学習記録の保存を先に確定。失敗時は入力とゲートを戻し、エラーを表示する | 攻撃／回復それぞれの正答・誤答にQuotaExceededを注入。正規セーブ原文、combo、正誤リストを保持し、次処理を予約しない。解除後の再回答で保存記録と対応する表示リストが各1件だけ確定 |
| NEW-04 | クイズの `stats`、`answers`、フィードバック確定、次問予約を保存成功後へ移動 | 保存失敗時は `correct=0`、`answers=0`、予約0。再試行成功後は `correct=1`、`answers=1`、正規セーブの正答1件、次問予約1件。再送信では増殖しない |
| NEW-05 | 復習のクリックを既存 `getGameCoordinates()` に統一。描画と判定に同一矩形を使用 | 390px幅・黒帯ありの表示をモデル化し、戻る／回答決定の中央と四隅付近を実ハンドラーへ送った。5点すべて同じ操作になり、回答の重複タップ・IME変換中のタップは記録を増やさない |
| NEW-06 | grade7〜10の欠落を別学年で代用しない。必須教材の取得・JSON本文に期限を設け、失敗した初期化を成功として公開しない | grade7のみ失敗、必須JSON失敗、fetch reject、fetch保留で失敗状態へ進み、grade7の代用集合は空。取得を正常に戻して再試行すると、88通常ステージの各問題集合が定義と一致 |

NEW-01〜06の上表の再現条件は、修正後の実行テストで解消した。実ブラウザーや実Firestoreまで合格したという意味ではない。

**2. 関連するP1の対応**

| 監査ID | 今回の対応・確認 | 検証の限界 |
| --- | --- | --- |
| E03 | 通常戦闘・練習・クイズの保存失敗後の再試行を確認。タイトルの実保存ボタンも失敗時は失敗通知、解除後は成功通知を1回表示 | Storageは障害注入可能なメモリ実装。実端末の容量管理・強制終了との組合せは未確認 |
| E04 | 起動時に原本を保持して再読込／設定のバックアップ読込へ誘導。不正JSON等の `corrupt` 状態では、ユーザーが選んだ場合だけ完全なクラウド記録から復元できる。復元前にローカル原本を退避 | 未知バージョンや原本と確認済みコピーの競合を自動上書きしない。競合は設定のバックアップ読込へ進む。実クラウド復元は未確認 |
| E06 | fetchとJSON本文・認証・クラウド復旧・ステージ素材待ちに10秒の期限。SDKスクリプトは1ファイル8秒、失敗した要素を除去して再試行可能にする。名前のローカル確定後はクラウドを待たず進む | 起動全体を10秒以内に保証するものではない。各段階の期限であり、段階が直列なら加算される |
| E08 | 画面ライフサイクルにrAFの管理を追加。戦闘・練習・クイズの入場処理、撃破後処理、レベルアップinterval、ステージ読込、復旧待機の完了処理を離脱時に無効化 | 指定された経路の局所実行確認。アプリ全画面・全外部イベントの網羅証明ではない |
| E09 | 共通のIME判定を維持し、練習完了コマンドにも適用。通常戦闘・練習の実バインドイベント、復習のcomposition中タップ、クイズの変換中Enterを確認 | 合成イベントによる検証。日本語IME固有の実イベント順序は実機確認が必要 |
| T01 | 世界編を「漢検4級〜2級」と説明し、今回のゲーム行動・登録読みの範囲に合わせた | 全教材の教育的校閲は含まない |
| T03 | 誤答後の正規の再回答と二重送信を分離。既存復習項目の再誤答・戦闘内再出題はPHASE Bで再確認。復習0件の案内と離脱時タイマー取消も実行 | 実機の長時間連続プレイは未確認 |
| T04 | 支援なし／ヒント1〜3／答え表示後の観測を維持。誤答後に答えを見た練習の再正答を自力へ格上げしない。クイック復習へも同じ採点結果を渡す | 過去の支援情報がない記録を推測して分類し直していない |
| T05 | 図鑑・週次・クイズ結果を正解入力と今回確認した字の範囲で表現 | 支援を含むゲーム上の成功・称賛は維持 |
| U01 | 共通矩形とCSS実寸を用いたボタン、入力欄の配置、復習・戦闘・練習・クイズの主要操作を修正 | 390px／768pxの座標・描画検査は実行。実CSSエンジン・キーボード表示下の見え方は未確認 |
| U04 | チュートリアルの操作説明と案内位置を現在のボタンへ合わせ、黒帯を共通座標変換で扱う。練習の再試行案内は390pxでも16 CSS px相当 | 文字拡大、ルビ、画面全体の可読性は実機確認が必要 |
| U05 | Canvas shake、全画面flash、レベルアップshake、盾の脈動・ひび演出、練習の成功粒子、経験値粒子・バーの光粒を動き軽減設定で抑制。正誤・レベルアップの静的情報を残す | 常時粒子のタイトル側抑制は既存Cを維持。実画面での動き・点滅の最終確認は未実施 |
| G07 | マスターをゲームの達成マークと説明し、すべての文脈・読み能力の習得を断定しない。ボーナス説明を通常面クリア＋各面の練習完了に合わせる | 解放条件や既存達成データ自体は変更していない |
| G08 | 練習完了時に「もう1もん／今日はここまで」を提示し、どちらも回答採点とは別操作にする | 子どもが選択を理解できるかの観察は未実施 |

ヒントと「おしい」判定は残した。保存失敗した解答を画面だけ成功扱いにせず、保存できた解答に対するゲーム上の成功と学習記録上の意味を分けた。

**3. 今回変更したファイル**

以下は今回手を入れたもの。Git全体のdiffには既存PHASE A/B/Cの変更も含まれるので、その全量を今回の修正とは扱わない。

| まとまり | ファイル |
| --- | --- |
| 回答・画面寿命・復旧 | `src/core/answerSubmission.js`、`src/core/screenLifecycle.js`、`src/core/saveBootstrap.js` |
| 外部処理の期限 | `src/core/asyncDeadline.js`（今回新規） |
| 教材・素材 | `src/loaders/dataLoader.js`、`src/loaders/assetsLoader.js` |
| 起動・クラウド | `src/main.js`、`src/services/firebase/sdkLoader.js`、`src/services/firebase/firebaseController.js`、`src/services/firebase/cloudSave.js` |
| 通常戦闘・練習・復習 | `src/screens/battleScreen.js`、`src/screens/battle/theme.js`、`src/screens/practiceBattleScreen.js`、`src/screens/quickReviewPracticeScreen.js`、`src/screens/reviewStage.js` |
| クイズ・名前・タイトル・読込 | `src/screens/gradeQuizScreen.js`、`src/screens/playerNameInputScreen.js`、`src/screens/titleScreen.js`、`src/screens/stageLoadingScreen.js` |
| 学習表示 | `src/screens/Dex/kanjiDexScreen.js`、`src/screens/profileScreen.js`、`src/screens/stageSelectScreen.js`、`src/tutorial/tutorialData.js` |
| 共通UI | `src/ui/learningControls.js`（今回新規）、`src/ui/uiRenderer.js`、`src/ui/bootProgress.js` |
| テスト実行定義 | `package.json`：`test:no-go`を追加 |
| 新規テスト・報告 | 下記6ファイル、本書 `YOMITABI_NO_GO_FIX_REPORT.md` |

**4. 正式に追加した回帰テスト**

| ファイル | 件数 | 主な実行対象 |
| --- | ---: | --- |
| `tests/no-go/screens.test.mjs` | 20 | 実練習ハンドラーの誤答→再回答・完了コマンド、攻撃／回復／練習／クイズの保存失敗→再試行、ヒント0・1・4の観測、空欄・近似入力・IME、正誤・撃破直後の実exit |
| `tests/no-go/mobile.test.mjs` | 9 | 実復習クリックの中央＋四隅、回答とcomposition、復習0件、練習完了の実update、戦闘の描画と判定、390px／768pxの5点操作、クイズの回答ボタンと入力の間隔、練習案内の文字実寸 |
| `tests/no-go/loading.test.mjs` | 7 | grade7のみ失敗、必須JSON失敗、reject、未settle→再試行と88問題集合、SDK失敗再試行、ロゴImage保留、stage loading期限と離脱後の遅いImage完了 |
| `tests/no-go/lifetime.test.mjs` | 6 | タイトル実保存ボタン、レベルアップinterval、解放済みCanvas、認証失敗／保留、名前確定時の保留クラウドと遅い完了 |
| `tests/no-go/recovery.test.mjs` | 7 | 明示的破損復旧、空／未来版／容量不足による拒否と原本保持、通常起動での暗黙復元禁止、復旧中離脱、タイムアウト後の遅い成功 |
| `tests/no-go/presentation.test.mjs` | 5 | 実チュートリアルの文言・黒帯座標、表示文言の範囲、実演出メソッドの動き軽減と途中切替 |
| 合計 | **54** | 失敗・skip・取消・todoはいずれも0 |

Storage、時計、DOM、Canvasコンテキスト、Firebase応答を隔離したNodeテスト。解答・描画・イベント・状態遷移は可能な限り実画面モジュールを呼ぶ。描画記録はスクリーンショットではなくCanvas API呼出しの観測であり、実ブラウザーの代替合格証拠ではない。文言そのものを確認する1件のみソース文言検査を含む。テスト用の架空保存を使い、実ユーザーの保存領域は操作していない。

**5. 全テスト結果・主要数値**

| コマンド | 最終結果 |
| --- | --- |
| `npm.cmd run test:phase-a` | 86 / 86 PASS |
| `npm.cmd run test:phase-b` | 22 / 22 PASS |
| `npm.cmd run test:phase-c` | 17 / 17 PASS |
| `npm.cmd run test:no-go` | 54 / 54 PASS |
| 合計 | **179 / 179 PASS** |
| `git diff --check` | PASS |

PHASE Aには容量不足、不正JSON、未知版、空データ、スロット失敗、復元失敗、復習あり→空への復元、5体目チェックポイント、旧セーブ・ID移行の検査を含む。既存のID重複・ステージ漢字参照切れ・敵参照切れ0の条件を維持した。今回の正常時／取得再試行後の88通常ステージの問題集合不一致も0。

390px幅・内部800×600・contain表示の場合、倍率は0.4875。44pxを満たすために内部高さ91とし、実寸は **44.3625 CSS px**。戻るは幅112.125px、戦闘3操作／復習回答は幅117px、練習への移動は幅126.75px、練習完了の各選択は幅180.375px。従来の復習戻る100×30は48.75×14.625pxだった。

768px幅・1024px高のモデルでは倍率0.96、共通高さ48は **46.08 CSS px**。この寸法でも戦闘主要5操作の中央＋四隅を実ハンドラーで確認した。共通のHTML入力欄は高さ48 CSS px。これらは与えた表示矩形とCanvas描画から計算・検証した値であり、実機測定値とは区別する。

誤答フィードバック2,200ms／正答後1,100msはPHASE Cの値を維持。敵HP、必要ダメージ、音訓ルール、ボスシールド、バランス版、既存ベストタイムの扱いは変更していない。

**6. 本番ビルド**

`npm.cmd run build -- --outDir C:/Users/socce/AppData/Local/Temp/yomitabi-no-go-final-71cd49b2f9f34c17bb738548a2406b97`：成功。

Vite 5.4.19、91 modules。mainは522.99kB／gzip156.51kB、CSSは48.35kB／gzip8.73kB。500kB超チャンク、静的／動的importの混在、Vite CJS APIの非推奨警告は残る。今回、警告を消すための分割・設定緩和はしていない。既存distと配信キャッシュを上書きしない一時出力先で確認した。

**7. 保存互換性への影響**

- セーブ形式v2、カタログ版、漢字ID、スロット形式、バックアップ形式は変更していない。既存記録の初期化・推測分配・支援履歴の捏造はしない。
- 保存に失敗した解答は、その解答による学習カウント・成功リスト等を確定せず、同じ問題から再試行できる。保存成功後に画面の成功処理を行う。
- 練習で誤答後に表示された答えを再入力した場合、今後の記録は新しい正規の学習行為1件として残す。答え表示済みの正答であることを既存の観測項目 `revealed` に記録し、自力正解とは分ける。
- 明示的な破損クラウド復旧では、検証できた完全なsnapshotだけを使う。欠けた旧summaryから破損ローカルを補完しない。ローカル原本退避が容量不足等でできなければ復元を中止する。未知版・競合を勝手に上書きしない。
- 名前確定はローカル保存成功を基準に先へ進む。クラウドの保留・失敗で画面を停止させず、遅い完了で前画面に戻さない。クラウド同期完了までを保証する変更ではない。

**8. 未解決・今回の確認範囲外**

ローカルで対象の再現条件は解消したが、監査37項目を全面的に再監査したわけではない。とくに以下は未完了として残す。

- 破損記録に有効なクラウドsnapshot／バックアップがない場合のデータ復元。原本を保持し、再試行／設定へ誘導するところまで。推測による復元は行わない。
- 実Firestoreの競合、スロット分離、認証保持、アクセス制御。今回のテストは応答スタブを用いたローカル処理確認。
- 公開先の実ヘッダー、旧Service Workerからの更新、ホーム画面起動、学校回線相当の初回読込。
- 全読み・全例文等の教材校閲と、小1・小3・小6の実通しプレイ。
- 前項のビルド警告。今回の公開阻止要因修正とは切り分けて残した。

**9. 実機でしか確認できない事項**

Browserスキルを使って接続を確認したが、利用可能なブラウザーは0件だった。したがって、以下について合格とは記載しない。

1. iPhone／iPad／Android／Windowsで、390px幅・対象タブレット・縦横切替・文字拡大・かなパッド／ソフトキーボード表示時に、問題・入力・決定・戻るが実際に同時表示され操作できること。
2. 日本語IMEのcompositionイベントとEnter、変換候補確定、タップ確定の実イベント順序。
3. OSの動き軽減設定によるCanvas／CSSの実表示、点滅、音・読み上げ・自動再生制約。
4. 子どもが誤答後に再回答し、ヒントの支援と今回の結果を理解し、自分で終了できること。

今回はここまでを修正成果として提出する。GO／NO-GOの最終判定、3D化、PROMPT相当の次工程には進んでいない。
