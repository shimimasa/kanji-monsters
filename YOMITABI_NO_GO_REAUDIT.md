# 漢字ヨミタビ NO-GO 再監査

監査日：2026-09-06（JST）  
対象：`main`、HEAD `d58c4ce116ba4b029de5fb36ebfb4629ed5a89f6` **に作業ツリーの変更を含めた現在の実装**。  
立場：独立QA。実装修正は実施していない。

## 最終判定：CODE NO-GO

**NEW-01〜NEW-06は、今回再注入した以前の障害条件についてすべてFIXED。しかし、E08に関連するP1を現在のコードで再現したため、コード全体の判定はCODE NO-GO。**

練習に入ってからチュートリアルの動的importが完了する前に離脱すると、離脱済み練習のチュートリアルが後から開始される。実際のTutorialManager／TutorialGuideまで実行し、`position:fixed; width:100vw; height:100vh; zIndex:100002; pointerEvents:auto` の幕が追加されることを確認した。前画面の処理が次画面の主要操作を覆う。閉じれば復帰できるが、「離脱後のPromiseが新画面へ干渉しない」というP1完了条件を満たさない。

既存P0のE01・E02について、元セーブ消失の再発は今回の障害注入では認めなかった。通常戦闘・クイズの保存失敗再試行も回復している。

実機・実IME・実Firebase・公開環境・子どもの試遊は未確認。**これらの未確認をNO-GOの理由にはしていない。** 本判定の阻害要因は、下記REAUDIT-01のコード上の再現である。

## 監査方法と証拠の範囲

- `YOMITABI_QUALITY_AUDIT.md`、`YOMITABI_V1_REGRESSION_AUDIT.md`、`YOMITABI_NO_GO_FIX_REPORT.md`を読み、過去の症状・完了条件・変更範囲を抽出した。「修正済み」「過去のPASS」は今回の合格証拠にしていない。
- 現行の回答ゲート、画面の実ハンドラー、保存／復元、教材取得、ライフサイクル、Canvas配置・演出・学習表示、および現行テストを確認した。全コード・全教材の逐語校閲ではない。
- Node.js v22.14.0で実モジュールを実行。Storageは架空データのメモリ実装、DOMはEventTargetと要素の代用品、CanvasはAPI呼出し記録、時刻は制御可能なtimeout／interval／rAFを使った。実ブラウザーの描画・タッチ測定とは区別する。
- 独立検証では、回答の見た目処理を既存テストのように一括で空関数に置き換えず、実画面処理を通した。Promiseによる遅いチュートリアル開始は実際の動的importの完了を待って観測した。
- テストの検出力確認は、一時ディレクトリのNode ESM loaderで**読み込む文字列だけ**を反事実的に変換した。リポジトリのソース・既存テストは書き換えていない。過去版そのものの復元ではなく、旧障害と同種の処理を戻した対照実験である。
- `src/`、`tests/`、`public/data/`は開始時のSHA-256と照合し、変更0。既存の未コミット変更は保持した。ビルド先も一時ディレクトリで、既存`dist/`を上書きしていない。

本書のFIXEDは「記載したコード上の障害条件を再実行し、期待する動作を確認した」の意味。全端末・全経路・実サービスの合格認証ではない。

## NEW-01〜NEW-06の独立判定

| ID | 判定 | 障害再注入と今回の観測 | 現行コードの根拠 |
| --- | --- | --- | --- |
| NEW-01 | **FIXED** | 実練習の入力購読へ「ねこ」→2,200ms→「いち」。さらに誤答2回→正答、待機中・正答直後の連打も実行。誤答2／正答1だけ保存。正答は`revealed`、1,100ms後に入力可能。ロックと問題トークンの両方が更新される | `practiceBattleScreen.js:343`の購読、`:833`付近のcommit、`:1096`の誤答後処理 |
| NEW-02 | **FIXED** | 完了時の空入力Enterを既存実行テストで再確認。独立に空入力の`yomitabi:submit`も実DOMイベントとして送信し続行。実クリックで「今日はここまで」→stageSelect。続行・終了による正解記録増加0。通常回答の空欄拒否を維持 | `answerSubmission.js:33`のcommand、`practiceBattleScreen.js:349`付近、`:2232`以降 |
| NEW-03 | **FIXED** | 攻撃／回復×正答／誤答の4経路。以前と同じジャーナル書込Quotaは既存画面テストで再実行。独立には`krb_save`書込を継続失敗させ2回再試行後に解除。失敗中は原文・HP・敵HP・正誤リストを保持、入力復帰、ゲート解除。解除後は同じ問題を1件だけ記録 | `battleScreen.js:4798`付近の`reportAnswerSaveFailure`、攻撃・回復でcommit後に成功処理 |
| NEW-04 | **FIXED** | 学年クイズの正答／誤答に書込失敗を2回注入。失敗中はanswers=0・次問予約=0・原文不変。解除後、購読経由の再回答＋連打でanswers=1・対応する正誤1・予約1。既存テストでも保存と画面の一致を確認 | `gradeQuizScreen.js:234`以降で保存がstats／answers／予約より先 |
| NEW-05 | **FIXED** | 390px幅で実review.enter／updateの描画矩形を取得し、その中心＋四隅内側をCSS座標へ独自計算して実クリックへ送信。5点すべて戻る。黒帯のクリックでは追加遷移0。既存テストの逆変換関数に依存しない検証でも成立 | `reviewStage.js:83`、`:178`付近、`coordinateUtils.js`、`learningControls.js` |
| NEW-06 | **FIXED** | grade7だけ503／JSON解析例外／JSON本文保留を別々に注入。現在の実ローダーは失敗を返し、grade7=0、別学年への代用なし。本文期限後の遅い成功も旧集合を公開しない。通信を正常化して再実行すると通常88面の定義ID集合と実出題集合が一致 | `dataLoader.js:61`以降、`:190`以降の整合性検査と失敗時初期化、`asyncDeadline.js` |

この6件のFIXEDと、E08残件による全体NO-GOは矛盾しない。前回の6つの症状は解消したが、それ以外の必須完了条件に再現可能なP1が残っている。

### 390pxの座標検証の詳細

独立検証はCanvas内部800×600、DOM矩形`left=13, top=27, width=390, height=700`。contain表示の倍率は0.4875、上下の黒帯は各203.75 CSS px。

実描画で得た戻る矩形は内部`[20,20,230,91]`。中心のイベント座標はCSS `(78.8125, 262.68125)`。四隅内側は内部座標を各辺から2だけ内側へ取り、次式で変換した。

```text
clientX = 13 + internalX × 390 / 800
clientY = 27 + (700 - 600 × 390 / 800) / 2 + internalY × 390 / 800
```

ボタン実寸は幅112.125×高さ44.3625 CSS px。`getGameCoordinates`へ入る実クリックハンドラーで判定した。独立検証では、以前と同じ黒帯なしの390×292.5・左上(0,0)でも5点を再実行し、5点すべて戻った。ただし物理画面でタップした結果ではない。

## 必須再検証10項目

| 要求 | 結果 | 証拠・限界 |
| --- | --- | --- |
| 1. 練習：誤答→再回答→正解 | **FIXED** | NEW-01。誤答後の正答を自力に格上げしないことまで確認 |
| 2. 練習完了：空入力で続行／終了 | **FIXED** | NEW-02。Enter、かなパッド用カスタム送信イベント、完了ボタンクリック。Canvasの有効なupdateが両選択を描くことも既存実行テストで確認 |
| 3. 通常戦闘：保存失敗→入力復帰→再試行 | **FIXED** | NEW-03。攻撃・回復、正答・誤答、原文・メモリ・画面カウントを照合 |
| 4. 学年クイズ：保存失敗→再試行 | **FIXED** | NEW-04。失敗を複数回挟んでも1回答・1予約 |
| 5. タイトル：保存失敗時に成功表示しない | **FIXED** | 実`_injectSaveButton()`のonclickへ、ジャーナルQuotaと正規セーブQuotaを別検証で注入。独立検証では連続2失敗で成功通知0、失敗通知2、解除後の成功通知1。変更予定の名前も入れ、書込が実際に必要な条件で実行 |
| 6. 復習：390pxの見える戻るボタン | **FIXED** | NEW-05。描画を観測して別実装の式でイベント座標を計算。単に内部座標でクリックしていない |
| 7. 教材取得：grade7失敗時に他学年fallbackしない | **FIXED** | NEW-06。既存テストには後述の検出漏れがあるため、独立実行結果を主証拠とした |
| 8. 離脱：levelUp／timeout／interval／rAF／Promise | **PARTIALLY_FIXED** | レベルアップの実exitでinterval解除・transform復元を確認。通常戦闘の正答／誤答／撃破後timeout・rAF、復習の空状態timer、名前の遅いクラウド完了、ステージ素材・復旧待機の遅い完了は現行テストで抑止を確認。しかし練習入場時のPromiseはREAUDIT-01を再現 |
| 9. reduced motion：Canvasにも反映 | **FIXED（重点経路）** | 実levelUp開始でinterval=0、静的levelUp情報はactive。石版shake・練習成功粒子・経験値粒子を抑制。既存粒子と動作中levelUpの設定切替でも停止。Canvas updateも実行。盾の脈動・ひび・levelUpの点滅分岐は静的確認。全演出・実OS表示の網羅とはしない |
| 10. 自力／支援込み／今回範囲の意味 | **FIXED（重点表示）** | ヒント0〜4の5正答を実commitし、合計5・自力1を確認。実図鑑セクションと実プロフィールで「5回」「ヒントを含む」を確認。実クイズ結果描画は10問、正解入力8、自力5。誤答後再回答はrevealed、旧累計のCSVは自力へ推測配分されない。ボーナス結果等に残る表現上の差は後述 |

8のtimeout／rAF検証は、実画面exit後に捕捉済みのguard付きコールバックも呼び、次画面の問題・ターンを書き換えないことを確認する既存テストを再実行したもの。画面全域の非同期処理が安全だという証明にはならない。

## 既存P0：E01・E02

### E01：FIXED

独立Storageに子Aの名前・学習記録・`hokkaido_area1:5`の旗・復習キュー、別スロットに子Bを用意。以下の**5地点それぞれに継続的QuotaExceededError**を注入し、実`switchToSlot(2)`を呼んだ。

| 失敗地点 | 切替結果 | 子Aの正規セーブ | 復習キュー | 行先の控え | 現スロット |
| --- | --- | --- | --- | --- | --- |
| `yomitabi_phase_a_pending` | false | 原文一致 | 一致 | 一致 | 1 |
| `yomitabi_slot_data_1` | false | 原文一致 | 一致 | 一致 | 1 |
| `krb_save` | false | 原文一致 | 一致 | 一致 | 1 |
| `yomitabi_slot` | false | 原文一致 | 一致 | 一致 | 1 |
| `yomitabi_storage_epoch` | false | 原文一致 | 一致 | 一致 | 1 |

既存phase-aでは1→2→1の正常往復、破損した行先、復元途中・スロット番号書込失敗、無関係設定の保持も再実行。元のP0「退避できないのに現在データを削除する」は再現しなかった。実端末の強制終了や複数タブまで保証する結果ではない。

### E02：FIXED

- 不正backup相当の`{}`、null、配列、version999、player型不正を、設定画面と同じ`saveNow(data,{replace:true})`入口へ与えた。全拒否、元セーブ原文一致。
- 壊れたJSON、空文字、JSONのnull、未知版の正規saveへ`loadSave()`と`loadGameData()`を実行。原文保持、正常起動として採用しないことを確認。
- phase-aの復元ミラー失敗・原本退避失敗・トランザクション復旧、復習あり→正常な空backupの復元も再実行。空の有効saveと不正な空JSONを区別している。
- no-go/recoveryでは明示的な破損クラウド復旧、空／未来版／容量不足による拒否、離脱・期限切れ後の遅い成功による原本保持を再確認。

有効な復元元がない破損記録を修復できるという意味ではない。壊れた原本を初期値で失わせないことが今回のP0確認対象である。実際のファイル選択UI・実Firestoreは使っていない。

## 新たに再現した残件

### REAUDIT-01：NEW／P1 — 練習離脱後に旧チュートリアルが次画面を覆う

関連：E08、U04、G08。E08の部分修正に残った漏れとして扱う。前回未報告の具体経路であり、過去版との同一条件比較がないため「今回の変更が新規に作った回帰」とまでは断定しない。

**現行の原因**

1. `src/screens/practiceBattleScreen.js:100`で既存`this._lifecycle`をactivateする。
2. 同`:151`で、そのライフサイクルのguardを付けてTutorialManagerのimport完了を予約する。ボーナス背景の`:136`にも同じ所有関係がある。
3. 同`:163`の`battleScreenState.enter.call(this,canvasEl)`が、`src/screens/battleScreen.js:815`で**新しいライフサイクルを代入**する。置換前のライフサイクルをdeactivateしていない。
4. 練習exitの`:2537`と親exitが止めるのは置換後のライフサイクル。置換前のguardはactiveのまま。
5. 遅いimport完了で`startIfNeeded('practiceBattle',...)`が呼ばれ、`TutorialManager.js:38`で既存guideを破棄し、`TutorialGuide.js:24`以降の全画面DOMを追加する。

**再現条件・手順**

- メモリStorageに正常セーブを用意。tutorialEnabled／tutorial_seen_practiceBattleは未設定（初回の既定条件）。ローカルJSONを正常取得し、通常ステージとCanvas／入力を用意する。
- 実`practice.enter(canvas)`を実行。初期化失敗・alertがないことを確認する。
- 動的importのPromise完了を待たず、実`practice.exit()`を実行し、次画面の状態にする。
- Promiseの完了を待つ。最初は実`startIfNeeded`から`_start`へ到達した回数を観測。次にその観測用置換も外し、実`TutorialGuide.createGuide`まで実行した。

```text
practice._active          false
practice.canvas           null
currentStageId            new-screen（次画面用マーカー）
遅れて開始したguide        practiceBattle × 1
離脱後に追加された全画面幕  1
position                  fixed
width / height            100vw / 100vh
zIndex                    100002
pointerEvents             auto
```

DOM構築を観測したコード再現であり、ブラウザーのスクリーンショットではない。次画面の全描画処理は代用しているが、遅い処理が追加する要素の大きさと入力遮蔽指定は実コードの値。FSMは`uiOverlay`を掃除する一方、この幕はbodyへ後から追加されるため、その掃除で防げない。ビルドでもTutorialManagerは別チャンクになっており、取得完了待ちの間に離脱する条件は実配信でも起こり得る。

**P1とした理由：** 画面離脱後に前画面の説明が主要操作を遮り、別guideがあれば破棄する。単なる非表示オブジェクトの更新ではない。閉じる操作で復帰可能であり、永続的な入力不能・データ消失とは区別する。

**再判定に必要な確認：** 練習・短期復習でライフサイクルを置換する前後を含めて、全予約の所有期間を一致させること。実enter→実exit→遅いimport／背景完了と、離脱直後の再入場をテストし、旧guide開始・旧背景反映・新画面へのDOM追加が0になること。今回は修正していない。

### REAUDIT-02：NEW／P2 — grade7回帰テストに別原因の失敗が混ざり、旧障害を見逃せる

対象：`tests/no-go/loading.test.mjs:6`以降、特に固定30回のmicrotask待ちと`:21`付近の11,000ms進行。

既存テストは「30回Promiseを待つ→タイマーを11秒進める→結果null」を確認する。ローダーの段階的処理が完了する前に、grade7以外の処理を期限切れにできる。失敗理由・対象URLをassertしていない。

次の2対照実験を実行した。

1. **grade7への503注入だけを無効化**し、すべてローカル正常応答にした読込実験でも、既存の「grade7 fails explicitly」テストはPASS。実際に捕捉された理由は「読み込みが時間内に終わりませんでした」。grade7の503を証明していない。
2. メモリ上で「上級学年取得失敗→grade6を代用」と「最終カタログ／ステージ参照検査を外す」を組み合わせた対照でも、既存NEW-06の4テストはPASS。その同じ対照ローダーを**タイマーを強制進行せず**実行すると、`success:true / grade7:191`になった。旧障害と同種の誤成功を既存テストが検出できない。

上級fallbackだけを戻した場合には、現在の最終カタログ整合性検査が防御する。この単独変異の生存だけをテスト欠陥とは判定していない。上記の正常応答対照と、実際に誤成功する複合対照により限界を確認した。

**現在の製品ローダーにgrade7代用が残るという意味ではない。** 変換なしの独立実行は`success:false / grade7:0`で、503・不正JSON・本文保留・復帰も正しく処理した。NEW-06のFIXEDはこの独立証拠に基づく。REAUDIT-02だけをCODE NO-GOの理由にはしていない。

## テストの質：何を検出できたか

既存の件数を品質点数にはしていない。各アサーションが旧失敗を観測するか、実処理をどこまで通るかを評価した。

| テスト群 | 検出力の確認 | 限界 |
| --- | --- | --- |
| no-go/screens NEW-01 | retry unlockをメモリ上で除くと、再回答acceptedのassertがFAIL。新しいトークン発行も現行ソースと独立の誤答2回→正答で確認 | 継承元のenterは通さない。画面の入場所有権漏れは検出できない |
| NEW-02 | 空文字commandを拒否する対照で空入力続行テストがFAIL | 完了状態を直接作るテストだけでは、実際の最終問題からの遷移全体は保証しない。描画検査も別途必要 |
| NEW-03 | 保存失敗時の入力復帰処理を除いた対照で攻撃／回復×正誤の4件がFAIL | 既存fixtureは演出・ログ等を空関数にする。独立検証ではその置換をせず正規save書込失敗も確認した |
| NEW-04 | stats／answers更新をcommit前へ移す対照でFAIL | 既存は`_checkAnswer`直呼出し。独立に購読イベント＋複数回失敗も実行した |
| NEW-05 | raw client座標を直接内部座標として使う対照で戻る／回答テストがFAIL | 既存では順変換と逆変換を同じ共通処理に依存。独立に描画矩形と別計算式からイベントを作って補った |
| NEW-06 | 旧症状を再導入した複合対照が既存でPASSすることを確認 | REAUDIT-02。固定microtask回数でのclock進行を、目的の失敗原因の証明とみなせない |
| lifetimeのlevelUp | reduced時のinterval非生成、解放Canvasへの例外抑止を実行 | 既存の解放テストは`_active=false`等を直接設定し、実exitによる解除をassertしない。独立に実exitのinterval=0を確認 |
| screensの離脱 | 実battle.exitと保留timeout／rAFを実行し、問題・ターンの不変をassert | 通常戦闘の検査であり、practice.enter内の旧ライフサイクルは通らない。REAUDIT-01は検出漏れ |
| presentationの文言1件 | 図鑑・プロフィール・解放説明のソース正規表現 | 「文言がある」だけで、表示値や到達する描画経路は保証しない。独立に図鑑・プロフィール生成、クイズ結果描画を実行した |
| phase-b/screen-wiring、phase-cの一部 | 関数名・ソース順・CSS・SW文字列の検査 | 接続の手掛かり。実行順、入力復帰、実寸、Promiseの所有期間を保証しない |
| phase-aの保存・復元 | Quota、不正型、破損、未知版、ロールバック等を実保存モジュールへ投入 | 本物のブラウザーStorage・強制終了・サーバールールまでは対象外 |

反事実対照のFAILは構文エラーやimport失敗ではなく、対象の振る舞いのassert失敗であることをログで確認した。最初の実験で発生したWindowsのloaderパス指定エラーは修正してやり直し、検出力の証拠には数えていない。

## 旧37項目の変更影響確認

全面的な再採点ではなく、現在の変更範囲・共通依存から再実行した結果を示す。「悪化未確認」は全面合格を意味しない。

| 旧ID | 今回の確認・残る範囲 |
| --- | --- |
| G01 | 名前のローカル確定→クラウド保留でも遷移する実行試験PASS。初回30秒・子どもの単独開始は未測定。チュートリアルにはREAUDIT-01が残る |
| G02 | 練習2,200ms／1,100msの再回答・次問処理を実行。復習1,000msは現行コードに残る。テンポの適否は再評価していない |
| G03 | 旗5の保存→再読込→クリア時解除をphase-aで再実行。独立スロット障害でも旗を含む原文保持 |
| G04 | 復習0件の入力非表示・空案内・離脱時timer取消を実画面で再実行。選択画面の復習分岐を静的確認。未解放ボーナスへの迂回は確認されない |
| G05 | 503・不正JSON・本文保留と正常化後の88定義集合一致。NEW-06で独立確認 |
| G06 | phase-cの各学年先頭・成長後の難易度式を再実行。学年内順序の係数、旧タイム分離を維持。実戦時間・盾の理解は未確認 |
| G07 | 図鑑・週次の支援込み説明、結果の今回範囲を確認。本人が具体的上達を説明できるかは未観察 |
| G08 | 練習完了の空入力続行／終了と記録非増加を実行。短期復習のOK／キャンセル表現は残る。離脱の期待にはREAUDIT-01が関係 |
| T01 | 実tutorialデータで世界編の漢検4級〜2級、攻撃ボタン案内を確認。全例文・全教材の校閲は対象外 |
| T02 | phase-bで既存SRS項目の再誤答、期限切れ、候補外retry保持を再実行。練習のその場再回答はNEW-01で独立確認 |
| T03 | 練習・通常・復習・クイズの連打／空欄／近似入力と保存失敗を再実行。画面の件数・正規save件数の不一致は今回の条件では再現しない |
| T04 | 独立でhint0〜4の合計5・自力1、図鑑／週次生成、誤答後revealedを確認。CSVの旧記録非推測分類もPASS |
| T05 | 実クイズ描画で8／10と自力5を分離。図鑑マスターの範囲限定説明と解放文言を静的確認。ボーナス結果の表現差は残る |
| U01 | 390px／768pxの主要戦闘ボタン、復習、練習完了、クイズ配置を既存実行テストで再確認。独立390px戻るもPASS。小さい補助文字・画面全体の重なり・キーボード併用は未網羅 |
| U02 | titleの動き軽減分岐・ロゴ設定を確認。物理画面の第一印象は再評価していない |
| U03 | 共通ボタンは実描画で確認。青系復習／紺系完了等の差は残る。全画面を並べた視覚再監査なし |
| U04 | 実tutorialのヒント位置・黒帯座標、練習の保存失敗文16 CSS px相当を再実行。遅い旧tutorialはREAUDIT-01 |
| U05 | 重点Canvas演出と途中設定切替を実行。レベルアップ50ms shakeのreduced時再発なし。全演出の網羅評価ではない |
| U06 | 通常戦闘の見えるstage矩形と5点操作を再実行。旧back判定の割込みは確認されない。復習はNEW-05も解消 |
| U07 | 素材全体の実見は今回再実行していない。今回の入力・保存修正からの直接的影響は小さく、旧P3の仕上げ判断を新たな合格にしない |
| U08 | profileは収集件数を出し、現在数を分母にしたバーを呼ばないことを実生成とソースで確認。未使用makeBarの存在を再発と誤認しない |
| E01 | 継続Quota5地点＋既存正常往復・不正行先。原文保持。P0再発なし |
| E02 | 不正backup、破損／空／未知版save、退避失敗。原文保持。P0再発なし |
| E03 | タイトルと各回答画面の失敗・再試行を再実行。失敗を成功通知する以前の症状は再現しない |
| E04 | no-go/recoveryとphase-a/bootstrapを再実行。暗黙の破損上書きなし、明示的復旧と期限／離脱時原本保持。実クラウド未確認 |
| E05 | 復習あり→空backupのメモリ／queue／図鑑／旗置換、復元失敗巻戻し、クラウド文脈検査を再実行。実サーバー競合は未確認 |
| E06 | fetch／本文・SDK・認証・Imageの保留／拒否試験を再実行。名前のクラウド待ち停止は解消。教材取得の独立期限試験もPASS |
| E07 | phase-aカタログ／ID移行／参照検査、現行stage ID検査を再実行。ID衝突・参照切れの再発を検出せず。教材文章の正しさは別問題 |
| E08 | **PARTIALLY_FIXED。通常戦闘timer／rAFとlevelUp intervalは改善。実練習enterのPromise所有権漏れでP1残存（REAUDIT-01）** |
| E09 | 合成composition・isComposing・229と入力購読・タップ確定を再実行。空完了commandもIME判定後。実IMEの順序は未確認 |
| E10 | stageRunの二重確定防止、通常／別runクリア＋1をphase-aで再実行。結果画面の二重加算再発は確認されない |
| E11 | 起動画像限定・共有読込・並列数と画像保留期限のテストを再実行。学校回線での実転送量・cold startは未測定 |
| E12 | 旧曲フェードが新曲を止めないAudio代用品の競合試験を再実行してPASS。実聴は未実施 |
| E13 | frameClock・非表示復帰と敵演出時間のテストPASS。音声fadeの固定frame刻み等は残る。全演出のFPS独立は未達 |
| E14 | 名称・同期SDK除去・限定SW掃除の静的テスト再実行。実公開更新・旧SW端末・ホーム画面は未確認 |
| E15 | 背景固定版URL・配信設定の静的確認。m4aや深いURLを含む実ヘッダーは未確認。前回残件を完了扱いしない |
| E16 | 全suiteとビルドを実行。巨大画面・継承・暗黙eventBus契約は残る。REAUDIT-01はライフサイクルの責任分散、REAUDIT-02はテスト設計の具体的残件 |

### コード上の軽度残件と未確認を区別する

- `resultWinScreen.js:505`のボーナス結果は、支援込みのcorrectCountを「よめた漢字」と表現し、通常結果の「正解入力」と文言が完全には揃っていない。自力を断定する語はないため、今回これ単独をP1とはしない。T04／G07の表現統一残件。
- 復習の自動1,000ms次問、短期復習のOK／キャンセル、小さい補助文字、音声fade等の固定frame刻み、ビルド警告は残る。これらを未確認の実機障害へ勝手に格上げしていない。
- 旧P3の全モンスター仕上げ、全画面分割、全面TypeScript化、PWA再実装をGO条件に追加していない。

## 実行結果と再現資料

| 実行 | 結果 |
| --- | --- |
| `npm.cmd run test:phase-a` | 86 PASS |
| `npm.cmd run test:phase-b` | 22 PASS |
| `npm.cmd run test:phase-c` | 17 PASS |
| `npm.cmd run test:no-go` | 54 PASS |
| 独立追加検証 | 20実験中18期待どおり、2 FAIL。2つのFAILは同一のREAUDIT-01を開始境界と実DOM構築の2段階で確認したもの |
| 反事実対照 | NEW-01〜05は対象assertがFAIL。NEW-06の既存テストにはREAUDIT-02の見逃し。正常grade7応答対照でも既存テストPASS |
| `node scripts/verify_stage_id_integrity.mjs` | oldIdHits=0、参照⊆定義OK |
| `git diff --check` | 終了コード0。既存ファイルの改行形式警告あり |
| 本番ビルド（一時出力） | 成功。Vite 5.4.19、main 522.99kB／gzip156.51kB、CSS48.35kB、TutorialManager別チャンク9.34kB |
| 原本変更検査 | `src/`・`tests/`・`public/data/`の開始時SHA-256と一致、変更0 |

最初に全テストを一括実行した際、Node test runnerの`Unable to deserialize cloned data due to invalid or unsupported version`がaudio-lifecycleファイルのプロセス間通信で1件発生した。179のassertは通っていたが、その一括実行を完全成功とは扱わない。phase-bを正式コマンドで再実行して22 PASS、他のsuiteも正式コマンドで再確認した。アプリのバグとしては分類していない。

全既存179 PASSでもREAUDIT-01が再現したため、件数やビルドだけではGOにできない。

一時資料は次のディレクトリに保存した（リポジトリのテストには追加していない）。

`C:/Users/socce/AppData/Local/Temp/yomitabi-reaudit-bcbdd4428a5a4b0a8acb668b8ed74157/`

- `independent.mjs`、`independent.log`、`independent-results.json`：独立障害注入20実験。未改変の現行コードを実行。
- `mutation-loader.mjs`、`NEW-01-mutation.log`〜`NEW-06-mutation.log`：メモリ上の反事実対照と検出結果。
- `healthy-loading-control.log`：grade7正常応答でもPASSした対照、実失敗理由の記録。
- `grade7-control.mjs`：現行コードと反事実ローダーの誤成功比較。現行はfalse／0、対照はtrue／191。
- `before-hashes.json`、`existing-tests.log`、`phase-b-retry.log`、`phase-a-final.log`、`phase-c-final.log`、`no-go-final.log`、`build.log`。

独立検証の再実行：

```powershell
node --experimental-default-type=module C:/Users/socce/AppData/Local/Temp/yomitabi-reaudit-bcbdd4428a5a4b0a8acb668b8ed74157/independent.mjs
```

このスクリプトは全実験を最後まで観測するため、期待違反を捕捉してJSONへ記録する。**プロセス終了コード0を合格と読まず、各`result`と最後の集計を確認すること。** 現在の期待違反は練習離脱後チュートリアルの2観測である。一時資料はOSに削除され得るため、長期保存する場合は本書と一緒に保管する。

## 再判定条件

CODE NO-GOを解除するために最低限必要なのは、REAUDIT-01を現行画面の実enter／exitを通すテストで解消すること。さらにREAUDIT-02の試験を、grade7に到達して意図した障害が発生したことを確認する形に直し、正常応答対照・旧症状の対照を正しく区別できるようにすることを求める。

その後、今回FIXEDの6条件とP0保全を再実行する。実機・実サービス・子どもの試遊は公開品質の確認として別途必要だが、未実施だけを理由に次回のCODE GOを拒否するものではない。

